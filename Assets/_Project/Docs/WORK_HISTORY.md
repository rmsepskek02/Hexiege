# Hexiege - 작업 이력

**역할:** 완료된 작업의 시간순 기록  
**상세 문서:** 각 작업의 Research/Plan/Testcase → `_Tasks/YYYY-MM-DD/HH_MM_작업명/`

---

## 마일스톤 이력

| 날짜 | 마일스톤 |
|------|---------|
| 2026-08-17 | **로그 체계(`GameLog`) 전환 — 네트워크·인증 계층 정리 + 씬 무관 로그 수집 완료. 컴파일 통과 확인됨 · ✅ 실기 검증 전 항목 PASS (2026-08-17)** (커밋 `4e027e68` 마스킹+파일명 단일화+헤더 / `675203ae` 감사 누락 마스킹 1건+정리 메뉴 / **`a253232e` 씬 무관 로그 수집** / `73574a23` `Role` 값 표기 통일). **[씬 무관 로그 수집 — 커밋 `a253232e`]** 기록을 켜는 배선이 `GameBootstrapper` 에만 있었고 그것은 **`Game.unity` 에만 배치**되어 있어, **로그인·로비 구간의 `GameLog` 호출 73건이 파일에 한 줄도 남지 않았다**(sink 0개 → `LogRules.md` **1.8** 의 콘솔 폴백만 탔다). 신규 정적 클래스 **`Infrastructure/Debug/LogSessionOwner.cs`** 가 sink 인스턴스·초기화 플래그·훅 중복 방지 상태를 **한 곳의 `static`** 으로 소유한다 — 계획서 가칭 `LogSessionBootstrap` 에서 **이름을 바꾼 이유**는 Infrastructure 에 `~Bootstrap` 을 두면 **세 번째 조합 루트로 오독**되기 때문이다(이 클래스는 의존성을 주입하지 않고 세션을 소유할 뿐이다). 규칙을 **여는 쪽 둘 / 닫는 쪽 하나**로 뒤집었다 — 두 부트스트래퍼(`LoginBootstrapper` · `GameBootstrapper`)가 `Awake` **첫 줄**에서 `EnsureInitialized()`(멱등)를 부르고, 닫는 것은 `UnityEngine.Application.quitting` **1회**다. `GameBootstrapper.OnDestroy` 의 `ShutdownLogging()` 호출은 **주석 처리**했다(WORKFLOW.md [4] 비활성화 우선) — 씬 언로드 때 닫으면 **앞 씬이 열어 둔 세션까지 끊겨** 목적과 정면으로 충돌하기 때문이다. `Shutdown()` 은 **`private`** 으로 두어 "닫는 주체는 하나"를 접근 제한자로 못 박았고(씬 전환 시점에 닫는 코드가 다시 생겨나는 것을 구조적으로 차단), 에디터 보조 배선은 **`EnteredEditMode`** 를 골랐다(`ExitingPlayMode` 는 오브젝트들의 `OnDestroy` **보다 앞설 수 있어** 종료 과정 로그를 흘릴 위험이 있다). **`LogRules.md` 1.9 의 전역 미처리 예외 훅도 함께 이관**했다 — 등록·해제와 되먹임 방어 3겹의 중복 방지 상태가 **인스턴스에 묶여 있어**, 커버리지만 고치고 훅을 두고 가면 **같은 구멍이 예외 쪽에 그대로 남기** 때문이다. `LoginBootstrapper.cs` **13행** 주석의 *"GameBootstrapper 는 Lobby/Game 씬에 존재"* 도 실측(`Lobby.unity` 참조 **0건**)에 맞춰 **Game 씬에만 존재**로 정정했다. **[⚠️ 부수 영향 — 기록해 둘 것]** 미처리 예외 로그의 `className` 이 `GameBootstrapper` → **`LogSessionOwner`** 로 바뀌어, 로그 라인이 **`[Runtime/GameBootstrapper]` → `[Runtime/LogSessionOwner]`** 가 된다. Infrastructure 인 이 클래스가 Bootstrap 레이어를 참조하면 의존 방향이 뒤집히므로 **불가피**하며, **로그를 grep 하는 쪽에 영향**이 있다(옛 이름으로 찾으면 이후 로그가 잡히지 않는다). **[✅ 실기 검증 — 전 항목 PASS]** 사용자가 랜덤매칭을 에디터+실기기 구성으로 진행하고 로그를 커밋했다(`_Logs/_editor/2026-08-17/RuntimeLog.txt` — **199줄**). 확인 8항목: ① 파일명 `RuntimeLog.txt`(역할 접미사 없음) ② 헤더 3줄 규정(목적 / 시각 / **빈 줄**) ③ 199줄 축적 ④ **같은 파일에 2번째 헤더**(일 단위 이어쓰기) ⑤ `Role=` 기록 ⑥ **`[Auth/...]` 7건이 파일에 기록됨 — 씬 무관 구현의 목적 달성** ⑦ 마스킹(`Uid=c442fb1a5aaa5851` · `PlayerId=f59803b95e641dc6` **16자리 16진수** · **`Email=` 0건** · `@` 포함 줄 **0건**) ⑧ 감사 누락분 `HostId` 가 `PlayerId` 와 **같은 해시**(본인이 host 였다). **[✅ 정리 메뉴 실기 — PASS]** `Hexiege > Logcat > 3. 오래된 에디터 로그 정리` 를 **실제로 실행**했다. `_Logs/_editor/` 에 `2020-01-01`(날짜 형식)과 `2020.01.01`(점 구분)을 만들고 확인: **`2020-01-01` 삭제** · **`2020.01.01` 제외**(`DateTime.TryParseExact(..., "yyyy-MM-dd", ...)` — `LogcatCapture.cs` 595행. **느슨한 `TryParse` 였다면 파싱에 성공해 삭제됐을 아슬아슬한 케이스**) · **오늘 폴더 보존** · **취소 경로**(아무것도 삭제 안 됨) · **정리 범위 한정 — `_Logs/` 직속 날짜 폴더 17개를 하나도 건드리지 않음**. **범위 한정 검증은 우연히 가장 위험한 시나리오를 시험한 것이다** — 사용자가 처음에 테스트 폴더를 `_Logs/` 바로 아래(`_editor/` 밖)에 만들었는데 도구가 **아예 스캔조차 하지 않았다.** 그 자리는 `LogRules.md` **1.10** 이 *"작업에 귀속된 영구 보존물"* 로 규정한 트리라 지워지면 복구되지 않는다. **[후속 — `Role` 값 표기 통일 (커밋 `73574a23`)]** 실기 로그에서 같은 `Role=` 키가 두 표기로 갈린 것이 발견됐다 — `NetworkGameManager` **5곳**은 `Role=Host`/`Client`, `NetworkCombatController` **1곳**은 `Role=host`/`client`. `LogRules.md` **1.4** 가 *"섞이면 집계가 조용히 둘로 갈라진다"* 고 경고하는 사안이라 **소수인 `NetworkCombatController` 쪽을 `Host`/`Client` 로 맞췄고**, 그 사례를 1.4 에 「값 표기 일관성」 항목으로 명문화했다. **[배경]** `LogRules.md` 2026-08-13 전면 개정으로 로그 판정 축이 **심각도 한 축에서 심각도 + 존속 두 축**으로 바뀌었고, 작업 성격도 *"진단 로그 삭제"* 에서 *"`GameLog` 로 이관"* 으로 바뀌었다. 그 계획의 마지막 단계와, 개정이 예고만 해 두고 코드에 반영되지 않았던 항목들을 한 번에 끝낸 사이클. **[선행 — 2026-08-14]** 네트워크·인증 **8파일 205건** 이관 완료(`개발` 120 / `운영` 85, `LogEvent` enum 최종 32개, `Unknown` 사용처 0건). **이 이관분은 사용자 유니티 컴파일 통과가 확인되었다.** **[이번 구현 4건]** ① **민감 데이터 마스킹 15곳** — `FirebaseAuthService` 10 / `UnityServicesInitializer` 2 / `LoginUseCase` 2 / `NetworkGameManager` 1. Firebase UID · UGS PlayerId 를 `GameLog.HashId` 해시로 치환하고 **이메일은 출력 자체를 없앴다**(`Email=` 잔존 0건, 표식 주석 잔존 0건, `HashId` 실제 호출 14건). **`개발` 로그에도 적용** — `LogRules.md` **1.6** 이 *"에디터 포함 항상 적용"* 이고 **에디터 로그 파일은 커밋되어 공유**되기 때문. ② **에디터 로그 파일명 단일화** — ~~`RuntimeLog_host.txt` / `RuntimeLog_client.txt`~~ → **`RuntimeLog.txt`**. 기존 이름은 *"에디터 = Host"* 라는 **틀린 전제**(실제 테스트 구성은 에디터 1 + 실기기 빌드 1이고 어느 쪽이 host 가 될지 정해져 있지 않다)에 기대고 있어, **에디터가 client 로 붙어도 `_host.txt` 에 기록되는** 상태였다. 빌드는 `#if UNITY_EDITOR` 로 파일을 쓰지 않아 **파일을 쓰는 프로세스가 항상 에디터 1개**라 나눌 이유 자체가 없었다. 역할은 `NetworkCombatController.OnNetworkSpawn` 이 `Role=host` / `Role=client` **로그 한 줄**로 남긴다(`key=value` 만이 서버 전송 시 구조화 필드가 되기 때문 — **1.4**). 삽입 위치는 `NetworkContext.Set(...)` **직후 · `if (IsServer)` 분기 바깥** — 분기 안에 넣으면 host 에서만 찍혀 목적을 잃는다. `GameBootstrapper.Setup.cs` 의 틀린 전제 주석도 함께 정정. ③ **`RuntimeLogger` 헤더 규정 준수** — 시그니처를 **`BeginSession(folderPath, purpose)`** 로 바꿔(기존 `role` 인자 제거) 헤더 1줄째를 고정 문자열에서 **호출자가 넘긴 목적**으로 채우고, 헤더 뒤 **빈 줄 1줄**을 추가. `append`/`AutoFlush`/`try-catch`/세션 소유권 가드는 전부 유지. ④ **수동 정리 메뉴 구현** — `Hexiege > Logcat > 3. 오래된 에디터 로그 정리`. **확인 2단계**(보존 기간 30/90일 선택 → 대상 목록 제시) · **`_Logs/_editor/` 하위 한정 + 삭제 직전 매 건 경로 재검사**(한 건이라도 벗어나면 통째로 중단) · **날짜 폴더 단위 판정**(개수 기준 금지 — 정렬이 어긋나면 최신 로그를 지울 수 있다) · 파싱 실패 폴더는 대상에서 제외. 기존 `1. 버퍼 비우기` / `2. 파일로 저장` 은 이름·동작·순서 그대로이며 파괴적 동작이라 구분선으로 분리. **[감사가 놓쳤던 1건]** `LobbyManager` 의 `"CreateOrJoin 완료"` 로그가 **`HostId` 를 평문 출력**하고 있었다 — `HostId` 가 UGS PlayerId 라는 근거는 같은 파일이 `CurrentLobby.HostId == AuthenticationService.Instance.PlayerId` 로 **직접 비교**한다는 점. 해시 경유로 수정하되 **로직 판정에 쓰는 비교문은 원본 유지**했다. 이 자리는 감사표(`LogAudit.md` §6)에 **없던 자리**라, 감사표의 신뢰도에 관한 정보로서 **누락 사실 자체를 §6 에 기록**했다(필드 이름만으로 훑는 방식의 한계 사례). **[사용자 결정]** 판정이 충돌해 남겨 두었던 **질의 Q-1~Q-4 (7건)** 을 *"잠정 판정을 그대로 확정한다"* 로 결정 — **판정 내용이 한 건도 바뀌지 않아 코드 변경 불필요**. **[⚠️ 과대 표기 금지 — 아직 완료가 아닌 것]** **커밋 `4e027e68` · `675203ae` · `a253232e` 는 사용자 유니티 컴파일 통과가 확인되었다**(2026-08-17 — 205건 이관분에 이어 확인. 사용자가 `a253232e` 상태로 실기 테스트를 수행했다) · **⚠️ 다만 후속 커밋 `73574a23`(`Role` 값 표기 통일)은 명시적인 컴파일 확인을 받지 않았다** — 문자열 리터럴 대소문자 1줄 변경이지만 **"통과했다"고 적지 않는다** · **`Lobby.unity` 직접 진입은 커버되지 않는다** — 이 씬에는 부트스트래퍼가 없어 직접 열고 실행하면 sink 가 0개다. **사용자가 "로비를 직접 열어 실행하는 경우는 없다(항상 Login 을 거친다)" 고 판단해 커버하지 않기로 결정**했으므로 **미해결 결함이 아니라 범위 밖 확정 항목**이다 · **잔여 조치 1건** — `GameBootstrapper.cs` **473행**의 주석 처리된 `// ShutdownLogging();` 삭제(사용자 테스트 통과 후 삭제 예정이었던 항목. 메서드 본체는 이미 `Setup.cs` 에서 완전 삭제됐다) · **나머지 계층 이관 미착수**(전체 잔존 `Debug.Log` **234건**은 이번 8파일 밖 — 사용자가 별도 task 로 분리 결정. `GameLog.cs` 9건은 sink 폴백이라 대상 아님, `NetworkCombatController.cs` 11건 포함) · **`FileSink.EditorLogsRootRelativeToAssets` 가 `private` 이라 `LogcatCapture.cs` 가 경로 문자열을 복제**(범위 밖으로 미룸) · **`.meta` 취급 규정 명문화 보류**(사용자 판단 대기 — 커밋 `23a8da06` 에서 `.meta` 를 실제 커밋한 관찰은 있으나 그것만으로 규정이 확정된 것으로 적지 않는다). 상세: `LogRules.md` **1.13**, task `_Tasks/2026-08-13/07_13_network-auth-log-cleanup/`(`Research.md` §13-2 · `Plan.md` §0-7 · §0-8 · §0-9 · `LogAudit.md` §4-3 · §6). |
| 2026-08-12 | **MistShrine(HealShrine) 물안개 힐 시스템 구현 완료 — 에디터 싱글플레이 실기 검증 완료 · ⚠️ 멀티플레이 미검증 (커밋 `be2b396` 구현 / `b0b663c` 에디터 셋업 / `68ab380`·`df6909d` 보완 / `be17148` 중첩 버그 수정 / `cfe73bb` 진단 로그 제거)** — 2026-08-10 기획 확정(규칙 1~27)을 실제로 동작하게 만든 사이클. **[구현 범위]** Domain `BuildingData.Heal(int)` 신설(프로젝트 최초의 건물 회복 경로), Application `MistShrineUseCase`(물안개 인스턴스 목록 + 물안개별 독립 누적기 + **매 틱 대상 재수집** — `_activeTimedEffects` HoT/DoT 목록을 쓰지 않아 규칙 14의 독립 채널이 구조적으로 보장됨) + `INetworkMistShrineController`, Infrastructure `NetworkMistShrineController`(`Request → ServerRpc(팀 검증) → ClientRpc` 3단, `ResolveServices()` 지연 재조회로 스폰 레이스 회피) · `NetworkHealthSync` **건물 힐 전용 RPC 신설((b)안 — 검증된 유닛 힐 경로를 한 줄도 건드리지 않음)** · `SpecialAttackConfig` 필드 5종, Presentation `MistShrinePanelUI`(`BuildingPanelBase` 상속, 탭=수동 시전 / 롱프레스 0.5s=자동 토글 / 자동 중 탭=해제, 자동 기본 OFF, `SkillCooldownOverlay` 재사용) + `MistShrineRangeIndicator`(지면 데칼 반투명 원) + `InputHandler` 분기, Bootstrap 조합 루트 배선·`OnBuildingDied` 구독 정리 경로. **[함께 고친 기존 결손(사용자 승인)]** `InputHandler`의 `ClosedFrame` 가드에 **연구 패널·스킬 패널 2종이 누락**돼 있어 배경 탭으로 닫은 그 클릭이 뒤쪽 타일 선택으로 새던 문제를 신규 MistShrine 패널 항과 함께 보정(TDD `PopupClosedFrame` 표준 패턴). `SkillAimReticle`의 **비활성 저장 오브젝트 `Awake` 자기 비활성화 함정**과 **기준 지름 오류**도 같은 계산식을 공유해 함께 수정. **[구현 후 보완 수정 4건]** ① 비활성 상태로 저장된 오브젝트가 `Show()` 순간 늦게 도는 `Awake`에 의해 스스로를 다시 꺼 버려 **범위 원이 영영 안 보이던** 문제 → 런타임 전용 `_showRequested` 플래그로 자기 비활성화만 건너뜀 ② **반경 값 이중화 제거** — 표시용 `[SerializeField]`와 판정용 `SpecialAttackConfig`가 따로 있어 에셋만 고치면 조용히 어긋나던 구조를 `MistShrineUseCase.GetRadius()` 단일 소스로 통합(주입 실패 시 **임의 기본값으로 그리지 않고 숨긴 뒤 경고** — 잘못된 범위를 자신 있게 보여 주는 것이 더 해롭다) ③ **원 기준 지름을 스프라이트에서 자동 산출**(`_baseDiameter` 제거 → `_baseDiameterOverride` 0이면 `sprite.bounds`에서 매 `Show()`마다 재산출, 비정사각형은 **긴 축 기준** — 범위 표시는 과대 표시가 과소 표시보다 치명적). ⚠️ **핵심 기법: 필드 이름을 바꾸고 `[FormerlySerializedAs]`를 일부러 붙이지 않아** 씬에 저장된 잘못된 값(`_baseDiameter: 1`)을 버렸다 — **직렬화 필드에 잘못 저장된 값은 코드 기본값 수정으로는 절대 고쳐지지 않는다**(Inspector 값이 항상 이김) ④ **중첩 해소 규칙 위반 버그 수정(가장 중요)** — 아래 별도 항목. **[🐞 중첩 해소가 동작하지 않던 규칙 13 위반 (커밋 `be17148`)]** **증상(로그 실측)**: 거리 0.87로 겹친 신전6 `(4.50,-14.72)`·신전10 `(4.50,-15.59)`가 같은 유닛을 각각 회복시켜 Unit3 HP가 **3초에 130→190(+60)** — 설정상 **+30**이어야 했다. 규칙 13의 "가장 가까운 하나"도 어긋나 **더 먼 신전10까지** 회복시켰다. **원인**: `ActiveMist.TickAccum`이 물안개마다 독립이라 시전 시각이 다르면 1초 경계를 넘는 프레임이 달라진다(실측 위상 신전6 ≈ 매초 `.36x` / 신전10 ≈ 매초 `.73x`, **같은 밀리초 동시 틱 0건**). 그런데 중첩 해소(`_bestUnitByTarget`)는 **같은 `_firingBuffer`에 담긴 물안개끼리만** 비교하므로 버퍼에 늘 하나뿐 → **해소 코드가 돌 기회 자체가 없는 사실상 죽은 코드**였다. **수정(두 겹)**: ① **위상 정렬** — 새 물안개의 `TickAccum`을 0이 아니라 기존 물안개와 같은 값에서 출발(`GetSharedTickPhase()`). `TickMists`가 모든 물안개에 같은 `dt`를 더하므로 한 번 맞춘 위상이 유지된다 ② **소유권 판정 분리** — `ApplyHealTick`이 발화분이 아니라 **활성 물안개 전체**를 순회해 대상별 최근접 소유자를 정하고, 그 소유자가 발화한 경우에만 적용(`FiredThisTick` 게이트). ①만으로도 동작은 맞지만 규칙 13의 "가장 가까운"이 **타이밍 전제에 기대게 되어** 소유권 판정 자체를 발화 여부와 분리했다. **규칙 충돌 없음**: 규칙 8-1이 요구하는 것은 "물안개마다 자기 누적기"이지 "시작 위상이 제각각"이 아니며, **누적기 개수 = 물안개 개수**(규칙 14 표) 유지 + 매 틱 재수집 구조 무변경으로 규칙 9도 유지 → 이 불변식을 **`GameSystemRules_Buildings.md` MistShrine 물안개 힐 시스템 규칙 8-1에 "8-1-a. 활성 물안개들은 서로 같은 위상으로 틱한다"로 보강 기재**(되돌림 금지 경고 포함). **건물 경로도 함께 고쳐졌다** — 규칙 13은 "한 대상"이라 유닛만의 문제가 아니었고 건물 해소 코드도 같은 이유로 돌지 못했다. **부수 효과(사양 범위 밖·기록만)**: 이미 다른 물안개가 깔린 상태에서 시전하면 첫 회복이 1초가 아니라 **0~1초 사이**에 들어온다(공유 위상 합류). 대상 입장의 회복 간격은 1초로 유지되어 이중 회복은 없고, 물안개가 하나뿐이면 종전과 동일. **[기존 로직 제거 누적 5건 — 전부 근거 기재]** 계획 단계 0건 / 보완 수정에서 직렬화 필드 2건(`MistShrinePanelUI._rangeRadius`, `_baseDiameter`→`_baseDiameterOverride`) / 중첩 버그 수정에서 3건(`_firingBuffer` = 버그 원인 자체, `PickedUnit`/`PickedBuilding`의 `SourceBuildingId`·`ShowText` 필드 → `ActiveMist` 참조로 교체(동작 동일), `ActiveMist` 4인자 생성자 → 5인자 대체·호출부는 `Activate` 한 곳). **셋 다 주석 처리로는 컴파일이 성립하지 않거나 남기는 것 자체가 버그를 존속시키는 항목**이라 WORKFLOW.md [4]의 "비활성화 우선" 기본을 따르지 않은 근거를 Plan 최상단에 기재. **[✅ 검증 완료(에디터 싱글플레이 실기 + 런타임 로그 실측)]** ① **범위 원 경계 = 실제 회복 판정 일치** — 회복된 최대 거리 **2.29** / 탈락한 최소 거리 **3.12**, 설정 반경 **3.00**과 **모순 0건** ② **범위 이탈 시 즉시 끊김(규칙 9)** — Unit5가 `2.29(Healed)`→`3.77(OutOfRange)`로 나간 **다음 틱부터 HP 110 고정** ③ **회복량** — 매 틱 **+10**(`_mistHealPerSecond: 10` 일치) ④ **중첩 해소(규칙 13)** — 수정 후 후보 물안개 **4개**에서도 대상당 **1회만** 적용, 거리 동률(0.87)에서 **Id 작은 신전9** 선택 ⑤ 자동/수동 토글·쿨다운 방향·적 건물 미반응·연구/스킬 패널 `ClosedFrame` 가드(육안) ⑥ 컴파일 오류 0건(사용자 에디터). **[⚠️ 미완 — 과대 표기 금지]** ① **멀티플레이 실기 미검증** — 검증은 **싱글플레이로만** 이루어졌다. 범위 판정·중첩 해소·회복량 계산 코드는 싱글·멀티가 **같은 `MistShrineUseCase` 경로를 공유**하므로 판정 로직 자체는 유효하지만, **건물 HP 동기화(`SyncBuildingHealClientRpc`)·클라이언트 표시·RPC 팀 검증·쿨다운 로컬 미러·이중 틱 여부는 멀티에서만 도는 경로이며 한 번도 실행되지 않았다** ② **물안개 지속 VFX 미제작** — 물안개는 현재 **눈에 보이지 않으며** 회복 여부는 HP·회복 텍스트로만 확인된다(등록 VFX는 `destroy`/`upgrade`뿐 — 규칙 26) ③ **사용(시전) 버튼 아이콘 미제작** — 임시 텍스트 라벨(UI 규칙 15) ④ **밸런싱 수치 전부 미확정** — 현재 값은 임시값이며 확정 시 `SpecialAttackConfig.asset` 한 파일만 교체하면 된다(반경 이중화가 제거되어 화면의 원도 함께 따라감). **[진단 로그]** `848d891`(추가) → `939bd87`(세션 배선 수정) → `be17148`(로그 확장) → `cfe73bb`(**전량 제거**). 로그 파일은 LogRules에 따라 `_Logs/2026-08-10/14_12_mistshrine-heal-implementation/RuntimeLog_host.txt`에 **영구 보존** — 위 검증 수치는 전부 이 로그의 실측값이며 추정이 아니다. **[교훈]** ① **"중첩 해소 코드를 넣었다"와 "중첩 해소가 동작한다"는 다르다** — 타이밍이 어긋나 비교 대상이 늘 1개면 해소 로직은 조용히 죽은 코드가 된다. 판정 로직은 **그 로직이 실제로 실행되는 조건까지** 확인해야 한다 ② **규칙 문서에 "왜 이렇게 해야 하는가"를 남기지 않으면 되돌려진다** — 위상 정렬은 얼핏 불필요한 결합으로 보이므로 규칙 8-1에 근거·재발 사례와 함께 명시 ③ **육안 테스트로는 배수 버그를 못 잡는다** — HP가 오르는 것만 보이고 "2배"인지는 보이지 않아, 로그 실측이 있어야 드러났다. task: `_Tasks/2026-08-10/14_12_mistshrine-heal-implementation/`(Plan §9 보완 수정 4건 · §10 완료 판정. **Testcase.md는 사용자 지시가 없어 작성하지 않음** — WORKFLOW.md [5-1]). 갱신 문서: `GameSystemRules_Buildings.md`(MistShrine 물안개 힐 시스템 규칙 8-1 보강 + 상태 표기 정정) · `GameSystemRules_UI.md`(상태 표기 정정) · `PROJECT_STATUS.md` · `ROADMAP.md` · `WORK_HISTORY.md` · `.claude/MEMORY.md`. |
| 2026-08-10 | **MistShrine(HealShrine) 물안개 힐 재설계 기획 확정 + 방어 타워 표기 오류 정정 — 문서 작업만 수행 (구현 미착수 · 커밋 `02dd54f`)** — ⚠️ **이번 작업은 문서 갱신만 수행했다. 코드·프리팹·씬·전용 패널 UI·범위 표시 UI·물안개 VFX는 어느 것도 구현되지 않았고 테스트도 진행되지 않았다(과대 표기 금지).** 현재 MistShrine은 여전히 배치·피격만 되는 시각 오브젝트다. **[재설계 확정 내용]** MistShrine(`BuildingType.HealShrine` = 6)을 공격형 방어 타워가 아닌 **아군 힐 전용 건물**로 확정. 시전하면 **건물 중심 고정 원형 범위**(지점 조준 없음)에 물안개가 깔리고, 지속 동안 범위 안의 **아군 유닛 + 아군 건물**(시전 건물 자신·본기지 Castle 포함)을 **1초 단위 discrete 틱**으로 회복한다(유닛 규칙 40의 초 단위 틱 방식 재사용). 최대 체력인 대상은 회복 없음·회복 텍스트도 미표시. **시전 시점 스냅샷이 아닌 아우라 방식**이라 범위를 벗어나면 즉시 회복이 끊기고 다시 들어오면 재개된다. **물안개 지속시간 < 쿨다운**으로 반드시 다운타임이 존재하며(상시 유지 설정 금지), **시전 비용 없음**(쿨다운으로만 제어), 물안개는 이동하지 않고, **시전 건물 파괴·철거 시 물안개는 즉시 제거**(자동 모드·쿨다운 상태도 함께 정리). **[중첩 규칙]** 물안개끼리는 **중첩 금지** — 한 대상은 효과를 하나만 받으며 **더 가까운 MistShrine** 우선, **거리가 완전히 같으면 건물 `Id`가 작은 쪽**을 적용하는 결정적(deterministic) 규칙으로 서버·클라 판정 불일치를 차단. 단 **연구소 초월계 자연회복과는 별개 효과로 중첩 적용**되며, 자연회복·BloomFairy 힐과 겹치지 않는 **별도 채널**로 구현해야 한다. **[UI 확정]** `BuildingSkillPanelUI`는 5슬롯·지점 조준 전제라 재사용 불가 → **`BuildingPanelBase` 상속 전용 패널 신설**. 조작은 생산 패널과 동일하게 **짧은 탭 = 수동 시전 1회 / 롱프레스(0.5초) = 자동 모드 토글 / 자동 중 짧은 탭 = 해제**, **자동 모드 기본값 OFF**. 쿨다운 표시는 `SkillCooldownOverlay` 재사용, 범위 표시는 **아군 건물 + 패널이 열린 동안만**(적 MistShrine 미표시), 회복 텍스트는 실제 회복된 대상만·표시 주기를 회복 틱(1초)과 분리(임시 3초). **[로직 확정]** `SkillActivationUseCase`는 `IsSkillBuilding` 게이트가 `HealShrine`을 차단하고 슬롯·종족 로드아웃·실행기 레지스트리를 전제하므로 **재사용 불가 → 전용 UseCase 신설**(Application 레이어, Unity.Netcode 미참조). 쿨다운 관리 패턴(`Dictionary<건물 Id, 남은 시간>` + 총 쿨다운 보관 + 서버 틱 이중 틱 금지 + **클라이언트 로컬 미러**)만 차용하고, 자동/수동 토글 동기화는 생산 시스템의 `Request → ServerRpc(팀 검증) → ClientRpc` 3단 구조를 따른다. 시전 판정·대상 수집·회복 적용·쿨다운은 **서버에서만** 실행. **[신규로 필요하다고 식별된 것]** ① 아군 유닛·아군 건물 수집 헬퍼(기존 원형 반경 헬퍼는 `BlastAttackBehavior`/`QuakeAttackBehavior`/`UnitCombatUseCase` 모두 **적 대상 전용**) ② **건물 회복 경로**(기존 힐 서브시스템 `UnitData.Heal`·`OnEntityHealed`·HoT는 유닛 전용이고 `BuildingData`에는 `TakeDamage`만 있어 회복 메서드가 없음) ③ 파괴·철거 시 물안개/자동 모드/쿨다운 정리 경로(`OnBuildingDied` 구독) ④ **물안개 VFX 신규 제작**(현재 등록된 MistShrine VFX는 `destroy`/`upgrade` 뿐). 범위 표시 반투명 원형은 기존 지면 데칼 셰이더 `SkillAimOverlay.shader` 재사용 우선 검토(**ZTest Always 금지**). **[함께 정정한 문서 오류]** GDD·TDD가 **Transcendence 방어 타워를 MistShrine으로 잘못 표기**해 왔으나, 정확한 Transcendence 방어 타워는 **`VineTower`(`BuildingType.AutoTower` = 2)** 이며 MistShrine(`HealShrine` = 6)은 enum부터 별개인 힐 건물이다. `AssetList.md`·`CommonAssetGuide.md`·`StatsReference.md`·`BuildingType.cs`·`GameSystemRules_Buildings.md`는 원래부터 올바르게 구분하고 있었다. 함께 방어 타워 공격력 표기도 ×10 스케일(2026-07-31) 반영값 **150**으로 정정(쿨다운은 ×10 대상이 아니라 Human/Trans 5.0s·Spirit 3.5s 유지). **[밸런싱 미확정(과대 표기 금지)]** 회복량 · 물안개 지속시간 · 쿨다운 · 범위 반경 · 회복 텍스트 표시 주기는 **전부 미확정**이며 밸런싱 단계에서 확정한다. `StatsReference.md`의 기존 MistShrine 값(HP 500 / 건설비 100 / 10 HP/s · 범위 3)은 **재설계 이전 수치**로 재검토 대상이다. **[문서 구조 결정]** MistShrine 규칙은 `GameSystemRules_Buildings.md` 내 **"MistShrine 물안개 힐 시스템"** 섹션(규칙 1~27)에 두되, **특수 동작 건물이 2종 이상이 되면 즉시 `GameSystemRules_SpecialBuildings.md`로 분리**한다는 분기 시점을 문서에 명시. **[갱신 문서]** 커밋 `02dd54f` 8종(`GameSystemRules_Buildings.md`·`GameSystemRules_UI.md`(MistShrine 패널 UI 규칙 1~9)·`GameDesignDocument.md` §4·§5·`TechnicalDesignDocument.md`·`StatsReference.md`·`GameSystemRules_Upgrade.md`·`PROJECT_STATUS.md` 등) + 후속 잔여 3종(`ROADMAP.md`·`Assets/VFXSFXList.md`·`WORK_HISTORY.md`). **[다음 단계]** 밸런싱 수치 확정 → 구현 착수(ROADMAP 우선순위표 "MistShrine 물안개 힐 시스템 구현" 미착수 항목). task: `_Tasks/2026-08-10/09_34_mistshrine-heal-redesign/`. |
| 2026-08-08 | **랠리포인트 조준 시 반투명 오버레이 잔존 버그 수정 완료 (실기 테스트 PASS · 커밋 `9a19cd5`)** — 배럭(생산 건물) 팝업에서 랠리포인트 버튼을 누르면 팝업 창은 사라지지만 화면 전체를 덮던 **반투명 배경(공유 BlockingOverlay)이 그대로 남아**, 집결지를 찍으려 지도를 탭하면 그 터치를 오버레이가 먼저 가로채 **지정이 그냥 취소된 것처럼 보이던** 버그. **[원인]** 오버레이는 `BuildingPanelBase.Show()`가 켜고 **끄는 곳은 `Close()` 단 하나**인데, `ProductionPanelUI.OnRallyPointClick()`은 `_popup?.Hide()`만 호출해 팝업만 숨기고 오버레이를 남김. 남은 오버레이는 `blocksRaycasts=true` + onTap=`Close` 상태라 맵 탭이 `Close()`→`OnBeforeClose()`(`IsSettingRallyPoint=false` + `HideAllRallyMarkers()`)를 실행시킴 — EventSystem과 `InputHandler` 중 어느 쪽이 먼저 도느냐에 따라 **설정 자체가 안 되거나(경우 B) 설정은 됐지만 깃발이 즉시 사라지는(경우 A)** 두 갈래가 되며 사용자 체감은 동일. **[수정]** `Presentation/UI/ProductionPanelUI.cs` **1개 파일 2줄 순수 추가**(기존 로직 제거·변경 없음): ① `OnRallyPointClick()`에 `UIManager.Instance?.HideBlockingOverlay();` — 이미 검증된 `BuildingSkillPanelUI`의 지점 조준 진입 패턴을 그대로 적용 ② `Close()`를 거치지 않는 `CompleteRallyPointSetting()`에 동일 호출로 **참조 카운터 반납**. **[②가 필수인 이유]** 랠리 완료 경로에는 원래 카운터 반납 지점이 없었고, 지금까지는 **버그(오버레이 잔존)가 대신 `Close()`를 태워 카운터를 우연히 맞춰주고 있었음** → ①만 고치면 카운터가 남아 다음 팝업의 오버레이 표시가 어긋나는 새 버그가 생기므로 두 수정은 반드시 함께 적용. `HideBlockingOverlay()`는 언더플로 가드 + 멱등이라 이중 호출 안전. **[부수 효과]** `GameSystemRules_Buildings.md` 랠리포인트 시스템 규칙 2의 "설정 직후 3초 표시"가 이 버그로 위반되고 있었으며 수정으로 정상화. **[근거 규칙]** `GameSystemRules_UI.md` 공통 UI 규칙 5(BlockingOverlay 단일 소유 + 참조 카운터) — **규칙 문서 자체는 이미 올바르게 서술돼 있어 변경 없음**(코드가 규칙을 다시 준수하도록 맞춘 수정). **[교훈]** `BuildingPanelBase` 계열에서 **조준 모드 진입을 위해 `_popup.Hide()`만 하는 경로는 `HideBlockingOverlay()`를 반드시 짝으로** 호출하고, **`Close()`를 우회해 끝나는 경로는 참조 카운터를 직접 반납**해야 한다(스킬 패널에 이어 2번째 발생). **[범위 밖·별도 작업 후보]** 조사 중 확인된 랠리포인트 결함 5건(단일 `_autoHideCoroutine`로 인한 깃발 영구 잔존, 싱글플레이 팀 필터 부재, 재경기 중복 구독·마커 누수, 랠리 좌표 유효성 검증 부재, 멀티 서버 거부 시 롤백 경로 부재). task: `_Tasks/2026-08-08/13_33_rally-point-blocking-overlay-bug/`(Research/Plan 하단 "구현 결과" append, Testcase 미작성 — 사용자 미지시). |
| 2026-08-08 | **건물 파괴 시 열린 패널/조준 UI 원복 구현 완료 (실기 테스트 PASS · 커밋 `8c7fa01`)** — 건물 패널 4종(생산/건물액션/스킬/연구)이 열려 있거나 스킬 조준 중일 때 그 건물이 파괴되면 화면에 유령 패널·조준 UI가 남던 시각적 갭을 해소. 실제 발동·철거는 서버 재검증으로 막혀 게임 상태는 안전했으나 **UI만 원복되지 않던** 버그. **[구현]** 공통 베이스 `BuildingPanelBase`가 `GameEvents.OnBuildingDied`를 구독해, 파괴된 건물이 **현재 표시/조준 중인 건물이면 `Close()`** 호출 → 각 패널 `OnBeforeClose`로 스킬 조준 취소(`CancelAim`)·생산 랠리 마커 숨김이 자동 연계(4개 패널 전부 커버). **코드 변경은 `BuildingPanelBase.cs` 1개 파일만**(자식 4개 패널 무변경). **[판정 기준]** `IsOpen`이 아니라 **`_currentBuilding.Id` 매칭** — 스킬 조준 중엔 `_popup.Hide()`로 `IsOpen=false`가 되기 때문(패널이 닫혀 있으면 `_currentBuilding==null`이라 매칭 자연 실패=오작동 없음). **[구독 해제 — 계획 대비 변경]** Plan은 `OnDestroy`+Dispose였으나, 자식 `ResearchPanelUI`가 **이미 자체 `OnDestroy`를 선언**해 베이스 `OnDestroy`를 **은닉(hide)** 하면 베이스 해제 로직이 누락되는 구독 누수 회귀가 생김 → 프로젝트 관용 패턴(`BuildingFactory`/`UnitFactory`/`HitPresentationQueue`)을 따라 **`.AddTo(this)`(UniRx)** 로 컴포넌트 수명에 묶어 해결. **[멀티]** `NetworkCombatController.HandleBuildingDied`가 클라에서 `OnBuildingDied`를 재발행해 싱글/호스트/순수 클라 전부 커버(별도 배선 불필요). **[실기 결과(PASS)]** 스킬 조준 중 시전 건물 파괴 시 조준 원 소멸·입력 잠금 잔존 없음, 생산 건물 파괴 시 랠리 마커 소멸, 연구소 파괴 시 연구 패널 닫힘(기존 연구 취소·환불과 충돌 없음) 모두 확인. **[교훈]** MonoBehaviour 베이스에서 자식이 자체 `OnDestroy`를 선언하면 베이스 `OnDestroy`가 은닉되므로, 베이스의 이벤트 구독 해제는 신설 `OnDestroy`보다 **`.AddTo(this)`가 안전**. 규칙 `GameSystemRules_UI.md`(공통 UI 규칙 11 신설 — 건물 팝업 대상 파괴 시 자동 닫힘)·`GameSystemRules_Skills.md`(서두 후속 항목 완료 갱신). task: `_Tasks/2026-08-08/07_40_building-death-ui-restore/`(Plan/Research 하단 "구현 결과" append, Testcase 미작성). |
| 2026-08-05 | **스킬 시스템 타입 C(전역 상태변경: 버프/디버프/CC/힐) Phase 2 구현 완료 (실기+멀티(클라) 테스트 PASS)** — 스킬 건물 프레임워크 위에 마지막 메커니즘 타입인 "전역 상태변경"을 구현하고 실기·멀티(클라이언트 재현)로 검증. 버프·디버프·제어(둔화·빙결)·회복을 **하나의 상태변경 시스템**으로 표현. **[타입 C 프레임워크]** Domain `Status/{StatusEffectKind,StatusEffect,UnitStatusState}`(순수 C#, 상태 목록→공격/이속 배율·CanAttack 순수 계산) + Application `Services/StatusEffectSystem`(유닛Id→상태 딕셔너리, **서버 권위** 부여/틱) + `Skill/GlobalStatusChangeExecutor`(타입 C 실행기, 조준 없음 전역 즉시, 대상팀 결정→상태 부여). `StatusEffectKind` = None/MoveSpeedMul/AttackPowerMul/AttackDisabled/Freeze(=이속0+공격불가)/HealOverTime. **[유효 스탯 합성(곱연산·확정 9-1·9-4)]** `UnitCombatUseCase`의 `EffectiveAttack`(기본×연구×상태)·`GetUnitMoveSpeedMultiplier`(연구×상태, 빙결 시 0)에 상태 배율을 연구 강화 배율과 곱연산 합성 + **신규 공격 게이트 `CanAttack(unit)`**(빙결/기절 시 데미지 봉쇄, 싱글=`TryAttack`/멀티=`NetworkCombatController.TickCombat` 2곳 삽입). **무상태면 배율1·CanAttack true → 기존과 완전 동일(회귀 안전).** **[빙결 애니 정지]** 빙결 시 `Animator.speed=0`으로 걷기 프레임 고정(제자리 걷기 방지) + `UnitAnimState.Frozen`·`OnUnitFreezeChanged`로 순수 클라 동기화. **[둔화 라이브 적용]** 이동 코루틴이 **매 프레임 유효 이동배율을 재조회**해 즉시 반영(경로 재캡처 불필요) → 둔화 0.5가 즉시 라이브로 걸림(실기 확인). ⚠️ 전투 종료 후 정렬 Lerp 구간만 캡처값 사용(미세 잔여). **[회복]** 상태 시스템에 미저장 → 기존 HoT(`ApplyTimedEffect` Heal) 재사용(HP는 `NetworkHealthSync`가 이미 동기화). **[UI 버튼 균일화]** 스킬 슬롯 CostContainer를 `SetActive(false)`(행 높이 붕괴) 대신 **CanvasGroup alpha=0(HideChildKeepLayout)**로 숨겨 행 높이 보존. **[멀티 동기화]** `StatusAppliedClientRpc((int)team,(int)kind,mag,dur)`로 빙결/둔화/버프 브로드캐스트(클라가 자기 유닛 재현, 서버는 skip), 회복은 HP 동기화로 재현(이중 힐 방지). **[정리(cleanup)]** 개발 중 넣었던 **진단 로그 코드를 LogRules 준수 위해 제거** → `IRuntimeLogSink`/`RuntimeLoggerSink` **삭제**(상시 기능 아님), 로그 파일 `_Logs/2026-08-04/16_49_skill-status-debug/RuntimeLog_host.txt`는 LogRules대로 보존. 좌표화 때 주석 비활성화했던 코드 3곳도 삭제 완료. **교훈: 로그 작업 착수 전 반드시 `LogRules.md`(RuntimeLogger 파일 기록·raw Debug.Log 금지)를 먼저 확인.** **[실기 결과(PASS)]** 공격버프·빙결(이속0+공격봉쇄+만료 복귀)·둔화(0.5 라이브)·회복(HoT)·**무상태 회귀(기존 유닛/연구강화 무변경)** + 순수 클라이언트 재현까지 모두 확인. **[유지 중인 테스트 스캐폴딩(과대 표기 금지)]** 종족별 플레이스홀더 스킬 5슬롯(1 폭탄A/2 빙결/3 공격버프/4 둔화/5 회복), 스킬 버튼 임시 텍스트 라벨 — 최종 스킬 기획·아이콘 확정 전까지 유지되는 테스트용. **[남은 것(별도 작업)]** ① 건물 파괴 시 열린 스킬 패널/조준 UI 원복 미구현(스킬 포함 4개 건물 패널 공통 갭, `BuildingPanelBase` OnBuildingDied 구독→Close 제안) ② 구체 스킬 목록·수치·아이콘(기획) 보류. 규칙 `GameSystemRules_Skills.md`(구현 상태 갱신, 규칙 13 본문은 구현과 일치해 무수정). task: `_Tasks/2026-07-28/12_14_skill-building-system-design/`(Plan/Research 하단 "Phase 2 완료 결과" append). |
| 2026-08-04 | **스킬 건물 시스템 Phase 1 구현 완료 (실기기 테스트 PASS)** — 스킬 건물(FlightFacility/MagicSpirit/WillowShrine)로 액티브 스킬을 발동하는 데이터 주도 프레임워크와 타입 A·B 실행기, 모바일 탭 조준, 전용 UI를 완성하고 실기 검증. **[프레임워크]** Domain `Skill/SkillMechanicType`(A/B/C — **C는 enum 선언만**)·`SkillAimType`(Instant/PointTarget), Application `Skill/{SkillData,ISkillExecutor,SkillActivationContext,SkillExecutorRegistry}`·`UseCases/SkillActivationUseCase`(발동 단일 진입점 `Activate`, 플레이어/AI 공유, 건물 글로벌 쿨다운=UseCase Dictionary), 로드아웃=RaceId 키(`SkillLoadoutConfig:ISkillDataProvider` — MagicBuilding enum 공유라 종족 분기). **[타입 A/B 실행기]** A(즉발 범위 피해)=`InstantAreaDamageExecutor`→`UnitCombatUseCase.ApplySkillInstantAreaDamage`(방어 감쇄 O·**Tank 2배 미적용**), B(장판 DoT)=`AreaDotDamageExecutor`→`ApplySkillAreaDot`(무감쇄, 규칙 40 초 단위 틱 재사용). 반경 수집은 건물엔 공격자 UnitData가 없어 도메인월드(`_mapper.HexToWorld`)+TeamId 기준 전용 헬퍼(`CollectEnemy{Units,Buildings}InRadiusDomain`) 신설. **[UI]** `BuildingSkillPanelUI`(BuildingActionPanel 복제로 룩 상속, 3×3 슬롯 1~5 스킬/6 철거/7~9 예약)+`SkillCooldownOverlay`(버튼 전체 덮는 어두운 반투명 + **12시 기점 시계방향 소멸** `fillOrigin=Top`+`fillClockwise=false`+남은 초, 유휴 시 숨김). **[모바일 탭 조준]** `SkillAimController` 2단계 — 버튼 탭→조준 모드 진입(패널 닫힘·조준원·취소 X, 버튼 자신 release 무시)→새 화면 press·드래그로 조준 이동+엣지 스크롤→release 발동, 하단 X 위 release로 취소(X hover 시 취소버튼 Lerp 확대 예고, 규칙 20-1). static `IsAiming` 가드로 카메라 팬/입력 억제. **[조준 지점 연속 좌표화(2026-08-04)]** 조준 중심을 타일 스냅(HexCoord)→**연속 도메인 월드 Vector3**로. **착탄 반경 판정은 원래 연속 원(중심 월드+반경 유클리드)이라 무변경 — "중심 입력"만 연속화.** 전 계층 시그니처 Vector3화(SkillAimController/BuildingSkillPanelUI/`SkillActivationUseCase.Activate(Vector3?)`/`SkillActivationContext.AimWorld`/`UnitCombatUseCase.ApplySkill*(Vector3 center)`/`NetworkSkillController` RPC=**NGO 2.9.2 Vector3 기본 직렬화**, int q,r 분해 폐지). **[맵 경계 재검증]** 서버 재검증을 유효타일(HasTile)→**맵 경계 안 점(point-in-bounds)**으로 교체. `Core/HexMetrics.{ComputeMapWorldBounds,IsWithinMapBounds,ClampToMapBounds}` 신규(최외곽 타일 중심 극값+반칸 AABB = **최외곽 타일 바깥선까지 엄밀 clamp**). HexGrid(Domain)는 Vector3 불가→Core 수학+클로저 주입(GameBootstrapper `_grid` 캡처=맵 재로드 대응). **[조준원 지면 데칼 렌더링(2026-08-04)]** 원인=조준원(y=0.05)과 HexTile(ProBuilder 실린더) coplanar→z-fighting으로 파묻힘. 신규 셰이더 `Assets/_Project/Shaders/SkillAimOverlay.shader`(Transparent+ZWrite Off+**ZTest LEqual+Offset -1,-1**+Cull Off)=지형엔 안 가려지고 불투명 유닛/건물엔 정상 가려지는 데칼(**ZTest Always 금지**). 머티리얼은 셋업 스크립트(`SkillSetup_Scene.EnsureOverlayMaterial`)가 생성·3겹 SpriteRenderer+`SkillAimReticle._overlayMaterial` 배선. 타일 Y Scale(등각) 무변경. **씬 재셋업 필요.** **[취소 버그 근본 수정(2026-08-04)]** 실기(Android) 터치에서 취소 X 위에서 손을 떼도 발동·쿨다운이 걸리던 문제. 원인=손 뗀 프레임에 `TryGetPointerScreenPos`의 마우스 분기가 합성 마우스 좌표(0,0)를 유효로 반환해 캐시 폴백을 가로챔. 수정=release 프레임엔 라이브 좌표를 읽지 않고 캐시된 마지막 드래그 좌표(`_lastDragScreenPos`)로만 취소/발동 판정. **[쿨다운 안내 토스트(2026-08-04)]** 쿨다운 중 스킬 탭 시 조용히 무시하던 것을 안내 — 기존 ToastUI(에셋 기반)에 `ToastKey.SkillOnCooldown` 추가 + `ToastMessageConfig.asset` key:4 "스킬이 쿨다운 중입니다"(`BuildingSkillPanelUI.ToastUI.Show`). **[멀티/서버 권위]** `Infrastructure/Network/NetworkSkillController`(NetworkBehaviour) — 좌표만 전송 + 서버 재검증(건물 생존·글로벌 쿨다운·맵 경계 안 점), 발동 성공 시 `SkillActivatedClientRpc`로 양 클라 쿨다운 미러. 쿨다운 틱=싱글 GameBootstrapper.Update/멀티 서버 NetworkCombatController.TickCombat/순수 클라 오버레이 미러(이중 틱 금지). **[미완(별도 작업·과대 표기 금지)]** ① 타입 C(전역 상태변경: 버프/디버프/CC/힐) Phase 2 미구현(enum만 선언·실행기 미구현, 업그레이드 연동 규칙 9-x도 함께) ② 건물 파괴 시 패널/조준 UI 원복 미구현(스킬 포함 4개 건물 패널 공통 갭, `BuildingPanelBase` OnBuildingDied 구독→Close 제안) ③ 구체 스킬 목록·수치(기획) 보류. 규칙 `GameSystemRules_Skills.md`(1~26, 조준 좌표화 정정 17·19·22·22-1·24·26·구현 상태 갱신). 관련 커밋(브랜치 claude/building-skills-discussion-3v8d5k): 규칙정정 `13bb7c1`, 좌표화+렌더링 `9e79a2f`, 취소 1차 `2e88dfa`, 취소 근본+토스트 `4e5da5e`. task: `_Tasks/2026-07-28/12_14_skill-building-system-design/`(프레임워크·Phase 1) + `_Tasks/2026-08-04/04_46_skill-aim-coordinate-based/`(조준 좌표화·렌더링·버그수정). |
| 2026-07-31 | **연구소 유닛 강화 시스템 + 전투 스탯 ×10 스케일 개편 + 연구 패널 UI 구현 완료 (기능·멀티플레이 실기 PASS)** — 연구소(Research 건물)로 팀 단위 유닛을 강화하는 신규 시스템 전체를 구현하고 멀티플레이 실기로 검증. **[강화 스탯]** 공격력(유닛별 고정 정수 증가 = `Round(기본×8%)×레벨`) / 방어력(신규, 비율 감쇄) / 이동속도(배율) 3종 + 초월계 전용 자연회복(상시 HoT, 3~15 HP/s). 팀별 그룹×스탯 트랙(인간 9·정령 9·초월 7). **[×10 스케일]** 유닛 HP·공격력·건물 HP·타워 HP/공격력·DoT 틱값·힐량 전부 ×10 — config `.asset`에 ×10 커밋 반영(적용에 쓰였던 셋업 스크립트는 역할 종료 후 제거됨). HP·공격력 동일 배율 → **TTK 불변**(상성·매치업 불변, 강화 그리드만 규칙적으로 개선). 사거리·이동속도·쿨다운·비용·비율은 불변. **[방어력 감쇄]** 순수 함수 `Domain/Combat/DamageCalculator.ApplyDefense(raw, defense)` = `Max(1, Round(raw×(1−def/(def+120))))`, K=120, floor 1, 하드캡 65%, `raw<=0`·`defense<=0`이면 원본 반환(하위호환). 방어 트랙 0/8/16/24/32/40. **직격·스플래시·타워→유닛**에 일괄 적용(`UnitCombatUseCase.ComputeFinalDamage`/`TowerCombatUseCase`), **DoT 틱값 미적용**. Tank/CannonCart 건물 대상 2배 분기 포함. **[(B) 소급 강화]** Application `UseCases/UnitUpgradeUseCase.cs`가 팀별 트랙 레벨을 보관하고 데미지·이동 사용 지점에서 실시간 배율/증가치를 조회(`GetEffectiveAttack`/`GetMoveSpeedMultiplier`/`GetDefense`/`GetRegenPerSecond`/`ScaleByGroupAttack`) — 유닛 스냅샷 미변경이라 이미 전장에 나온 유닛도 연구 완료 즉시 강화. 그룹 매핑 `Domain/Unit/UpgradeGroup.cs`. 자연회복은 BloomFairy 힐과 별개 채널(상호 간섭 없음). **[네트워크·서버 권위]** Infrastructure `Network/NetworkUpgradeController.cs` — 연구 요청 ServerRpc(골드 검증·차감·트랙 잠금·타이머), 완료 레벨은 **양 클라 브로드캐스트**(효과 양쪽 적용), 진행 중 연구는 **소유자에게만**, 연구소 파괴 시 진행 연구 취소·**투입 골드 100% 환불**. **MP 완료 처리의 서비스 스폰 레이스 버그를 `ResolveServices()`(지연 재조회)로 수정.** **[연구 패널 UI]** `Presentation/UI/ResearchPanelUI.cs`(`: BuildingPanelBase` — 공통 헤더·닫기·**철거+환불**) + 본문 2-레이어: **매트릭스**(그룹 행 × 공/방/속 열; 인간·정령 3×3, 초월 2×3 + 자연회복 전체폭 버튼, 셀=Lv X/5+비용, `ResearchMatrixView`/`ResearchCellView`) ↔ **진행 게이지**(이름·Lv X→X+1·게이지·남은시간·취소, `ResearchProgressView`). **연구소 단위 전환**(연구 중=진행, 유휴=매트릭스), 진행 중 트랙 잠금(팀 중복 방지), 배경 탭 닫기. 아이콘 대신 텍스트 라벨. **[신규 코드]** `DamageCalculator.cs`·`UpgradeGroup.cs`·`UnitUpgradeUseCase.cs`·`NetworkUpgradeController.cs`·`ResearchPanelUI.cs`·`ResearchMatrixView.cs`·`ResearchCellView.cs`·`ResearchProgressView.cs` + UnitStats/UnitData/BuildingData/UnitStatsConfig/SpecialAttackConfig/UnitCombatUseCase/TowerCombatUseCase/GameBootstrapper/GameEvents/InputHandler/AIOpponentController/BuildOrderStep 등 수정. **[셋업 스크립트 정리]** ×10 적용·배선·디버그에 쓰였던 에디터·디버그 스크립트는 config `.asset` 커밋 반영·씬 배선 완료 후 역할이 끝나 제거됨. **[후속 보류(별도 작업 — 과대 표기 금지)]** ① 연구 패널 UI 레이아웃 다듬기(에디터 스크립트 자동생성 골격, 사용자가 Unity에서 직접) ② 매트릭스 헤더 아이콘(공/방/속·그룹) ③ AI 시나리오 연구 사용(코드는 있으나 실기 미검증) ④ MistShrine(HealShrine) 힐(미구현) ⑤ 싱글플레이 자연회복(실기 미검증, 코드상 정상 예상). 규칙 `GameSystemRules_Upgrade.md`(규칙 1~13, 구현 상태 섹션)·`GameSystemRules_Units.md` 규칙 44. task: `_Tasks/2026-07-22/10_08_unit-upgrade-system/`(Research/Plan/BalanceReview). |
| 2026-07-20 | QuakeSpirit(대지의 정령) 착탄형 즉발 범위 딜러 구현 완료 (특수 유닛 5종 **마지막 = 5종 전량 + InfernoSpirit DoT까지 전부 완료**, QA PASS·**멀티플레이 실기 로그 검증 완료**) — 적에게 공격이 착탄하는 순간 그 자리를 중심으로 **즉시 한 번 폭발**하는 즉발 범위 딜러(시간에 걸쳐 뚝뚝 들어가는 DoT와 근본적으로 다름). **[착탄형 즉발 특수 핸들러]** `QuakeAttackBehavior`(`Application/Combat/`, `ReplacesPrimaryAttack=false`) + 레지스트리 `QuakeSpirit → QuakeAttackBehavior` 1줄. 판정 = 착탄 중심(주 타깃 위치) 기준 **월드 좌표 원형 반경**(`_quakeRadius` 기본 1.0=인접 1칸, XZ, arc 없음 — 규칙 38 판정 재사용). **[직접 100% + 스플래시 50% 역할 분담(건물 포함)]** 직접 100%(공격력 20) = 기존 `ExecuteAttack` 주 타깃 단일 피해(`ApplyDamageToVictim`, 주 타깃이 건물이면 100%로 공성), 스플래시 50% = 특수 핸들러가 착탄 반경 내 **다른 적 유닛 + 적 건물**에 **즉발** 피해(올림 `CeilToInt(공격력×0.5)`=`CeilToInt(20×0.5)`=10, **주 타깃 제외** — 중복 피해 방지). 배율은 거리 감쇠가 아니라 **고정 2단계**(주 타깃 100% / 나머지 50%). 아군 무피해, 서버 권위. ⚠️ **MushroomBomber/BattleAxe와 달리 스플래시가 적 건물도 포함**(그쪽 두 유닛의 AoE는 유닛만 순회 — QuakeSpirit만의 차별점, 폭발이 주변 건물까지 무너뜨리는 광역 공성 성격). **[원형 반경 헬퍼 공용화 + 건물 순회 신설 + hit-set 분리]** 규칙 38의 유닛 수집 헬퍼 `CollectEnemyUnitsInRadius`를 `internal static`으로 **공용화**해 유닛 수집에 그대로 재사용(MushroomBomber "유닛만" 로직 무변경 — 회귀 없음) + 건물용 `CollectEnemyBuildingsInRadius` **신설**. 유닛 Id/건물 Id 카운터 값 충돌 방지 위해 **유닛/건물 hit-set 분리**(규칙 29 계승). **[튜닝·연출]** `SpecialAttackConfig` `_quakeRadius`(1.0=인접 1칸)/`_quakeSplashRatio`(0.5) 추가, GameBootstrapper float 주입(미연결 시 폴백 1.0/0.5 — "에셋 생성 ≠ 씬 배선"). 즉발 연출은 규칙 26(단일 타격 프레임=보류 큐 전부 방출)에 따라 폭발과 동시 표시. 규칙 `GameSystemRules_Units.md` **43 등재**(규칙 38 헬퍼 재사용 사례 상호참조 추가, 규칙 27 QuakeSpirit `OnAttackHit` 항목 갱신). **[멀티플레이 실기 로그 검증]** Host/Client 실기 로그로 **주 타깃 100%=20·스플래시=10·건물 스플래시·주 타깃 제외·반경 이내만 적중·아군 무피해·서버↔클라 동기화**를 전부 정상 확인(로그: `_Logs/2026-07-20/11_00_quakespirit-aoe-verify/`). **[알려진 이슈(보류, 별도 task 분리)]** ① QuakeSpirit **타격 애니↔데미지 텍스트 타이밍 어긋남** — 원인은 QuakeSpirit Attack 클립 `OnAttackHit` **미주입**(규칙 27 잔여) + `hitFrameTimes` placeholder(1.0s)라, 스플래시 데미지 텍스트가 공격자 로컬 타격 프레임 대신 `HitPresentationQueue` 타임아웃(쿨다운×1.5=7.5s)까지 지연 방출됨(피해·판정·동기화 자체는 정상, 연출 표시 시점만 어긋남). ② 멀티플레이 원거리 공격 facing 버그(InfernoSpirit 작업에서 진단). 둘 다 사용자 결정으로 이번 미수정·별도 작업 이관(ROADMAP F-4·우선순위표). 이로써 **특수 유닛 전량 완료** — 잔여 특수 공격 핸들러 없음. task: `_Tasks/2026-07-20/10_24_quakespirit-impact-aoe/`. |
| 2026-07-20 | InfernoSpirit(지옥불 정령) 단일 대상 지속 피해(DoT) 구현 완료 (특수 유닛 5종 중 5번째 = 전 5종 완료, QA PASS·사용자 실기 완료) — 이미 완성된 원거리 유닛(에셋·VFX·생산·`OnAttackHit` 완비)에 **DoT 특수만** 얹음. **[단일 대상 DoT 특수 핸들러]** `InfernoAttackBehavior`(`Application/Combat/`, `ReplacesPrimaryAttack=false`) + 레지스트리 `InfernoSpirit → InfernoAttackBehavior` 1줄. MushroomBomber(`BlastAttackBehavior`)와 달리 **착탄 반경이 없다** — 반경 수집 코드 없이 **주 타깃 1마리만** 판정(AoE 아님). 유닛으로 캐스팅 성공(적 유닛)·`IsAlive`·팀 필터 통과 시 DoT 부여, 건물이면 무동작. **[직접 25 + DoT 역할 분담]** 직접 25 = 기존 `ExecuteAttack` 주 타깃 단일 피해(`ApplyDamageToVictim`, 공격력 25, 건물 공성 포함), DoT = 특수 핸들러(주 타깃 적 유닛만). 주 타깃 유닛=직접 25+DoT, 주 타깃 건물=직접 25만(DoT 없음), 아군 무피해, 직접 25로 주 타깃 사망 시 DoT 스킵. **[DoT 초 단위 틱 재사용(규칙 40)]** MushroomBomber에서 만든 DoT 초 단위 discrete 틱 시스템을 **단일 대상으로 재사용** — 5/초×3초(총 15), 1초 간격·틱당 올림(`CeilToInt`, 최소 1)·총량 15 클램프·매초 `OnEntityDamaged`로 남은 체력 데미지 텍스트(힐과 반대로 억제 안 함)·서버 권위·이중 틱 금지·갱신=리셋(중첩 없음). 틱 간격은 MushroomBomber와 동일 `BlastDotTickInterval`(1.0s). **[유닛별 DoT 값 분리(MushroomBomber 회귀 방지)]** InfernoSpirit(5/3)은 MushroomBomber(2/3)와 **별도 필드·델리게이트·진입점**으로 분리: `SpecialAttackContext.ApplyInfernoDot` → `UnitCombatUseCase.ApplyInfernoDot`(`_infernoDot*` 필드)이 자기 값으로 `ApplyDamageOverTime` 호출. MushroomBomber `ApplyBlastDot`(`_blastDot*`) 경로 무변경. 튜닝값 `SpecialAttackConfig` `_infernoDotPerSecond`(5)/`_infernoDotDuration`(3) 추가, GameBootstrapper float 주입(미연결 시 폴백 5/3 — "에셋 생성 ≠ 씬 배선"). 규칙 `GameSystemRules_Units.md` 41~42 등재(규칙 40에 재사용 사례 상호참조 추가). **[알려진 이슈(보류)]** 멀티플레이 원거리 공격 시 유닛이 타겟을 정확히 안 바라보는 facing 버그 — 진단상 InfernoSpirit 에셋은 정상 유닛(FlameSpirit)과 동일 → 멀티 원거리 facing 공유(서버 회전+NetworkTransform) 로직 문제로 추정(근접 유닛은 미노출). 사용자 결정으로 이번 미수정·보류(DoT만 구현), 별도 작업으로 이관(ROADMAP). **잔여 특수 유닛**: QuakeSpirit(착탄형 — MushroomBomber 원형 반경 헬퍼 재사용) — 레지스트리 확인 결과 유일 미등록. BloomFairy는 힐러 전용 경로로 의도적 미등록. task: `_Tasks/2026-07-20/03_22_infernospirit-dot-and-attack-facing/`. |
| 2026-07-20 | FlatTop 11×21 무작위 대전 맵 구현 계약 문서 동기화 완료(미구현) — 초기 골드 전용 `GameConfig.MapTestModeEnabled`(ON=5000/OFF=광산 수 표), 싱글 로컬 권위·멀티 Host 권위, 테스트 표식(0/1)과 실제 초기 골드의 canonical bytes/SHA-256/로그 포함을 확정. persistent 1KB chunk 전송·64KB 상한·10초/incomplete 1회 재전송, `SameMap`/`NewMap`, 임시 `MapVersion=1`과 별도 전역 `GameProtocolVersion` 경계를 포함해 RandomMap 규칙·GDD·TDD·Research/Plan·현황·로드맵을 정합화. 코드·QA·Testcase 변경 없음. task: `_Tasks/2026-07-19/15_31_random-battle-map-design/`. |
| 2026-07-19 | FlatTop 11×21 무작위 대전 맵 기획·규칙 문서화 완료(구현 예정) — 다섯 맵 유형 동일 20%, 모든 생성 요소·장식 exact 180° 대칭, 시작 공간 팀별 고유 10타일, 유형별 중립 광산 수와 초기 골드, 국소 건설 불가 구역/완전 차단 지형, 결정적 seed·100회 재시도·동일 조건 폴백·로그·검증 기준 확정. 중앙 단독 광산과 180도 대응 광산 쌍의 공정성 규칙을 분리해 `GameSystemRules_Map.md` 정정, `GameSystemRules_RandomMap.md`·루트 `CONTEXT.md` 신규 작성. PointyTop 제외. task: `_Tasks/2026-07-19/15_31_random-battle-map-design/`. |
| 2026-07-19 | MushroomBomber(버섯폭격기) 착탄형 범위 DoT 딜러 구현 완료 (특수 유닛 5종 중 4번째, QA PASS·사용자 실기 완료) — 폭탄(포자)을 던져 착탄 지점에서 폭발시키는 착탄형 딜러. **[착탄형 특수 핸들러]** `BlastAttackBehavior`(`Application/Combat/`, `ReplacesPrimaryAttack=false`) + 레지스트리 `MushroomBomber → BlastAttackBehavior` 1줄. 판정 = 착탄 중심(주 타깃 위치) 기준 **월드 좌표 원형 반경**(XZ, arc 없음 — 도끼병 부채꼴에서 arc 제거). 반경 내 적 유닛 수집(주 타깃 포함·아군/사망/공격자/건물 제외) 후 DoT 부여. 수집부 `CollectEnemyUnitsInRadius`는 향후 QuakeSpirit(중심 100%/인접 50% 감쇠) 재사용 위해 static 헬퍼 분리. **[직접 10 + DoT 역할 분담]** 직접 10 = 기존 `ExecuteAttack` 주 타깃 단일 피해(`ApplyDamageToVictim`, 건물 공성 포함), DoT AoE = 특수 핸들러(적 유닛만). 주 타깃 유닛=직접+DoT, 반경 내 다른 적 유닛=DoT만, 주 타깃 건물=직접만(그래도 핸들러는 실행돼 주변 적 유닛엔 DoT), 아군 무피해. **[DoT 초 단위 틱 모드(규칙 34 확장)]** `ActiveTimedEffect.TickInterval` 분기(0=연속 HoT / 양수=discrete DoT — HoT 연속 경로 무변경). `ApplyDamageOverTime`: 틱 간격 1.0s, 틱당 피해 = 초당×간격 올림(`CeilToInt`, 최소 1), 총량 = 초당×지속 반올림 클램프(2×3=6). 매 틱 `OnEntityDamaged` 발행 → **매초 남은 체력 데미지 텍스트**(힐 HoT 규칙 37과 정반대로 억제 안 함). 서버 권위·이중 틱 금지(`TickTimedEffects` 기존 진입점). 갱신=리셋(중첩 없음). **[튜닝·데이터]** `SpecialAttackConfig` blast 필드 추가(`blastRadius` 1.0=인접 1칸/`blastDotPerSecond` 2/`blastDotDuration` 3), GameBootstrapper float 주입(미연결 시 폴백). UnitStatsConfig(26) HP40/공격력10/사거리2.0/감지2.0/이동1/쿨다운3.0/생산15·200골드, 클립 `OnAttackHit` 주입(규칙 27, 1개). VFX(투사체·폭발)는 사용자 별도 제작. **[식물 라인 생산 배선(에디터 스크립트)]** UnitFactory `_transcendencePrefabs` type 26 등록 + `ProductionPanelUI._buildingUnitMappings` 식물 라인: SporePatch(1단계)=MushroomBomber, FloralNursery(2단계)=BloomFairy(BloomFairy 생산 노출도 완성). 규칙 `GameSystemRules_Units.md` 38~40 등재. **잔여 특수 유닛**: QuakeSpirit(착탄형 헬퍼 재사용)·InfernoSpirit(DoT 초 단위 틱 재사용) — 코드 확인 결과 핸들러 미등록(미구현). task: `_Tasks/2026-07-19/01_42_mushroombomber-impact-dot/`. |
| 2026-07-18 | BloomFairy(꽃요정) 힐러 + HoT/DoT 공용 시간 지속 효과 시스템 구현 완료 (특수 유닛 5종 중 3번째, 사용자 실기 완료) — 적을 때리지 않고 부상 아군을 회복하는 첫 힐러. 기존 전투 흐름(적 감지→공격)에 얹을 수 없어 **힐러 전용 경로**를 신설. **[힐러 전용 경로]** `ExecuteAttack`/`ISpecialAttackBehavior`/`SpecialAttackRegistry`(적 공격 흐름)에 **등록하지 않고**, 상태머신이 데이터(힐러 플래그)로 인식해 힐 루프를 타는 독립 경로(TorrentSpirit처럼 special-only이나 대상이 아군이라 적 파이프라인과 완전 분리). 힐 발동은 `OnAttackHit`이 아니라 상태머신 `HitFrameTimes` 타이머로 구동(`OnAttackHit`은 힐 연출 전용 — 데미지 관례와 동일). **[부상 아군 탐색]** 기존 적 탐색은 무변경, 아군 탐색을 별도 메서드로 신설(같은 팀 AND 살아있음 AND `Hp<MaxHp`, **본인 포함**, 아군 유닛만·건물 제외, 사거리 4.0 월드). 우선순위 = 잃은 체력 비율 최대 → 동률 시 거리 최소. **[HoT/DoT 공용 시스템(신규)]** 서버 권위 diff 틱(`ActiveTimedEffect`/`ApplyTimedEffect`/`TickTimedEffects`, Application, 파도 틱과 대칭). 대상별 동종 1레코드·갱신=리셋. HoT는 매 프레임 diff로 총량 정확 도달(3초 20HP). Heal→`UnitData.Heal`+`OnEntityHealed`(규칙 30 재사용). Damage(DoT) 분기는 구조로 수용(후속 MushroomBomber가 초 단위 틱으로 실제 배선). **[힐 유휴 감시·쿨다운 예외]** 경로 끝 도달 후에도 부상 아군 지속 감시(`HealerIdleWatchV3`). ⚠️ **쿨다운 예외(프로젝트 유일)**: `AttackCooldown`(3.0s)이 힐 발동 준비(1.0s)를 미포함 → 발동 후부터 카운트라 실제 힐 주기 4.0s(의도된 설계, 되돌리지 말 것). **[HoT 힐 텍스트 집계(2026-07-19 실기 확정)]** HP 회복은 종전대로 틱마다 상승(HP바·멀티 동기화 무변경), 플로팅 힐 텍스트만 틱마다 억제하고 효과 정상 종료 시 회복 후 현재 HP로 1회 표시(대상 사망 시 생략). `EntityHealedEvent.ShowText`+`NetworkHealthSync` 전파, `ActiveTimedEffect.ActualHealed>0`일 때만. 규칙 `GameSystemRules_Units.md` 32~37 등재. task: `_Tasks/2026-07-18/03_40_bloomfairy-healer/`. |
| 2026-07-17 | TorrentSpirit(물의 상급 정령) 파도형 이동 AoE + 힐 서브시스템 구현 완료 (특수 유닛 5종 중 2번째). 도끼병에서 만든 전략 핸들러 구조 위에 3가지 신규 요소를 얹음. **[special-only 모델]** `ISpecialAttackBehavior.ReplacesPrimaryAttack`(TorrentSpirit=true) → `ExecuteAttack`이 주 타깃 단일 피해를 건너뛰고 특수 핸들러만 실행(단일 공격이 없는 유닛). 도끼병(false)/일반 유닛 무변경. **[서버 권위 이동 파도]** `TorrentAttackBehavior`(+`WaveSpawnRequest`)는 파도 모양·방향(공격자→주 타깃, 월드 XZ)만 계산해 `SpawnWave` 요청, 실제 전선 전진·판정·효과는 `UnitCombatUseCase.SpawnWave`/`TickWaves`(`ActiveWave`)가 담당(규칙 18 서버 권위). 판정=월드 좌표 직사각형(폭 3×전방 3, 타겟 방향), 전선이 `waveTravelTime`(0.5) 동안 전진하며 닿는 대상에 1회(유닛·건물 별도 hit-set). **적 유닛·적 건물 20 피해 / 아군 유닛 10 힐**, 시전자·죽은 대상 제외. 틱: 싱글=GameBootstrapper(`!IsNetworkMode`)·멀티=NetworkCombatController(IsServer), 이중 틱 없음. 파도 피해 연출은 대상별 시점이 달라 `ImmediatePresentation`으로 큐 우회 즉시 방출. **[힐 서브시스템(신규, BloomFairy 공용)]** `UnitData.Heal`(MaxHp 클램프), `OnEntityHealed`/`EntityHealedEvent`(피격과 분리 채널), `NetworkHealthSync`가 HP 증가(힐)도 동기화(`SyncHealClientRpc`+클라 재발행 — 기존엔 감소만 동기화), `FloatingHpTextSpawner` 치유 색상. **[튜닝 SO]** `SpecialAttackConfig`에 `waveWidth`(3)/`waveLength`(3)/`waveTravelTime`(0.5)/`waveHeal`(10) 추가(Inspector), 피해는 공격력 재사용. **[VfxPoolItem 버그 수정]** 파도 프리팹은 빈 루트 아래 형제 파티클 3종(Main_Water_Surge/Ground_Impact_Ripples/Front_Foam_Crest) — 기존엔 첫 하나만 재생돼 "원형 파문만 잠깐" 보였음 → 루트에 PS 없으면 직속 자식 시스템 전부 재생하도록 수정(루트-PS 프리팹 불변). **[데이터 배선(YAML 직접)]** UnitStatsConfig(18) HP100/공격력20/사거리3/이동0.5/쿨다운4/생산30·400골드, `EffectPreset_TorrentSpirit_Attack`(vfx_torrentspirit_attack) 생성+`UnitEffectConfig` 연결, 클립 `OnAttackHit` 0.5s(임시) 주입, SpecialAttackConfig.asset 파도 필드 명시. **[QA]** qa-tester CONDITIONAL PASS — 로직·서버권위·레이어·회귀 견고, **BUG-002(special-only 건물 공격 불가) 수정 완료**(파도가 적 건물도 순회), BUG-003(경계 epsilon)·BUG-004(OnAttackHit 다중 주입 금지)는 기록만. VFX 세부 튜닝은 사용자 진행. 규칙 `GameSystemRules_Units.md` 28~31 등재. 교훈: special-only 유닛은 건물 공격 경로 누락 주의(성 파괴=승리조건); NetworkHealthSync는 HP 감소만 동기화했음(힐은 증가 분기 신설); 다중 파티클 시스템 프리팹은 형제 전부 재생 필요; EffectPreset은 vfx 프리팹 감싸는 SO. 세션 한도로 서브에이전트 반복 실패 → 메인 세션이 구현 검증·버그수정·문서를 직접 수행. task: `_Tasks/2026-07-17/12_59_torrentspirit-wave-aoe/`. |
| 2026-07-17 | 도끼병(BattleAxe) 휩쓸기형 AoE 구현 완료 (특수 유닛 5종 중 첫 구현, 사용자 실기 PASS) — 게임의 모든 유닛이 "한 번에 한 마리"만 때리던 단일 타깃 구조에서, 도끼병에게 처음으로 **범위 공격(AoE)** 을 부여. 앞으로 진행할 특수 유닛 5종(BattleAxe/QuakeSpirit/TorrentSpirit/MushroomBomber/BloomFairy)의 특수 공격 처리 기반 구조를 함께 확립. **[특수 공격 전략 핸들러 아키텍처(신규)]** `ISpecialAttackBehavior`(계약, `Apply(SpecialAttackContext)`) + `SpecialAttackContext`(공격자·주 타깃·유닛 목록·재사용 피해 헬퍼·월드 좌표 조회 수단·reach/arc) + `SpecialAttackRegistry`(`UnitType → 핸들러` 매핑, 현재 `BattleAxe → SweepAttackBehavior`만) + 유닛별 핸들러. 모두 `Scripts/Application/Combat/`. `UnitType` 키 매핑이라 인스펙터 배선 불필요 → **신규 특수 유닛 = 핸들러 추가 + 레지스트리 등록 1줄**, `ExecuteAttack` 재수정 불필요. **[피해 수렴점 단일화]** 싱글/멀티 공통 수렴점 `UnitCombatUseCase.ExecuteAttack`의 인라인 단일 피해 로직을 `ApplyDamageToVictim` 헬퍼로 추출(주 타깃/AoE 공용 → 멀티플레이 HP 동기화 일관), 말미에 특수 공격 훅 1줄 추가. **[휩쓸기 판정 = 월드 좌표 전방 부채꼴]** 초기 "전방 5타일 타일 기준"에서 실기 피드백 후 변경. forward=공격자→주 타깃 방향(월드 XZ), 각 적에 XZ 거리 ≤ `sweepReach` AND 각도 ≤ `sweepArcHalfAngle`이면 피격. Y축(UnitYOffset) 무시, 겹친 적(거리≈0) 포함, 아군/사망/공격자/주 타깃 제외, 건물 미대상. 월드 좌표는 `IEntityPositionProvider`(서버 권위). **[튜닝 SO 신규]** `SpecialAttackConfig`(Infrastructure/Config) — `sweepReach`(기본 1.0, 실기값 0.75)·`sweepArcHalfAngle`(기본 120). GameBootstrapper가 SO 값을 float로 UnitCombatUseCase 생성자에 주입(Application→Infrastructure 역참조 회피, 미연결 시 폴백). 에셋 `Resources/Config/SpecialAttackConfig.asset` + `_specialAttackConfig` 배선 완료. 에디터 툴 `Assets/Editor/Setup/CreateSpecialAttackConfigAsset.cs`(메뉴 `Hexiege/Setup/Create SpecialAttackConfig Asset (Game)`)로 에셋 생성+배선 멱등 자동화. **[타격 타이밍]** BattleAxe `hitFrameTimes = 1.1667s`(클립 타격모션 0:28~1:05의 종료 프레임 35f/30fps). `OnAttackHit` 애니 이벤트를 `Hexiege/Combat/Inject OnAttackHit Events` 인젝터로 주입(특수 유닛 5종은 클립에 이벤트가 없어 F-4로 남아 있던 항목). **[AoE 피격 연출 동시 방출]** `HitPresentationQueue.OnLocalAttackHit`이 공격자 `HitFrameTimes.Length≤1`(단일 타격 프레임)이면 보류 큐 전부 방출(휩쓸기 N마리 동시 표시), `>1`(LionKnight 2타·FlameSpirit 6타)이면 기존대로 1건(회귀 없음). 데미지·HP는 서버에서 전원 정확 적용 — 변경은 연출 타이밍만. **[스탯]** UnitStatsConfig(unitType 5): HP80/공격력15/**attackRange 0.75**(0.5→사용자 조정)/detectRange1/moveSpeed1/attackCooldown3.05/생산20s/골드200/인구1. 근거리A 라인(TrainingCamp/WarAcademy/HumanBarracks) requiredStage 3 생산. **[병합]** main 최신화 병합 완료(폰트 에셋 충돌은 main 버전으로 정리). 규칙 `GameSystemRules_Units.md` 23~27 등재, TDD 0.22.0. 교훈: SO 튜닝값은 "에셋 생성 ≠ 씬 배선"(미배선 시 폴백 함정); 유닛 `attackRange`(주 타깃) vs 특수 AoE `sweepReach`(SpecialAttackConfig)는 별개; 헥스 인접 타일 거리 ≈ 0.9~1.0 월드; 특수 유닛 5종 Attack 클립에 OnAttackHit 없음(인젝터 주입 필수); HitPresentationQueue는 "타격 프레임 1개=피해 1건" 전제라 AoE는 `HitFrameTimes.Length` 분기로 해결. task: `_Tasks/2026-07-16/18_06_battleaxe-aoe/`. |
| 2026-07-17 | 매치메이킹 404 수정 — 호스트 결정을 Lobby CreateOrJoin으로 전환 (A방식, 초기 정상·지속 관찰 중) — 랜덤 매칭은 성사되나 **직후 호스트 결정 단계에서 HTTP 404**가 나며 게임 연결이 끊기던 문제. **원인**: `MatchmakerManager.DetermineIsHostAsync` 내부의 `MatchmakerService.GetMatchmakingResultsAsync(matchId)`가 **전용 서버(Multiplay)가 매치 결과를 조회할 때 쓰는 서버 지향 API**인데, 우리 게임은 P2P(Relay) 구조라 일반 플레이어가 호출하면 조회 대상 리소스가 없어 404가 반환됨(매칭 자체=티켓/폴링/MatchId 발급은 정상, **호스트 결정 단계만 실패**). 게다가 이 API가 주던 "전체 플레이어 목록"이 사라지면 기존 해시 방식은 입력을 잃어 성립 불가 → API 교체가 아니라 **호스트 결정 방식 자체를 전환**해야 함. **해결(A방식 — Unity 공식 P2P 정석)**: 호스트를 "계산"하지 않고 **Lobby CreateOrJoin 선점**으로 전환. 모든 플레이어가 동일 `matchId`를 `lobbyId`로 `LobbyService.Instance.CreateOrJoinLobbyAsync(lobbyId, lobbyName, maxPlayers, options)`(com.unity.services.multiplayer@2.0.0) 호출 → **없으면 생성=호스트 / 있으면 참가=클라이언트**. 서버가 원자적으로 처리하므로 두 클라 동시 요청에도 정확히 한 명만 호스트가 되어 race condition이 원천 차단됨. 호스트/클라 판별은 기존 `LobbyManager.IsHost`(`CurrentLobby.HostId == 내 PlayerId`) 재사용. **SDK 시그니처 확정**: `matchId`를 `lobbyId`로 직접 사용 가능(별도 키 매핑 불필요), `options.Data`(MatchId S1 인덱스)는 "생성" 시에만 반영·"참가" 시 무시. **클라 참가 경로 일원화**: 구 경로(`JoinByMatchIdAsync` → `FindLobbyByMatchIdAsync` 폴링 → `JoinGameByIdAsync`)를 비활성화하고 CreateOrJoin 한 번으로 통합, 남은 대기는 "RelayJoinCode 채워짐 대기"뿐 — `CurrentLobby`가 참가 시점 스냅샷이라 나중에 채워진 JoinCode가 안 보이므로 신규 `RefreshCurrentLobbyAsync()`(내부 `GetLobbyAsync` 재조회)로 최대 15회(~15초) 폴링. **수정 파일(3개, 모두 Infrastructure/Network)**: `LobbyManager.cs`(추가: `CreateOrJoinLobbyByMatchIdAsync`, `RefreshCurrentLobbyAsync` / `FindLobbyByMatchIdAsync`는 미사용화·삭제 안 함), `MatchmakerManager.cs`(비활성화 주석: `DetermineIsHostAsync`, `GetStableHash`), `NetworkGameManager.cs`(추가: `StartMatchmadeGameAsync`/`HostMatchmadeGameAsync`/`JoinMatchmadeGameAsync`, `StartMatchmakingAsync` 분기 교체, 구 클라 참가 경로 비활성화 주석). **상태**: 초기 매칭 실기에서 404 없이 정상 연결 확인. 단 이 버그는 **간헐적(intermittent)** 이라 사용자가 **지속 테스트 중** — "초기 정상, 지속 관찰 중"이며 확정 PASS 아님. **비활성화 우선 원칙 준수** — 폐기 로직은 즉시 삭제가 아니라 블록 주석(`/* */`)으로 비활성화, 최종 삭제는 지속 테스트 확정 후 별도 단계. 잔여 리스크: ① SDK 시그니처는 공식 문서 기준 확정(에디터 컴파일 최종 확인 권장), ② "정확히 한 명만 호스트"·간헐 재현은 지속 멀티 실기(Host+Client) 검증 필요, ③ 클라 RelayJoinCode 대기 15초 타임아웃. 교훈: **P2P(Relay) 매칭에서 호스트 결정은 매치 결과 조회가 아니라 Lobby CreateOrJoin 원자 선점으로 해야 한다**(서버/Multiplay용 API를 P2P 클라가 호출하면 404). 브랜치 `claude/matchmaker-404-error-pi9qdn`, 커밋 `a3dbc73`. Testcase 미작성(사용자 TC/QA 미요청, 계획 대비 확정/변경은 Plan.md "실제 구현 결과" 절에 기록). task: `_Tasks/2026-07-16/19_09_matchmaker-404-host-determination/`. |
| 2026-07-15 | Android AAB 빌드 용량 최적화 완료 (main 반영) — `codex/asset-size-optimization` 작업으로 Android AAB 용량을 **190.66 MB → 125.30 MB**로 줄여 총 **65.36 MB** 절감. 핵심 절감은 3D 모델 텍스처 Android import max texture size 조정: `Assets/_Project/Texture/Buildings/**`, `Assets/_Project/Texture/Units/**`를 `1024 → 512`로 낮춤. `_Old` 미사용 에셋 디렉터리 7개 삭제, normal-map PNG 93개 + roughness PNG 84개 삭제, 건물/장비 FBX에 보수적 import 설정(mesh compression, blend shape/animation import 비활성화 등) 적용. TMP Font Atlas 축소는 패킹 기준 절감은 있었지만 최종 AAB 효과가 작아 되돌림. UI 배경/유닛 초상화/건물 아이콘/UI 스프라이트/TextMeshPro 폰트는 시각 품질 확인 전 유지. 후속: 기기 QA에서 설치/실행, 로그인, 로비 UI 가독성, 인게임 유닛·건물 텍스처 품질, 팀 색상 변형, 공격 이펙트/emission 품질 확인. 상세 문서: `AABSizeOptimization.md`, `BuildAssetOptimizationReport.md`, `UnusedAssetAudit.md`. |
| 2026-07-13 | 코드 정리 3건 완료 (실기 통과, main 반영) — 한 세션에서 독립적인 3건의 정리/수정. **[1) 죽은 코드 제거]** `UnitView.StopMovement()` 삭제 — 코드베이스 Grep 전수 확인 결과 호출 0건인 미사용 메서드. 함께 있던 주석이 이미 제거된 `OnUnitWalkStopped` 이벤트를 언급하던 문서 불일치도 해소. 순수 삭제로 런타임 동작 불변. 커밋 `8840798`(main). **[2) Animator 상태 의존 제거(리팩토링)]** 전투 종료 후 Walk 재개 3곳(`EnterCombatLoopV3` 멀티 서버 분기/싱글 분기, `ResumeFromForwardTileV3`)이 `Animator.GetCurrentAnimatorStateInfo`로 "이미 Walk인지"를 판별 → CrossFade 블렌딩 도중 Animator가 목표가 아닌 출발 상태를 반환할 수 있어 판별이 어긋날 잠재 취약점(현재는 항상 전투 안정화 후 호출돼 실버그 아님). 프로젝트 자체 원칙("Animator 런타임 상태 의존 제거", `WaitForAttackCycleEnd` 제거 시 확립)·`GameSystemRules_Units` 규칙 18(서버 데미지 타이밍)/규칙 22(애니메이션 값 기반 동기화)와 동일 방향. **해결**: 신규 필드 `_currentAnimStateHash`(마지막으로 CrossFade 지시한 상태 해시를 로컬 추적, 초기값 0은 어떤 실제 해시와도 구분) + 공통 헬퍼 `ResumeWalkAnimation()`(speed=1 복원 후 `_currentAnimStateHash==StateWalk`면 재-CrossFade 생략, 아니면 Walk로 CrossFade) 도입. CrossFade 발생 4곳(`MoveAlongPathV3` Walk 시작·`StartWalkAnimation`·`PlayAttackAnimation`·`StartCombatAnimation`)에서 필드 갱신, 위반 3곳을 헬퍼 호출로 대체하여 Animator 런타임 상태 질의 완전 제거. 실행 컨텍스트가 `MoveAlongPathV3` 서버 가드 이후라 서버/호스트/싱글 한정 → 클라 애니메이션(규칙 22 값 기반 레벨 동기화 경로) 무영향, 재개 직전 로컬 상태는 항상 Attack이라 겉보기 동작 불변. 비활성화(주석 처리)했던 기존 블록은 실기 통과 후 최종 삭제 완료. 커밋 `97adaad`+후속 삭제. task: `_Tasks/2026-07-13/09_28_anim-resume-state-tracking/`(Research/Plan, Testcase 미작성 — 겉보기 동작 불변 리팩토링). **[3) Firebase 인증 게이트 제거(로그인 버그 수정)]** main 커밋 `528c7c6`이 도입한 `#if HEXIEGE_ENABLE_FIREBASE_AUTH` 게이트가 심볼 미정의 환경에서 스텁 `FirebaseAuthService`를 컴파일시켜 로그인이 무조건 실패하던 문제. Firebase Unity SDK는 프로젝트 `.gitignore` 정책상 git에 커밋하지 않고 각자 로컬 임포트(용량 대형)하는 방침 → 게이트를 제거해 실제 Firebase 코드가 무조건 컴파일되도록 복원(검증된 `combat-system-visuals` 브랜치 상태와 동일). 수정 파일: `FirebaseAuthService.cs`(게이트+스텁 제거), `LoginBootstrapper.cs`(GPGS 가드 2곳), `Assets/Plugins/Android/mainTemplate.gradle`(firebase-auth 24.1.0/firebase-auth-unity 13.11.0/firebase-app-unity 13.11.0/gpgs-plugin-support 2.1.0 등 Android 의존성 복원). 사용자가 Firebase Unity SDK 13.11.0 + GooglePlayGames v2.1.0을 로컬 임포트하여 컴파일/테스트 통과. 커밋 `4fe1cf0`(main). 잔여(별도 이슈): 에디터 "Firebase 초기화 실패" 런타임 로그(코드 게이트와 무관), Firebase/EDM 저장소 버전 관리 방침 정리(ROADMAP Phase F-5). 교훈: 외부에서 로컬 임포트하는 대형 SDK를 `#if SYMBOL` 컴파일 게이트로 감싸면 심볼 미정의 시 스텁이 조용히 대체돼 기능이 무조건 실패 — SDK 존재 여부는 컴파일 심볼이 아니라 임포트 자체로 판단하게 두고 게이트를 두지 않는 것이 안전. Animator 상태 판별은 런타임 질의(`GetCurrentAnimatorStateInfo`) 대신 마지막으로 지시한 상태를 로컬 필드로 추적해 CrossFade 블렌딩 중 오판을 원천 차단. |
| 2026-07-13 | 이동/Walk 애니메이션 동기화 완료 (레벨 동기화 전환, 실기/로그 검증 PASS) — 갓 생산된 유닛에서 걷기 모션이 안 나오고, 유닛이 순간 뒤로 밀려 보이던 두 증상을 구조적으로 제거. **[근본 전환: 엣지 RPC → 레벨 동기화]** 유닛 애니메이션 상태를 "바뀌는 순간 한 번 쏘는 신호(1회성 RPC)"에서 `NetworkUnit`의 **NetworkVariable(`UnitAnimState` None/Walk/Attack, 서버 쓰기 / 클라 읽기)** 로 전환. 클라이언트는 `OnValueChanged` + **스폰 시 현재 값 자동 적용**으로 상태를 받으므로, 유닛이 늦게 준비돼도 준비되는 순간 서버의 현재 값을 자동 수신 → 첫 Walk RPC가 구독 완료 전 도착해 유실되던 스폰 레이스(Phase 1 계측: 클라 스폰 223기 중 222기가 첫 Walk RPC 1건씩 유실)가 **구조적으로 소멸**. 호스트/싱글플레이는 기존 로컬 Animator 직접 제어 유지. **[Phase 2 후속 봉합: 초기화 후 재적용]** 상태 적용이 `UnitView.Initialize`보다 이르면 애니메이터가 아직 준비 안 돼 무음 실패(재시도 없음, unit=-1 125건/무귀속 15기) → `UnitView.Initialize` 말미에서 `NetworkUnit.ReapplyAnimStateToView()`(멱등)로 현재 값을 재적용해 봉합. **[Phase 3: 위치 보정]** 재경로 재발급 시 출발점이 뒤처진 도메인 타일(`_unitData.Position`) 기준이라 첫 스텝이 최종 목적지 역방향으로 향하던 "뒤로 밀림"(Phase 1 계측: 서버 파일 282건, 클라 0건 → 보간 무죄·서버 경로 자체가 원인) → `MoveTo` 단일 지점 `AlignPathStartToTransform`: 첫 스텝이 최종 목적지와 역방향일 때만 실제 `transform` 기준 전방 타일(`FindForwardClosestTile`)에서 경로를 재발급(규칙 11 강제). 3차에서 보정 353건(실이동 278), 역방향 282→41 급감. **[Phase 3 잔여 41건 종결]** 코드 무수정으로 종결 — 41건 전부 스폰 직후 유닛의 정상 우회 경로이고, 판정 지표가 "첫 걸음이 최종 목적지(먼 성) 직선 방향인가"라 정상 우회를 오탐한 것(계측 지표의 한계). 육안 밀림 없음, 문서에 명시. **[최종 3차 검증]** 클라 로그 15,316줄 — 생성 유닛 634기 전원이 애니메이션 상태 적용 보장(무귀속 15→0기), Initialize 후 재적용 634회 발동, 육안으로 걷기 미재생·뒤로 밀림 모두 소멸. **[정리]** `[MOVESYNC-LOG]` 계측 코드 전량 제거, Phase 2에서 주석 처리했던 엣지 경로(`StartWalkAnimationClientRpc`/`OnNetworkWalkStarted`) 최종 삭제. **[Plan 대비 변경]** `_combatAnimationSent` 가드는 애니메이션이 아니라 데미지(`ExecuteAttack`)·타겟 RPC 게이팅 기능 가드로 확인돼 주석 처리하지 않고 유지(직전 combat-timing 태스크 수정 3과 동일 판단). **[규칙]** `GameSystemRules_Units.md` 규칙 22(애니메이션 상태의 값 기반 동기화) 등재 — 규칙 21(Attack 루프 이탈 시 재전송 가드 해제, 엣지 트리거 봉합책)을 상위 대체. TDD 유닛 애니메이션 동기화 방식을 "엣지 RPC → NetworkVariable 레벨 동기화"로 갱신. 교훈: 애니메이션 상태처럼 "현재 상태" 자체가 의미인 값은 엣지 트리거(순간 신호)가 아니라 레벨 동기화(값 그 자체 공유)로 다뤄야 스폰 레이스·신호 유실에 구조적으로 안전; 계측 지표는 정상 동작(우회)을 오탐할 수 있어 잔여 수치를 무조건 버그로 보지 않고 성격을 확인해야 함. task: `_Tasks/2026-07-12/07_55_movement-walk-anim-sync/` (Research/Plan, Testcase 미작성 — 로그 검증 대체). |
| 2026-07-12 | 전투 타격 타이밍 동기화 완료 (실기/로그 검증 PASS) — 화면 타격 동작과 실제 데미지 순간의 어긋남, 그리고 타워·원거리 유닛의 연출 공백을 4개 축으로 해결. **[축1 타이밍 단일 소스]** `HitFrameTimes`를 Attack 클립 `OnAttackHit` Animation Event 시간에서 자동 추출(`UnitFactory`) — 수동 입력은 이벤트 없을 때만 폴백. 에디터 검증 메뉴 `CombatHitEventValidator` + 이벤트 주입 메뉴 `CombatHitEventInjector`로 13종 클립에 이벤트 주입(미구현 특수 타격 5종 BattleAxe/QuakeSpirit/TorrentSpirit/MushroomBomber/BloomFairy는 의도적 제외). **[축2 서버 정밀화]** `NetworkCombatController.TickCombat`의 50ms 격자 오버슈트를 데미지 딜레이+쿨다운 리셋 양쪽에서 차감. **[축3 피격 표현 큐]** `EntityDamagedEvent`/`SyncHealthClientRpc`에 공격자 정보 추가, `HitPresentationQueue` 신설 — HP텍스트/피격VFX/스케일펀치를 공격자의 로컬 `OnAttackHit` 프레임에 동기화하되 도메인 HP는 서버 값 도착 즉시 갱신(서버 권위 유지). `EffectManager.PlayUnitHit`+`UnitEffectConfig.hitPreset`, `HitReactionPunch`, `FloatingHpTextSpawner`를 `ShowDamage` 단일 진입점화. **[축4 연출 공백]** 타워 발사 VFX(`BuildingEffectConfig.attackPreset`+`PlayBuildingAttack`, 타워는 즉발 유지·피격 큐 즉시 방출), 원거리 트레이서(`TracerProjectile`+`UnitEffectConfig.tracerPreset`, 착탄 시 피격 연출 방출, 데미지 타이밍 불변). **[검증 중 기존 버그 3건 수정]** ① `Update()` Tick 이월 잔여분 이중 계산으로 쿨다운 15~25% 조기 소진(Pistoleer 2.0초 대비 실측 1.71초) → 실제 경과 시간 1:1 감소로 수정(원 목표였던 불일치의 숨은 근본 원인). ② 피격 큐 공격자 사망/전투 중단(StopCombat) 시 잔여 방출 부재 → 즉시 방출 경로 추가. ③ 클라 Attack 루프 이탈(Walk RPC) 시 `_combatAnimationSent` 잔존으로 StartCombat 재전송 억제 → 유닛이 굳어 보이는 시각 버그(실기 75초 Assault 사례). Walk RPC 전송 시 가드 해제로 수정. 3건 모두 이번 작업 이전부터 존재한 기존 결함으로 이번 계측이 처음 가시화(승패 무관). 튜닝 1건: 큐 타임아웃 쿨다운×1.5. **[최종 검증]** 4차 로그 16,014줄 — 사이클 간격 오차 평균 -0.7ms(1프레임 이내), 타격 프레임 방출 85.7%, 유닛 고착 소멸(실기 확인). 잔여 타임아웃 2.7%(타겟 전환 순간 표시 ~0.5초 지연)는 이동/Walk 애니메이션 동기화 후속 태스크로 이관. **[정리]** 임시 계측 로그 코드 전량 제거, `Presentation/Unit/UnitEffectView.cs` 최종 삭제(DEPRECATED 대체 완료), `StatsReference.md` 표기 정리(콜론=소수점 범례 + 총기 5종 초 환산). **[규칙]** `GameSystemRules_Units.md` 규칙 17~21(전투 연출 동기화 섹션 신설), `GameSystemRules_Buildings.md` 방어 타워 시스템 규칙 12(타워 발사 연출) 등재. 교훈: 데이터(HP)는 서버 시계, 연출은 로컬 시계로 역할 분리; 데미지를 Animator 상태에 종속시키지 않음(누락 방지); 계측 로그가 원 목표 외 기존 잠복 버그 3건을 가시화. task: `_Tasks/2026-07-09/01_12_combat-hit-timing-sync/`. 별개 단발: Android TargetSDK 변경(빌드 설정). |
| 2026-07-09 | 인게임/로비 볼륨·음소거·프로필 버튼 UI 로직 연결 완료 (2026-07-10 실기 PASS) — 사용자가 Unity Editor에서 재설계한 게임씬(인게임 설정 메뉴)·로비씬(프로필/설정 탭 분리) UI 껍데기에 실제 동작 로직을 연결하고, 실기 중 발견된 버그 3건을 함께 수정. **[음소거]** `AudioManager`에 `SetMuted(bool)`/`IsMuted()`/`ResetAllVolumes()` 추가 — 저장값 보존형(Master 채널만 -80dB(`MutedDb`)로 눌러 전체 무음, BGM/SFX 논리 볼륨값 보존). `ApplyVolume`→`ApplyDb(param,dB)` 리팩터로 무음·볼륨변환이 SetFloat 진단 로깅 공유. 슬라이더 조작 시 자동 언뮤트, PlayerPrefs 키 `"Muted"` 영속화. **[공용 로직]** `Presentation/UI/Common/VolumeControlBinder.cs`(순수 C#) 신규 — 인게임/로비 공통 볼륨 UI(슬라이더3+On/Off/Reset/Back+색상) 캡슐화, 프로그램 값 설정은 `SetValueWithoutNotify`로 자동 언뮤트 부작용 차단. **[색상 토큰]** `UIColorConfig`에 `soundOnColor`(초록)/`soundMutedColor`(빨강) 추가. 별개로 미사용 `confirmButtonColor`/`cancelButtonColor` 죽은 코드 제거(→ `ConfirmPopup.Awake()`/`_colorConfig` 제거). **[인게임]** `InGameSettingsUI` 프로필 버튼 추가(사운드 버튼과 동일 CanvasGroup 패턴, 규칙6, 내부는 범위 밖 빈 토글) + VolumeControlBinder 연동. **[로비]** `LobbySettingsView` Profile 로직 제거·Setting 전용화, 컴포넌트를 자식→`SettingPanel` 루트로 이동. **[버그1 — 로비 설정 탭 배선 누락]** `LobbyViewModel.LobbyTab` enum에 `Setting` 부재 + `TabBarView`/`LobbyRootView`에 설정 버튼·패널 참조 부재로 설정 탭이 클릭 무반응·항상 선택된 것처럼 표시 → enum 추가(Profile↔Ranking 사이)·버튼 바인딩·색상 갱신·패널 CanvasGroup 전환 완성(별도 task `_Tasks/2026-07-09/09_58_lobby-setting-tab-wiring/`). **[버그2 — 설정 닫힘 화면 깜빡임]** `InGameSettingsUI.Hide()`가 닫힐 때 서브패널을 메인으로 복원하는 부수효과로 메인 화면이 잠깐 비침 → 현재 화면 그대로 페이드아웃하도록 수정, `Show()`/`Initialize()`는 `ResetToMainView()`로 통합. **[버그3 — On/Off 버튼 위치·크기 불균등]** 전체소리켜기/전체음소거 버튼이 VerticalLayoutGroup에서 서로 다른 슬롯·크기 차지 → `MuteToggleSlot` 래퍼로 `SetParent()` 재부모화(파괴/재생성 없이 fileID 참조 보존)하여 완전 겹침. 이후 실기에서 추가 발견된 높이 불균등(빈 슬롯 선호높이 0)은 `LayoutElement.preferredHeight=0f`/`flexibleHeight=1f` 비율 가중치 방식으로 여러 차례 반복 수정 끝에 최종 확정(고정 픽셀 금지, 공통 규칙 2 준수). Editor 스크립트 `FixMuteToggleOverlap_20260709.cs`. **[Editor 1회성]** `SetupVolumeProfileUI_20260709.cs`(신규 필드 자동 배선·LobbySettingsView 컴포넌트 이동·UIColorConfig 참조 연결·ProfileSubView 자동 생성) — 사용자가 Unity에서 직접 실행하여 `Game.unity`/`Lobby.unity` 반영. **[규칙]** `GameSystemRules_UI.md` 규칙6·로비 ProfilePanel/SettingPanel 분리, `GameSystemRules_Sound.md` 규칙23~26 + 규칙26 음소거 내부 구현 확정 반영. 커밋 범위 `66c66797..87a1dd6d`. 교훈: 프로그램 슬라이더 값은 `SetValueWithoutNotify`; GameObject는 파괴 대신 `SetParent()` 재부모화로 Serialized 참조 보존; VerticalLayoutGroup 형제 크기 불균등은 `flexibleHeight` 비율 가중치로 해결; Editor 자동 배선의 이름 기반 매칭은 오연결 위험(`_backButton`이 `OffButton`에 잘못 연결된 사례) → 참조 적으면 수동 배선이 안전. task: `_Tasks/2026-07-09/06_09_ingame-lobby-volume-profile-ui/`, `_Tasks/2026-07-09/09_58_lobby-setting-tab-wiring/`. |
| 2026-07-08 | 사운드 시스템 실기 버그 3종 수정 완료 — 실기 테스트에서 발견된 3가지 버그 해결. 브랜치 `claude/sound-system-review-itwt0t`. **[BUG-1] BGM 씬 전환 시 소리 겹침**: `AudioManager.StartCrossfade()`가 새 전환 요청 시 `StopCoroutine(_crossfadeRoutine)`으로 코루틴만 중단하고, 페이드아웃 중이던 AudioSource는 `Stop()`하지 않아 이전 BGM이 계속 재생되던 문제. 코루틴 중단 직후 페이드아웃 중이던 채널(active가 아닌 채널)을 즉시 `Stop()`(+ volume 0, clip null)하여 강제 정리하도록 수정. `GameSystemRules_Sound.md` 규칙 8에 이 요건을 명문화(기존 규칙이 StopCoroutine만 기술하여 버그를 유발). **[BUG-2] 볼륨 조절 UI 규칙 위반**: 에디터 스크립트 `SetupInGameVolumePanel.cs` / `SetupLobbySettingsTab.cs`의 슬라이더 서브 요소(FillArea/HandleSlideArea/Handle) 고정 픽셀값을 앵커 비율 기반으로 교체(규칙 2 준수), 모든 TextMeshProUGUI에 `Maplestory Bold SDF` 폰트 적용(규칙 6 준수, 계획의 Light SDF에서 변경) + `EditorUtility.SetDirty()` 추가로 씬 저장 시 폰트 반영. 라벨 너비 확대·여백/간격 개선·BackButton lavender 스프라이트 적용·MainButtonContainer 패딩 확대(패널 테두리 겹침 해소). 최종 레이아웃 미세 조정은 사용자가 직접 수행. **[BUG-3] SFX 볼륨 슬라이더 미작동**: 사전 추정(Exposed Parameter 이름 불일치)과 달리 AudioMixer의 `MasterVolume`/`BGMVolume`/`SFXVolume` 세 이름 모두 정상 확인. `AudioManager.ApplyVolume()`에 `SetFloat` 실패 감지용 디버그 로깅을 추가하여 진단 경로 확보. 수정 파일: `Presentation/Audio/AudioManager.cs`, `Assets/Editor/Setup/SetupInGameVolumePanel.cs`, `Assets/Editor/Setup/SetupLobbySettingsTab.cs`, `GameSystemRules/GameSystemRules_Sound.md`. 교훈: BGM 크로스페이드 중단 시 코루틴 정지만으로는 부족 — 페이드아웃 채널의 AudioSource도 명시적으로 Stop 해야 겹침이 발생하지 않음. task: `_Tasks/2026-07-07/12_28_sound-system-bugfix/`. |
| 2026-06-27 | Google 로그인 실기 디버깅 완료 (로그인 성공) — 실기 기기에서 구글 로그인 시 계정 선택 UI 미표시 + 즉시 `signInStatus=Canceled` 반환 문제를 3가지 원인으로 분해하여 순차 해결. **[문제 1]** `FirebaseAuthService.cs`가 `PlayGamesPlatform.Instance.Authenticate()`(내부적으로 `isAuthenticated()`만 호출 → 기존 세션 없으면 무조건 Canceled)를 호출하던 것을 `ManuallyAuthenticate()`(`signIn()` 호출 → 계정 선택 UI 표시)로 변경(GPGS Plugin 2.1.0). **[문제 2]** Firebase Console에 SHA-1이 릴리즈 1개만 등록돼 있던 것을 디버그 + Play App Signing 추가하여 `google-services.json` SHA-1 3개로 보강·재다운로드. **[문제 3, 근본 원인]** logcat `PlayGamesServices[SignInAuthenticator]`에서 실제 APK 서명 SHA-1(`18:E0:32:5F:5A:F9:C5:A7:3F:22:34:BE:65:1F:E6:CA:61:2E:DE:3D`)을 확인한 결과 등록된 3개 어느 것과도 불일치 — 실제 빌드 `hexiege-release.keystore`가 SHA-1 등록 시 쓴 키스토어와 다른 파일이었음. 실제 SHA-1을 Firebase Console + Play Console GPGS 사용자 인증 정보에 추가 등록·게시하고 Firebase Authentication Play Games 제공업체 활성화(Web Client ID/Secret)하여 해결. 결과: `signInStatus=Success`, GPGS Server Auth Code 정상 발급(`length=0`→`73`), Google Firebase 로그인 성공(`UID=xdmWpVNyyvaBe0cB878mSg0URm83`, IsLoggedIn=True, IsAnonymous=False). 잔여 이슈: Firebase 로그인 직후 UGS OIDC 브릿지(`SignInWithOpenIdConnectAsync("oidc-firebase")`)가 `id provider not found`로 실패 — UGS Dashboard OIDC 제공자 미등록, 멀티플레이 제한(별도 미해결 이슈). 수정 파일: `Infrastructure/Auth/FirebaseAuthService.cs`, `Assets/google-services.json`. 외부 설정: Firebase Console / Play Console GPGS / Firebase Authentication. 교훈: SHA-1은 ① Firebase Console, ② Play Console GPGS 사용자 인증 정보, ③ 실제 빌드 키스토어 — 세 곳이 모두 일치해야 GPGS `signIn()` 성공. DEVELOPER_ERROR/즉시 Canceled 시 logcat의 실제 Cert SHA1 fingerprint를 우선 확인. task: `_Tasks/2026-06-27/12_26_google-login-debug/`. |
| 2026-06-26 | 방 만들기 취소 후 텍스트 잔류 버그 수정 완료 — 커스텀 게임 방 만들기를 취소한 뒤 다시 CustomHostPanel에 진입하면 이전 세션의 LobbyCode(방 코드), ConnectedPlayers(접속 인원), ErrorMessage(오류 메시지) 값이 그대로 남아 어색하게 표시되던 문제. `BattleViewModel.CancelHosting()` 메서드에 세 값 초기화 코드 3줄 추가(`LobbyCode.Value = ""`, `ConnectedPlayers.Value = 0`, `ErrorMessage.Value = ""`)하여 취소 시점에 패널 상태를 깨끗이 비움. 수정 파일: `Presentation/UI/ViewModels/BattleViewModel.cs`. main 머지 완료. |
| 2026-06-26 | 게임포기 시 로딩 인디케이터 미해제 버그 수정 완료 — 멀티플레이 게임포기(Forfeit) 시 `InGameSettingsUI.OnForfeitConfirmed()`에서 `UIManager.ShowLoading(true, "게임을 포기하는 중...")`를 호출한 뒤 씬 전환이 일어나지 않아 `ShowLoading(false)`가 호출되지 않고 로딩 인디케이터가 화면에 영구히 남던 버그. 포기는 씬 전환 없이 GameEndUI만 표시하므로 ShowLoading 호출 자체를 제거. `GameSystemRules_UI.md` 규칙 L-2에서 "게임 포기(멀티)" 항목 제거하고 "게임 포기(싱글/멀티 모두)는 씬 전환 없이 GameEndUI만 표시하므로 해당 없음" 명시. 수정 파일: `Presentation/UI/InGameSettingsUI.cs`, `GameSystemRules/GameSystemRules_UI.md`. 사용자 실기 PASS. 브랜치 `claude/game-quit-loading-indicator-0h3w0u`. 교훈: ShowLoading(true)는 씬 전환이 실제로 일어나는 경우에만 사용(ShowLoading(false) 책임자가 씬 전환 후 Bootstrapper/RootView이므로). task: `_Tasks/2026-06-26/02_16_forfeit-loading-indicator-stuck/`. |
| 2026-06-26 | IUnitFactory 인터페이스 도입 완료 (Bootstrap 의존성 제거 리팩토링) — Application → Infrastructure 역방향 의존 제거. 문제: `IGameServices`(Application 계층 인터페이스)의 `GetUnitFactory()`가 구체 클래스 `UnitFactory`(Infrastructure)를 반환 타입으로 노출하여 Application → Infrastructure 역방향 의존이 발생. 해소: 신규 파일 `Assets/_Project/Scripts/Application/Interfaces/IUnitFactory.cs` 추가(`GetUnitObject(int)` / `RegisterUnitObject(int, GameObject)` / `InitializeUnitView(UnitData)` 3개 멤버, `UnityEngine.GameObject` 참조 허용). `UnitFactory.cs`(Infrastructure)가 `IUnitFactory` 구현. `IGameServices.GetUnitFactory()` 반환 타입을 `UnitFactory` → `IUnitFactory`로 변경. 호출부 5개(`GameBootstrapper.cs`, `NetworkProductionController.cs`, `NetworkCombatController.cs`, `NetworkUnitMovementController.cs`, `NetworkUnit.cs`)가 `IUnitFactory` 타입으로 수신하도록 업데이트. 기존 `IEntityPositionProvider` / `IForfeitService`와 동일한 의존성 역전(인터페이스를 안쪽 Application 레이어에 선언, 바깥 레이어가 구현) 패턴. 동작 변경 없음 — 싱글/멀티플레이 실기 이상 없음(사용자 확인). TDD에 "의존성 방향 추상화(Application 인터페이스 패턴)" 섹션 신규 추가(IGameServices/IUnitFactory/IEntityPositionProvider/IForfeitService 정리). 브랜치 `claude/code-refactor-cleanup-jsa24o`. Task 문서 없음(별도 리팩토링 작업). |
| 2026-06-25 | 랜덤 매칭 2회차 실패 버그 수정 완료 — 첫 게임 완료 → 로비 복귀 → 2번째 랜덤 매칭 시 NGO "Cannot start Host while an instance is already running" + 로딩 무한 대기. 원인: `GameEndUI._networkGameManager` Inspector 미연결(null) → ReturnToLobby에서 BackToLobby 미호출 → NetworkManager.Shutdown 없이 씬 전환 → 2번째 매칭 시 IsListening=True 상태로 StartHost 재호출. 수정: `GameEndUI.Initialize()`에 `FindFirstObjectByType<NetworkGameManager>()` 자동 탐색 추가(LobbyUI 동일 패턴). 실기 PASS. 교훈: DontDestroyOnLoad 매니저 참조는 Inspector 연결 대신 자동 탐색이 안전. |
| 2026-06-25 | RuntimeLogger 유틸리티 생성 완료 — `Infrastructure/Debug/RuntimeLogger.cs` 신규. `BeginSession(folderPath, role)` / `Log(level, system, className, message, data)` / `EndSession()` API. `#if UNITY_EDITOR`에서 파일 기록, 항상 `Debug.Log` 출력(실기기 Logcat 대응). task: `_Tasks/2026-06-25/07_25_runtime-logger/`. |
| 2026-06-25 | GameBootstrapper.Setup.cs 하드코딩 배열 파생 완료 — 환불 캐시 초기화(`InitializeBuildingStatsFromConfig`)에서 손으로 나열하던 건물 목록 배열 2개를 `BuildingTypeHelper` 공개 API 파생으로 교체. ① `stage1Buildings`(1단계 생산건물 9개 하드코딩) → `Array.FindAll((BuildingType[])Enum.GetValues(typeof(BuildingType)), t => BuildingTypeHelper.GetStage(t) == 1)`. ② `nonProductionBuildings`(비생산 건물 6개, Castle 제외 하드코딩) → `Array.FindAll(..., t => !BuildingTypeHelper.IsProductionBuilding(t) && t != BuildingType.Castle)`. Setup.cs에 `using System;` 추가(Enum/Array 짧은 이름). 환불 캐시 foreach 루프(단계 체인 순회 + 비생산 누적)는 변수명·타입(`BuildingType[]`) 동일 유지로 무변경. Phase 2 lookup table 통합의 연장선 — 건물 목록 단일 소스화 완성으로 **신규 생산건물 추가 시 `BuildingTypeHelper._buildingTable` 한 줄 추가만으로 환불 캐시 목록까지 자동 반영**(Setup.cs 무수정). 선택지: 안 1(헬퍼에 목록 조회 공개 메서드 추가) vs 안 2(Setup.cs에서 기존 공개 API로 직접 파생) 중 안 2(도메인 레이어 무변경) 선택, 곧바로 교체(파생 목록이 기존 배열과 값 동치·환불 계산 순서 무관임을 Research에서 검증). 동작/환불 값 불변. 사용자 실기 PASS(생산/비생산 건물 철거 환불 금액 정상). 커밋 `8d74e06`(main). task: `_Tasks/2026-06-25/07_23_하드코딩배열-파생/`. |
| 2026-06-25 | 코드 구조 개선 Phase 2 완료 — 동작 보존 리팩토링(SINGLE 7 + MULTI 2 전 항목 사용자 실기 PASS). ① `BuildingTypeHelper.cs`: IsProductionBuilding/GetStage/GetNextStage 3개 switch가 동일 건물 목록을 중복 나열하던 것을 단일 `Dictionary<BuildingType, BuildingMeta>` lookup table로 통합. `BuildingMeta` private readonly struct(IsProduction/Stage/NextStage). 세 메서드는 TryGetValue 단순 조회(미등록=비생산). 신규 생산건물 추가 시 table 한 줄만 추가하면 세 질문 자동 정합 → 세 곳 분산 관리로 인한 데이터 불일치 위험 제거. CanUpgrade/CanShowActionPanel은 호출 기반이라 무수정 자동 반영. 시그니처/반환타입 동일 → 호출부 무영향. PrimalSanctuary(동물A 3단계)는 초기 Research에서 IsProductionBuilding 누락 의심했으나 qa-tester 정적분석 결과 기존 switch에도 포함돼 있던 항목(오판)으로 확인 → table에 `(true,3)` 명시하여 동작 보존. ② `GameBootstrapper.Network.cs`: StartNetworkGame의 HexMetrics 수동 설정 4줄 → `ApplyConfig(HexOrientation.FlatTop, oc)` 1줄로 대체. ApplyConfig가 UnitYOffset까지 설정 → 수동 4줄에 빠져 있던 부분중복 해소. ApplyConfig 멱등(멀티 경로 StartNetworkGame 1회 + LoadMap 내부 1회 = 2회 실행되나 같은 값 재대입이라 무해). 순서 제약(ApplyConfig→GridCenter→ViewConverter.Setup→LoadMap) 유지, 싱글 경로 미변경. 기존 switch 3개 본문 + 수동 4줄은 주석 처리로 보존(사용자 지침상 별도 삭제 지시 시 제거). 브랜치 `claude/code-refactor-phase2-structural`(커밋 3838c4d). task: `_Tasks/2026-06-23/15_37_구조개선-Phase2/`. |
| 2026-06-23 | document-manager 에이전트 신규 생성 — 프로젝트 전체 문서(CLAUDE.md, AGENTS.md, WORKFLOW.md, 설계 문서, 메모리 파일, Task 문서 등) 통합 관리 전담. `.claude/agents/document-manager.md` + `.claude/agent-memory/document-manager/MEMORY.md` 생성. AGENTS.md 에이전트 역할 표·메모리 경로 표·업데이트 체크리스트 항목 인덱싱 완료. |
| 2026-06-23 | 코드 정리(클린업) Phase 1 완료 — 히스토리성 주석 및 폐기 코드 제거. 약 30개 파일에서 `[2026-XX-XX]`/`[Phase X]` 형식 이력 라벨 + 구방식→현재방식 전환 설명 주석 제거. 폐기 코드 블록 제거(`GameBootstrapper.cs` `_enableAI` 주석 처리 블록 + `_confirmPopup` 전환 설명 블록). `NetworkGameFlow.cs` 빈 섹션 헤더 제거. `GameBootstrapper.Setup.cs` 중복 RaceId 배열 → 지역 변수 1개 통합(원소·순서 동일 → 환불 캐시 결과 불변). 순수 주석/폐기코드 정리로 런타임 동작 변경 없음. 미래 사용 의도 주석은 미발견(플래그 없음). 브랜치 `claude/code-refactor-cleanup-jsa24o`. task: `_Tasks/2026-06-23/00_00_코드정리-클린업/`. 구조 변경(switch→Dictionary 등)은 Phase 2로 별도 진행. |
| 2026-06-23 | 스플래시 화면 로그인 흐름 개선 완료 — 로그인된 상태 재실행 시 스플래시 → "Tap to Start" → 탭 → 로비 직접 이동(FadeOut 없음). `SplashOverlayView`에 `_skipFadeOnTap` 필드 + `SetTapCallback(callback, skipFade=false)` 파라미터 추가. `OnPointerClick` skipFade 분기로 즉시 콜백 실행. `LoginBootstrapper` 자동 로그인 성공 분기에서 `skipFade:true` 적용. 로그인 X 기존 흐름(FadeOut → 로그인 화면 노출) 변화 없음. |
| 2026-06-23 | 로그인 팝업 CloseButton 무반응 수정 완료 — AnonymousWarningPopup / NetworkErrorPopup 두 팝업에 `_closeButton` SerializeField 추가 + Inspector 연결. CloseButton GO가 씬에 활성화 상태로 존재했으나 코드 필드 누락으로 클릭 무반응이던 버그 해결. |
| 2026-06-23 | LoadingIndicator 전수 적용 완료 — SceneLoader 정적 유틸리티 신규, 모든 씬 전환(로그아웃/포기/로비복귀/재경기/연결끊김) ShowLoading 커버. UIManager.LoadSceneWithDelay 텍스트 지연 버그 수정(ShowLoading 코루틴 외부로 이동). AnonymousWarningPopup 초기 메시지 누락 수정("로그인 중..."). NetworkGameManager Infrastructure→Presentation 레이어 위반 수정(GameEvents.OnNetworkBackToLobby 이벤트 경유). GameSystemRules_UI.md 로딩 규칙 L-1~L-4 신설. |
| 2026-06-22 | ConfirmPopup z-order 버그 수정 완료 — InGameSettings Panel(Canvas SO=200)이 ConfirmPopup 위에 렌더링되던 문제. ConfirmPopup.prefab 루트에 Canvas(Override Sorting=true, SO=250) + GraphicRaycaster 추가(Inspector 직접 작업). Canvas SortingOrder 전체 구조 확정(SO=0/100/200/250/300) 및 GameSystemRules_CanvasSortingOrder.md 신규 작성. Fix_AddCanvasToConfirmPopup.cs 에디터 스크립트 작성(LoadPrefabContents 환경에서 SerializedObject 방식 필수). |
| 2026-06-22 | BlockingOverlay 씬 전환 버그 수정 완료 — DontDestroyOnLoad 루트 GO 제약. Canvas SO 구조(0/100/200/300) 수립. RuntimeLogWriter [DEBUG-TEMP] 코드 제거. |
| 2026-06-20 | LogRules.md 작성 완료 — 런타임 로그 + QA-Fix 로그 규칙 통합. 실기기 Logcat 공유 방식 확정. WORKFLOW.md Log.md 섹션 이동. |
| 2026-02 이전 | 싱글플레이 코어 루프 완성 (헥스, 전투, 건물, 생산, 승패) |
| 2026-02 | 멀티플레이 Phase 1~8 완성 |
| 2026-02-27~03-01 | 2D→3D 전환 완료 (XZ 좌표계, 55도 카메라, 3D 모델) |
| 2026-03-02 | 전투 거리 정밀도 버그 수정 (IEntityPositionProvider) |
| 2026-03-02 | GameConfig 정리 (AnimationFps 제거, TileHeight 수정) |
| 2026-03-07 | 공격 방향 Transform 기반 구현 완료 (UnitView._meshYOffset=30f, Atan2 기반 방향 계산) |
| 2026-03-07 | 유닛별 AttackCooldown 시스템 완료 (UnitData.AttackCooldown/AttackCooldownRemaining) |
| 2026-03-01 | 생산 큐 클라이언트 UI 즉시 갱신 수정 (OnProductionQueueChanged → SyncQueueStateClientRpc) |
| 2026-03-07 | 자동생산 멀티플레이 지원 완료 (ToggleAutoServerRpc + AutoProductionChangedClientRpc) |
| 2026-03-07 | Siege/AI 이동 서버 권위 동기화 완료 (BroadcastServerMove + BroadcastMoveClientRpc, 클라이언트 화면 불일치 수정) |
| 2026-03-13 | 팀별 피아식별 프리팹 에셋 완료 (Castle/Barracks/Pistoleer/Assault/Sniper Blue+Red) |
| 2026-03-13 | 반응형 팝업 UI 완료 (ProductionPopup/BuildingPopup 앵커 기반 배치) |
| 2026-03-14 | 팀별 초상화 동적 업데이트 완료 (ProductionPanelUI/BuildingPlacementUI — Show() 시 팀별 스프라이트 교체) |
| 2026-03-14 | 전투 범위 임계값 수정 (UnitCombatUseCase epsilon 제거 → 타일 중심 간 정확한 거리 기준) |
| 2026-03-14 | 팀별 프리팹 코드 연동 완료 (UnitFactory 팀+타입별, BuildingFactory 팀별 분기) |
| 2026-03-14 | Assault/Sniper 코드 연동 완료 (UnitType enum, UnitStats, UnitProductionStats, ProductionPanelUI 생산 버튼) |
| 2026-03-14 | 공격 애니메이션-타격 시각 동기화 완료 (AnimationEventRelay + Animation Event + scale punch) |
| 2026-03-14 | 유닛 스탯 재조정 (ATK: Pistoleer=6, Assault=1, Sniper=10 / cooldown=클립 길이 기준 DPS 산출) |
| 2026-03-14 | 유닛 메시 방향 보정 완료 (하위 Mesh Y=30°, Root Motion OFF, _meshYOffset=공격 전용) |
| 2026-04-29 | 유닛 메시 방향 보정 전면 통일 — 전 유닛(9종) Mesh Y=0, _meshYOffset 코드 제거, 이동 anim offset=0, DirectionAngles={60,120,180,240,300,0} (FlatTop 실제 월드 각도로 재계산) |
| 2026-03-14 | 유닛 회전 DOTween 보간 완료 (이동/공격 모두 DORotate + Ease.OutQuad, _rotationDuration SerializeField) |
| 2026-03-14 | 공격 후 Walk 복귀 버그 수정 (타겟 소멸 시 Play(StateWalk) 명시 호출, 멀티/싱글 공통) |
| 2026-03-15 | 로비 씬 분리 MVVM 완료 (Lobby/Game 씬 분리, UniRx 기반 MVVM, 탭 4개 + 전투 서브화면 5개, 씬 빌드 완료) |
| 2026-03-15 | 멀티플레이 게임 시작 버그 수정 (Host OnClientConnectedCallback 누락 → HandleClientConnected 추가) |
| 2026-03-16 | 랜덤 매칭 게임 씬 전환 버그 수정 (GetHashCode 크로스-프로세스 비결정성 → GetStableHash polynomial hash 교체) |
| 2026-03-17 | 전역 로딩 스크린 완료 (LoadingScreen.cs 싱글턴, Lobby 씬 UI, 씬 전환 자동 Hide) |
| 2026-03-17 | 멀티플레이 로비 복귀 버그 수정 (Inspector _lobbySceneName 값 오류 발견, 로컬 독립 처리로 설계 변경, 30초 자동 복귀 타이머) |
| 2026-03-17 | 커스텀게임 재경기 시스템 완료 (양측 동의 재경기, RematchRequestPopup 신규, 레이스 컨디션 처리) |
| 2026-03-18 | 랜덤매칭 재경기 지원 완료 (커스텀게임과 동일 흐름) |
| 2026-03-18 | 건물 인근 타일 이동/공격 불가 버그 수정 (HexPathfinder goal blocked 체크 제거, Epsilon=0.05f 추가) |
| 2026-03-19 | 카메라 줌 DOTween 보간 완료 (CameraController _targetZoom + DOTween.To Ease.OutCubic) |
| 2026-03-19 | UI DOTween 애니메이션 프레임워크 완료 (UIAnimator + AnimatedPanel, SlideFromTop/Bottom, blocksRaycasts 버그 수정) |
| 2026-03-20 | 코드 정리 완료 (TeamAssigner.cs 삭제, 주석 정리, IsNetworkMode() 헬퍼 추출) |
| 2026-03-20 | 싱글플레이 ViewConverter 초기화 버그 수정 (Reset() → LocalPlayerTeam 기반 Setup, ApplyConfig() 직후 호출) |
| 2026-03-23 | 자동/수동 생산 하이브리드 시스템 완성 (전역 규칙 5가지, IsCharged 기반 골드 차감, BUG-01~13 수정) |
| 2026-03-24 | Game UI Lifecycle Framework 완료 (IGameUI 인터페이스 + GameUIManager, 멀티플레이 클라이언트 팝업 버그 수정) |
| 2026-03-26 | 유닛 NGO NetworkObject 전환 완료 (NetworkTransform 위치 동기화, 클라이언트 예측 제거) |
| 2026-03-27 | 이동 전 회전 선행 완료 (Rotate-then-Move, _isPreRotating 플래그) |
| 2026-03-27 | 공격 타이밍 정밀화 완료 (타격 프레임 데미지, 타겟 고정, 쿨다운 통일) |
| 2026-04-04 | 전투 애니메이션 시스템 재정비 완료 (3-신호 RPC, 6가지 규칙, _combatAnimationSent 경쟁조건 수정) |
| 2026-04-04 | 자동생산 BUG-20 수정 (CompleteProduction IsCharged 리셋 누락) |
| 2026-04-06 | 로비 종족 선택 UI 완료 (RaceId enum 3종족, 캐러셀 방식, RenderTexture 캐릭터 미리보기, DOTween 전환) |
| 2026-04-06 | 종족명 자연→초월 변경 (Nature→Transcendence enum 전체 rename) |
| 2026-04-06 | Pistoleer Idle 애니메이션 버그 수정 (Pistoleer.controller Idle m_Speed 0→1) |
| 2026-04-06 | Android URP RenderTexture 잔상 + RenderPass 에러 수정 (RT antiAliasing 2→1, allowMSAA/allowHDR false) |
| 2026-04-07 | 종족 인게임 적용 완료 (UnitFactory/BuildingFactory 종족별 6세트 분기, 에디터 자동 연결 스크립트) |
| 2026-04-11 | UnitType 개편 + 근접 사거리 시스템 완료 (9종 독립 enum, FindPathToNeighbor, ClaimedTile non-walkable 예외) |
| 2026-04-11 | 근접 공격 거리 다듬기 완료 (MeleeContactDist=0.3f, BuildingDetectionRadius=0.2f, 타겟 타입별 분리) |
| 2026-04-11~12 | 원거리 유닛 공격 중 회전 추적 완료 (Transform 참조, RotateTowards 270°/s, 타겟 고착성, 멀티 실기 MULTI-001~007 PASS) |
| 2026-04-12 | 종족+팀별 건물/유닛 초상화 완료 (BuildingRacePortraitSet 6세트, Inspector 연결) |
| 2026-04-13 | Spirit Blue ManaRift 초상화 스프라이트 제작 완료 (초상화 세트 전 종목 완성) |
| 2026-04-13 | 유닛/건물 스탯 확정 적용 완료 (Spirit/Transcendence 6종 HP/ATK/생산시간/비용, Transcendence 건물 HP 종족별 분기) |
| 2026-04-13 | 피격 시 부유 HP 텍스트 완료 (FloatingHpText DOTween, 오브젝트 풀, 줌 스케일링, 멀티 클라이언트 표시) |
| 2026-04-13 | 부유 텍스트 팀별 색상 완료 (Blue=연두, Red=노랑, Inspector 조정 가능) |
| 2026-04-18 | 타겟 고정(Target Lock) 데미지 불일치 버그 수정 — 멀티플레이에서 바라보는 타겟(B)이 아닌 가까운 유닛(C)에게 데미지가 적용되던 버그. NetworkCombatController.TickCombat() damageTargetId 지역 변수 분리로 수정. 멀티 실기 PASS |
| 2026-04-19 | 유닛 생산 패널 전면 재작성 완료 — PendingQueue 단일 큐 구조(QueueSlot), 자동/수동 통합, CancelAutoTypeIfNeeded 헬퍼 추가. 싱글 실기 TC-001~018 전체 PASS |
| 2026-04-19 | 생산 슬롯 깜빡임 버그 수정 — 큐 비어있을 때 자동 등록 시 슬롯1→슬롯0 1프레임 이동 버그. ToggleAutoProduction에서 !CurrentProducing.HasValue이면 즉시 TryStartNext 호출로 수정 |
| 2026-04-30 | 종족/팀 초상화 및 생산 연동 정비 — UI 스킨 로직 제거, 종족별 Unit/Building Entries 리스트 기반 동적 바인딩 및 프리팹 생성 일치화 완료. 인스펙터 구조 단순화. |
| 2026-05-14 | 유닛 회전 시스템 전면 개편 완료 — 방향 계산 Atan2 통일 (FacingDirection.FromCoords 제거), A*/정렬/추격 모든 단계 RotateTowards 적용, _rotationSpeed 단일 Inspector 필드로 통일. MovementLogger.cs 삭제. |
| 2026-05-15 | 랜덤 매칭 후 캐릭터 잘못 표시 버그 수정 — Lobby 씬 CharPreview 오브젝트가 실제 유닛 프리팹(NetworkTransform 자동 추가됨) 인스턴스여서 Host의 캐러셀 위치가 Red 클라이언트로 동기화되던 원인 확정. Unpack Completely 후 UnitView/AnimationEventRelay/NetworkUnit/NetworkTransform/NetworkObject 5종 제거. |
| 2026-05-15 | 혼잡도 기반 유닛 분산 시스템(v2) 완료 — 모든 유닛이 세로 줄지어 이동하는 현상 개선. 타일별 혼잡도 누적(CongestionMap) + 혼잡도 가중 A*(CongestionAwarePathfinder) 도입. CastleApproachManager(v1) 폐기 삭제. 설정값(DecayInterval/CongestionWeight)은 GameConfig에 통합. 사용자 테스트 PASS. |
| 2026-05-16 | 랠리포인트 깃발 팀별 표시 분리 완료 — 클라이언트 설정 시 호스트에도 깃발이 표시되던 버그 수정. RallyPointChangedEvent에 TeamId 추가, ProductionTicker에 로컬 팀 필터 추가. 싱글플레이 영향 없음. |
| 2026-05-16 | 유닛 생산 실패 피드백 시스템 완료 — 골드 부족/인구 초과/큐 초과 시 토스트 메시지 표시. 유닛별 생산 비용 텍스트 빨간색, HUD 인구수 텍스트 빨간색. 자동 생산 자원 부족 시 즉시 취소(IsCharged=false만). 범용 ToastUI 시스템(DontDestroyOnLoad, 큐, DOTween 페이드아웃) + ToastMessageConfig ScriptableObject 신규 구현. |
| 2026-05-16 | 건물 배치 패널 실패 피드백 + UI 개선 완료 — 골드 부족 시 건물 비용 텍스트 빨간색(OnResourceChanged 실시간 갱신) + 토스트 메시지 + 팝업 유지. 비용 텍스트 'G' 접미사 제거(숫자만 표시). ToastUI SetActive 버그 수정(ClearAll/FinishCurrent의 SetActive 제거 → blocksRaycasts+interactable로 대체). |
| 2026-05-17 | 건물 생성/파괴 시 유닛 이동 멈춤 수정 완료 — 경로 재계산 시 코루틴을 즉시 재시작하지 않고 _pendingPath 예약 방식으로 교체. 앞 타일이 막힌 경우에만 즉시 재시작. UnitView.cs 단독 수정. |
| 2026-05-17 | 자동 생산 Rule 20 슬롯0 확장 완료 — 슬롯0에서 수동 A 생산 중 A 자동등록 시 슬롯1에 중복 추가 없이 슬롯0을 자동으로 전환(CurrentIsAuto=true). 완료 후 자동 순환 자연 시작. GameSystemRules.md 규칙 20 문구 업데이트. |
| 2026-05-17~18 | 건물 업그레이드 시스템 완성 — BuildingType enum 26종 확장(단일 Barracks → 종족별 생산라인×3단계). BuildingTypeHelper.cs 신설(Domain: IsProductionBuilding/GetStage/GetNextStage/CanUpgrade). BuildingData.Stage 파생 프로퍼티. BuildingStats.GetUpgradeCost() + _totalInvestedCostCache(누적 투자비). GameEvents.OnBuildingUpgraded 이벤트. BuildingPlacementUseCase.UpgradeBuilding(). BuildingFactory.UpgradeBuildingObject(). NetworkBuildingController RequestUpgradeServerRpc/UpgradeBuildingClientRpc. ProductionPanelUI BuildingUnitMapping + requiredStage 단계별 잠금 + UpgradeButton + ToastKey.UpgradeRequired. GameBootstrapper 누적 투자비 캐싱. 신규 3D 에셋(HumanBarracks2/3, AncientGrove1/2/3, PrimalSanctuary1/2/3) Blue/Red 완료. |
| 2026-05-18 | 건물 스탯 전체 확정 + BuildingStatsConfig 완성 — StatsReference.md 기준으로 3종족 전체 건물(기지/채굴소/배럭/방어포탑/특수) HP·비용·공격력·업그레이드비용·힐량 확정. BuildingStatsConfig.asset 3개 항목 → 32개 BuildingType 전체 채움. BuildingTypeEntry에 종족별 AttackCooldown(float) 필드 추가, BuildingStats.GetAttackCooldown() API 신규. AutoTower 쿨다운 적용(Human/Trans 5.0s, Spirit 3.5s). MistShrine 힐량 1 HP/s 범위 3 확정. |
| 2026-05-18 | ProductionPopup UI 레이아웃 재구성 완료 — BuildingIconEntry 팀별 Sprite 분리(blueIcon/redIcon), 2유닛 건물 [유닛1][빈슬롯][유닛2] 레이아웃(CanvasGroup alpha=0 방식), UpdateButtonPortraits() 2유닛 슬롯 매핑 수정, HeaderText 건물 이름 동적 표시, 철거 환불 누적 계산(GameBootstrapper 초기화 시 체인 순회 캐싱 → BuildingStats.GetTotalInvestedCost()). |
| 2026-05-18 | 2/3단계 건물 랠리 마커 미표시 버그 수정 — ProductionTicker가 OnBuildingUpgraded를 구독하지 않아 업그레이드된 건물이 UnitProductionUseCase._states에 미등록되는 문제. OnBuildingUpgraded 핸들러 추가: UnregisterBarracks(OldBuildingId) + RegisterBarracks(NewBuilding). 전 종족(Human/Spirit/Transcendence) 테스트 통과. |
| 2026-05-18 | 건물 철거 시스템 완료 — 생산 건물 철거 버튼(50% 골드 환불 + 생산 큐 전액 환불). UnitProductionUseCase.CancelAllQueue() 신규(랠리포인트 제거 → 진행 중 유닛 환불 → PendingQueue IsCharged 항목 환불 → UnregisterBarracks). BuildingPlacementUseCase.DemolishBuilding() 신규(OnBuildingDied 발행 → RemoveBuilding). 멀티: RequestDemolishServerRpc/DemolishBuildingClientRpc. BuildingFactory OnBuildingDied 구독(B방식: _buildingObjects Dict O(1) GO 파괴). BuildingView.cs + MiningEffectView.cs 삭제(미사용 코드 정리). 채굴소 철거 UI는 별도 작업으로 연기. |
| 2026-05-18 | OnEntityDied 이벤트 분리 리팩토링 완료 — 단일 공용 이벤트(OnEntityDied/EntityDiedEvent) 삭제 → OnUnitDied(UnitDiedEvent) + OnBuildingDied(BuildingDiedEvent) 강타입 이벤트로 분리. 발행 4곳·구독 9곳 전면 교체(13개 파일). 구독자의 is-캐스팅 타입 필터 전면 제거. NetworkCombatController 서버 구독 단일→2개 분리. RPC 시그니처(EntityDiedClientRpc)는 호환성 유지. |
| 2026-05-18~19 | 비생산 건물 공용 액션 패널 UI 완료 — BuildingPanelBase 추상 베이스(Template Method 패턴) 신규. BuildingActionPanelUI(비생산 건물 팝업 + 철거 버튼) 신규. ProductionPanelUI BuildingPanelBase 상속 리팩토링. InputHandler CanShowActionPanel 분기 추가. GameBootstrapper 비생산 건물 환불 캐시 루프 추가. SetupBuildingActionPanelUI 에디터 스크립트 신규. |
| 2026-05-19 | 인게임 설정 메뉴 + 게임 포기 기능 완료 — InGameSettingsUI(IGameUI, 싱글 일시정지 timeScale=0, SharedBackground 등록) + ConfirmPopup(범용 확인 팝업, BlockingOverlay 공유 Background 차단) + GameEndUseCase.Forfeit()(싱글 포기) + NetworkGameEndController.ForfeitServerRpc(RequireOwnership=false, AnnounceWinnerClientRpc 재사용) + SetupInGameSettingsUI 에디터 스크립트(HUD 재배치 + 패널 생성 + 배선). AnimatedPanel._backgroundOverlay 배선 추가. |
| 2026-05-23 | AuthSystemRules.md 작성 완료 — Firebase Auth 기반 로그인 시스템 설계 규칙 확정. 로그인 방식 3종(익명/Google Play Games/이메일+비밀번호), Firebase UID → UGS 브릿지(SignInWithCustomIdAsync), 백엔드 Option A(Firebase 생태계: Firestore 실시간 리더보드 + Google Play Billing IAP) 확정. Login.unity 별도 씬으로 분리 예정. |
| 2026-05-24 | 로그인 시스템 C# 구현 완료 — Firebase SDK v13.11.0 + GPGS v2.1.0 설치. 신규 파일 11개(FirebaseAuthService, LoginUseCase, AccountLinkUseCase, LoginBootstrapper, LoginRootView, LoginSelectView, EmailLoginView, SignUpView, EmailVerifyView, PasswordResetView, AnonymousWarningPopup) + 기존 2개 수정(UnityServicesInitializer 폴백 익명 로그인 추가, ProfileView 구현). 컴파일 에러 전체 해결(CS0103/CS0029/CS1061/CS0234). 기존 UGS 익명 로그인 폴백으로 멀티플레이 기능 정상 동작 유지. Firebase/Google 기능은 컴파일만, 런타임 활성화는 추후 진행 예정. SignInWithCustomIdAsync 미지원으로 Firebase→UGS 브릿지는 임시 익명 로그인 처리. |
| 2026-05-24 | UGS 401 Unauthorized 버그 수정 — Lobby/Relay API 호출 시 HTTP 401 에러로 매칭 실패. 원인: IsSignedIn=true(기기 캐시)이지만 서버 토큰이 만료된 상태에서 재로그인을 건너뛰어 만료된 토큰으로 UGS API 호출. UnityServicesInitializer.InitializeAsync()를 항상 SignOut() → SignInAnonymouslyAsync() 순으로 수정하여 매 초기화 시 유효한 토큰 보장. 커스텀 게임 + 랜덤 매칭 모두 정상 동작 확인. |
| 2026-05-25 | 공통 UI 규칙 수립 및 CanvasGroup Rule 5 전환 완료 — 공통 UI 규칙 10개 확정(GameSystemRules.md). 로비 7개 뷰(LobbyRootView, BattleMainView, CustomGameView, CustomHostView, CustomJoinView, RandomMatchView, ProfileView) SetActive → CanvasGroup(alpha/blocksRaycasts/interactable) 전환. 랜덤 매칭 대기화면 GameObj inactive 버그 Inspector 직접 수정. |
| 2026-05-26 | 로비 배경 Safe Area 수정 완료 — LobbyRoot Image가 SafeAreaContainer 안에 위치해 Safe Area 경계에서 배경이 끊기는 버그 수정. Canvas 직속 자식 LobbyBackground 오브젝트 신규 추가(전체화면 stretch, 남색), LobbyRoot Image 비활성화. FixLobbyBackground.cs 에디터 스크립트로 적용. 실기기 테스트 PASS. |
| 2026-05-27 | 로비 CanvasGroup Rule 5 전환 실기 테스트 완료 — TC-SINGLE-001~014 전체 PASS. TC-SINGLE-001(기본 탭 표시), TC-SINGLE-002~005(탭 전환), TC-SINGLE-006~008(커스텀 게임 흐름), TC-SINGLE-009~010(에러 메시지 표시/사라짐), TC-SINGLE-011(방 참가), TC-SINGLE-012~014(랜덤 매칭). TC-SINGLE-015~016(로그인 섹션 전환)은 로그인 미구현으로 SKIP. |
| 2026-05-29 | BuildingActionPanelUI 씬 계층 재설계 + 런타임 슬롯 제어 완료 — TC-SINGLE-BAP-001 FAIL(패널 높이, X버튼 위치 불일치) 해결. BuildingPlacementUI와 동일한 3x3 VLG+HLG 그리드 구조. 래퍼 anchoredPosition/sizeDelta 오프셋 제거. CancelButton 위치 통일. BuildingActionPanelUI.cs에 _allSlotButtons/_activeSlotButtons 필드 추가, OnShow() 오버라이드에서 런타임 CanvasGroup alpha 제어(BuildingPlacementUI._buttonCanvasGroups 패턴 동일). HeaderText 앵커 순수 앵커 기반 변환. |
| 2026-05-30 | 로비 UI 에셋 제작 완료 — TabBar 아이콘 4종(Battle/Shop/Profile/Ranking), 공통 아이콘 2종(Settings/Quit), 로비 버튼 아이콘 9종(SinglePlay/RandomMatch/CustomGame/CreateRoom/JoinByCode/Email/Logout/Back/Cancel), 버튼 배경 2종(Primary/Secondary), 랜덤 매칭 스피너 1종(HexOrb). 총 18종 에셋. 저장 경로: Assets/_Project/Sprites/UI/. AssetList.md 업데이트 완료. |
| 2026-05-29 | BuildingPlacementUI 씬 계층 재설계 완료 — TC-SINGLE-BP-001/002 FAIL(패널 높이 부족, 버튼 테두리 침범, 골드 아이콘 정렬 불일치) 해결. GridLayoutGroup 제거 → VLG+HLG 중첩 구조(GameSystemRules Rule 2). BuildingPanel anchor=(0,0)~(1,0.4). GridContainer anchor=(0.08,0.123)~(0.92,0.864). CancelButton anchor=(0.883,0.852)~(0.993,0.97). 모두 순수 앵커 기반(anchoredPosition=0, sizeDelta=0). 버튼 내부: IconImage(flexibleWidth=6) + CostContainer(flexibleWidth=4) → GoldIcon(ui_icon_gold, 44px) + CostText(Maplestory Light SDF). BuildingPlacementUI.cs _buildingGoldIcons 필드 추가. GameSystemRules.md Rule 2 Layout Group 반응형 패턴 보완. GameSystemRules Rule 2/4/5/6 전체 준수 검증 완료. RebuildBuildingPlacementUI.cs 에디터 스크립트 신규. |
| 2026-05-31 | 건물 업그레이드 생산 상태 처리 오류 수정 완료 — 업그레이드 시 생산 중 골드 환불 누락 + 랠리포인트 초기화 버그 2건 수정. ProductionTicker.OnBuildingUpgraded()에서 UnregisterBarracks → CancelAllQueue 교체(환불 포함), CancelAllQueue 호출 전 RallyPoint 저장 후 RegisterBarracks 완료 시점에 SetRallyPoint로 복원. 수정 파일: ProductionTicker.cs 1개. 실기 테스트 PASS(골드 환불 확인, 랠리포인트 유지 확인). |
| 2026-06-01 | 방어 타워(AutoTower) 공격 기능 구현 완료 — TowerCombatUseCase 신규(Application 레이어). 종족별 스탯(사거리 4.0/공격력 15, 쿨다운 Human·Trans 5.0s/Spirit 3.5s). 타겟 선택: 월드 좌표 기준 가장 가까운 적 유닛. 배치 즉시 공격 가능(쿨다운 0 초기값). 멀티플레이 서버 권위 처리(NetworkCombatController.TickCombat에 타워 Tick 추가). 실기 테스트 PASS. |
| 2026-06-05 | 자동생산 재등록 슬롯 중복/누락 버그 구조 개선 — CurrentIsAuto를 수동 필드에서 파생 계산 getter로 전환(IsAutoMode와 동일 패턴). AutoTypes에서 타입 제거 시 getter가 자동으로 false 반환 → UnregisterAutoType·DisableAutoMode 수동 reset 불필요. RegisterAutoType에 PendingQueue.Count==0 조건 추가로 큐에 다른 항목 있을 때 슬롯3 추가 허용. GameSystemRules 규칙 20 보완. TC-01~06 정적분석+실기 전체 PASS. |
| 2026-06-08 | 전체 유닛 사망 VFX 적용 완료 — EffectPreset_Unit_Death_Common.asset 신규 생성(vfx_unit_death.prefab + 사망 SFX). SetUnitDeathVfxAll 에디터 스크립트로 UnitEffectConfig 전체 24종 유닛 deathPreset 일괄 연결. 기존 EffectPreset_Pistoleer_Death.asset 삭제. 코드 변경 없음(에셋 작업만). |
| 2026-06-05 | 자동생산 완료 사이클 슬롯2 깜빡임 버그 수정 — 자동생산 완료 시 재순환 항목이 슬롯2에 1프레임 표시되다가 사라지는 버그. CompleteProduction에서 ChargeVisibleSlots+OnProductionQueueChanged 직접 발행 제거 → TryStartNext 즉시 호출로 대체. 2026-04-19 AddNewAutoSlot 수정과 동일 패턴(완료 사이클 경로가 미처 처리되지 않았던 것). 실기 테스트 PASS. |
| 2026-06-02 | Human CannonTower 초기 방향 설정 완료 — BuildingFactory.GetInitialRotation() 신규. "내 진영 vs 상대 진영" 기준으로 회전 결정(Blue/Red 팀 색깔 기준 아님). ViewConverter.IsFlipped로 로컬 팀 판별 → 상대 포탑 Y180도, 내 포탑 기본값. 실기 테스트 PASS. |
| 2026-06-02 | UnitStatsConfig 미사용 필드 제거 + 스탯 정비 완료 — AttackKind enum/Kind 필드/GetAttackKind() 제거(2026-05-11 비활성화 코드 삭제). occupancySize 제거(TileOccupancyManager 미구현). 유닛 9종 attackCooldown/hitFrameTimes를 StatsReference.md 기준으로 정비. StatsReference.md 업그레이드 비용 표기 출발점 기준으로 통일. ApplyStatsReference.cs 에디터 스크립트로 일괄 적용. |
| 2026-06-05 | BuildingView Missing Script 정리 완료 — Spirit/Transcendence 건물 프리팹 8개에서 삭제된 BuildingView 컴포넌트 참조 제거. Editor 스크립트 1회 실행으로 일괄 처리. |
| 2026-06-05 | 신규 유닛 프리팹 컴포넌트 부착 에디터 스크립트 완료 — Human 5종·Spirit 6종·Transcendence 5종 × Blue/Red 총 32개 프리팹에 UnitView/AnimationEventRelay/NetworkUnit 등 컴포넌트 자동 부착. Assets/Editor/Setup/SetupNewUnitPrefabs.cs. 실기 테스트 예정. |
| 2026-06-06 | NetworkGameManager 고아 필드 + Game씬 중복 NGM 제거 완료 — GameBootstrapper._networkGameManager SerializeField(코드 사용처 없는 고아 필드) 제거. Game.unity에 중복 배치된 NGM GameObject 제거(DontDestroyOnLoad 구조상 Lobby씬 NGM이 유지되므로 불필요). 싱글플레이+멀티플레이 실기 PASS. 콘솔 DontDestroyOnLoad 경고 제거. |
| 2026-06-08 | 멀티플레이 유닛 사망 GO 미파괴 + 이펙트 미재생 버그 수정 (NGO Despawn 패턴 정립) — 근본 원인: 서버 UnitView(Presentation)의 Destroy(gameObject) 호출이 NGO Despawn 메시지를 클라이언트에 전파하지 않음. 수정: NetworkCombatController(Infrastructure)에서 EntityDiedClientRpc 발행 직후 NetworkObject.Despawn(destroy:true) 명시 호출. UnitView에서 Unity.Netcode 직접 참조 완전 제거(레이어 규칙 준수 — NetworkContext 홀더 패턴으로 교체). 런타임 로그로 검증: 13킬 전체 OnNetworkDespawn 게임플레이 중 발생 + 이펙트 재생 완료 확인. 수정 파일: NetworkCombatController.cs, UnitView.cs, NetworkUnit.cs, EffectManager.cs. |
| 2026-06-08 | 유닛 VFX 디테일 개선 3종 완료 — ① VFX 프리팹 3개(vfx_pistoleer_attack/vfx_tank_attack/vfx_unit_death) ParticleSystem ScalingMode Local→Hierarchy 일괄 변환(VfxScalingModeFixer 에디터 스크립트). 루트 Transform Scale로 이펙트 크기 조절 가능. ② 피스톨러 공격 VFX 스폰 위치 개선 — UnitView `_vfxSpawnPoint` 필드 추가, VfxSpawnPoint GO(스켈레톤 본 손 부위 하위, 총구 위치)의 position만 참조 / rotation은 `Quaternion.LookRotation(transform.forward)` 고정(본 하위 배치로 _vfxSpawnPoint.rotation에 본 회전 ~(0,-90,-90)이 섞이는 문제). EffectManager.PlayUnitAttack 시그니처에 Quaternion rot 파라미터 추가. ③ vfx_unit_death 퍼짐 효과 제거 — 3개 PS startSpeed 모두 0으로 YAML 직접 수정. |
| 2026-06-10 | 전 종족 AI 빌드오더 시나리오 문서 완료 — Human 시나리오 골드 수지 검토 추가(A=900g/B=1,200g/C=1,400g, 5,300g 대비 최대 26% 확인). Spirit 3개 시나리오(Spirit-Inferno/Spirit-Torrent/Spirit-Quake) 신규 작성: 각 원소 라인 메인 집중형, 총 건물 비용 1,450g. Transcendence 3개 시나리오(Trans-Rush/Trans-Flora/Trans-Beast) 신규 작성: 초반 물량/균형/고테크 분화, 총 건물 비용 1,150~1,450g. 3종족 9개 시나리오 전원 5,300g 경제 범위 내 검증 완료. GameSystemRules_AI.md에 Spirit/Transcendence 건물-유닛 매핑 및 시나리오 참조 섹션 추가. |
| 2026-06-10 | AI 시나리오 ScriptableObject 3종족 개편 완료 — 종족별 단일 에셋(3시나리오 내장), Domain 레이어 아키텍처 정리(DifficultyLevel/BuildOrderStep/AIActionType), 종족 분기 로드 구현 |
| 2026-06-10 | 사운드 시스템 코드 구현 완료 — SoundConfig.cs(Infrastructure/Config) + AudioManager.cs(Presentation/Audio, DontDestroyOnLoad) 신규. BGM 크로스페이드(A/B 채널, 1초, 중단 재시작), SFX 풀(8개 한도, spatialBlend=0 2D, WaitForSecondsRealtime), 볼륨 3채널(Master/BGM/SFX, 0~1→dB, PlayerPrefs). EffectPreset.cs SFX 필드 제거(VFX 전용). EffectManager.cs SFX 코드 비활성화(주석). VFX+SFX 쌍 호출 복원(UnitView.OnAttackHit/OnUnitDied, NetworkUnit.OnNetworkDespawn). LoginBootstrapper AudioManager.Initialize 추가. InGameSettingsUI 볼륨 슬라이더(Master/BGM/SFX) 패널 연동. QA 정적분석 PASS(규칙 1~22 전체). Inspector 작업 + 실기 테스트 예정. 로비 볼륨 패널 미구현(별도 작업). |
| 2026-06-18 | 전역 UI 시스템 도입 완료 — UIManager(SingletonMonoBehaviour+IUIManager, DontDestroyOnLoad) + SplashOverlayView(DOTween 깜빡임+페이드아웃) + SpinnerRotator 신규. ConfirmPopup/LoadingIndicator Login 씬 1회 생성 전역 통합. ToastUI 씬 루트 직접 배치(자체 DontDestroyOnLoad). LoadingIndicator B안(Canvas/LoadingScreen.cs 제거 후 프리팹 추출). ShowLoading(bool, string) 단일 API로 씬 전환/Firebase/매칭 등 모든 로딩 통합. InGameSettingsUI/LoginRootView/ProfileView/BattleViewModel ConfirmPopup+LoadingScreen 직접 참조 → UIManager 전환. 에디터 스크립트 2종(ExtractUIManagerPrefabs, SetupUIManagerInScene). QA UI규칙 1/4/6 수정 포함. |
| 2026-06-15 | Lobby.unity 규칙 전수 재점검 및 추가 수정 완료 — YAML 173개 GO 파싱 전수 점검(Rule 1~6). Rule 5: LobbyUI._lobbyPanel 타입 GameObject→CanvasGroup 전환(Show/Hide 시 alpha/blocksRaycasts/interactable 제어). Rule 6: LoadingScreen>SafeAreaContainer>StatusText LiberationSans SDF→Maplestory Light SDF 교체(FixLobbyRuleViolations.cs 에디터 스크립트). BattleRootView.cs 미사용 using System 제거. Rule 1(Canvas Scaler)/Rule 2(앵커)/Rule 4(SafeArea) 전체 준수 확인. AnonymousWarningPopup._blockingOverlay 수정은 Login.unity 씬 작업으로 이관. |
| 2026-06-22 | ProfileView 로그아웃 버튼 UI 추가 완료 — `AddLogoutButtonToProfileView.cs` 에디터 스크립트. LogoutButton GO(Button+Image+TextMeshProUGUI "로그아웃", Maplestory Bold SDF) 생성 + `ProfileView._logoutButton` Inspector 자동 연결. 코드 변경 없음(ProfileView._logoutButton + OnLogoutClicked()는 기존 구현 완료 상태). Firebase 익명 로그인 후 로비 프로필 탭에서 로그아웃 → Login 씬 복귀 동작 확인. |
| 2026-06-22 | Lobby 탭 패널 CanvasGroup 에디터 사전 부착 완료 — `SetupLobbyPanelCanvasGroups.cs` 에디터 스크립트(`Hexiege/Setup/Lobby 패널 CanvasGroup 설정`). 비활성 상태였던 ShopPanel/ProfilePanel/RankingPanel 3개 활성화 + 4개 패널 모두 CanvasGroup 부착(BattlePanel alpha=1, 나머지 alpha=0). `LobbyRootView.Awake()` EnsureCanvasGroup() → GetComponent<CanvasGroup>() 교체, EnsureCanvasGroup() 헬퍼 삭제. 런타임 AddComponent 방식 폐기(Rule 5 원칙: 컴포넌트는 에디터에서 미리 부착). 실기 테스트 PASS. |
| 2026-06-22 | LoadingIndicator 최소 표시 시간 1초 보장 — `UIManager.ShowLoading(false)` 호출 시 표시 후 1초 미만이면 `WaitForSecondsRealtime`으로 지연 후 숨김. `_loadingMinDuration` SerializeField(기본 1f) 추가. 모든 호출부(LoginBootstrapper, EmailLoginView, BattleViewModel 등) 자동 적용. 실기 테스트 PASS. |
| 2026-06-22 | LoadingIndicator 독립 Canvas(300) 추가 — AnonymousWarningPopup(SortingOrder=200)이 LoadingIndicator를 가리는 문제 해결. `LoginUiSetup.cs` 에디터 스크립트에 `Hexiege/Setup/Login UI — LoadingIndicator 독립 Canvas 추가` 메뉴 항목 추가. LoadingIndicator에 독립 Canvas(SortingOrder=300, overrideSorting=true) + GraphicRaycaster 자동 부착. |
| 2026-06-22 | ConfirmPopup + NetworkErrorPopup 씬 버그 수정 — ① ConfirmPopup 루트 CanvasGroup 씬 프리팹 인스턴스 오버라이드(alpha=0, interactable=0) 수정 → 팝업 표시 및 버튼 클릭 정상화. ② NetworkErrorPopup에 남아있던 ConfirmPopup 컴포넌트 교체 → NetworkErrorPopup.cs로 교체, `_panel` 슬롯 연결. ③ LoginRootView + LoginBootstrapper의 `_networkErrorPopup` 슬롯 연결. 테스트 버튼을 통한 전체 흐름(ConfirmPopup → 확인 → NetworkErrorPopup) 정상 동작 확인. |
| 2026-06-10 | Firebase-UGS OIDC Bridge 구현 완료 — `SignInWithOpenIdConnectAsync("oidc-firebase")` 도입. 실계정(Google/이메일) 로그인 시 Firebase ID Token → UGS OIDC 연결로 기기 변경 후에도 PlayerId 일관성 보장. 익명 계정은 기존 UGS 익명 로그인 유지. UnityServicesInitializer OIDC 세션 덮어쓰기 방지 로직 적용. BridgeToUGSAsync Task<bool> 전환으로 UGS 미연결 상태 감지 가능. UGS 데이터 플랫폼(Cloud Save/Leaderboard/Economy) 채택 확정. Firebase Console + UGS Dashboard 설정 완료 후 실기 테스트 예정. |
| 2026-06-22 | BlockingOverlay 씬 전환 후 미표시 버그 수정 완료 — 파일 기반 RuntimeLog(RuntimeLogWriter.cs [DEBUG-TEMP]) 추가로 런타임 흐름 추적. 로그에서 Game 씬 전체 패널의 `UIManager.Instance=NULL` 및 `ApplyBlockingOverlayVisibility`가 세션 시작마다 새로 초기화됨 확인. 근본 원인: UIManager GO가 Login.unity에서 `[UI Systems]` 자식으로 배치되어 `DontDestroyOnLoad`가 루트 GO에만 동작하는 Unity 제약을 위반. Game 씬 전환 시 `[UI Systems]`와 함께 파괴됨. 수정: Login.unity에서 UIManager GO를 계층 루트로 이동(Inspector 직접 작업). Canvas SortingOrder 구조 확정: SO=0(HUD) / SO=100(UIManager BlockingOverlay+ConfirmPopup) / SO=200(패널 Canvas Override) / SO=300(LoadingIndicator). 디버깅 완료 후 RuntimeLogWriter.cs 및 [DEBUG-TEMP] 로그 호출 제거. 실기 테스트 PASS. |
| 2026-07-16 | 로비 프로필/랭킹 클라우드 연동 1차 완료 — UGS Cloud Save 기반 `PlayerProfileData`/`PlayerProfileUseCase`/`PlayerProfileService`, UGS Leaderboards 기반 `RankingUseCase`/`LeaderboardService`/`RankingView`/`RankRowView`를 main 작업 브랜치에 통합. 이메일 인증 완료 후 최초 로그인 시 `NicknameSetupView`를 거치도록 수정했고, 로비 Profile 탭에 닉네임 코드/전적/내 랭킹/닉네임 변경 팝업을 연결. Ranking 탭은 숨김 상태 자동 로드를 제거하고 탭 선택/수동 새로고침에서만 로드하도록 보정. Profile/Ranking/NicknameChangePopup 기본 UI 레이아웃을 런타임 보정 및 에디터 생성 스크립트 기준값에 반영. 후속 작업으로 이메일 인증 대기 화면의 동적 이메일 표시와 미인증 계정 이탈 처리 정책을 분리. |
| 2026-07-18 | 이메일 인증 플로우 보정 완료 — `EmailVerificationOrigin`을 추가해 신규 회원가입 직후 인증 대기와 기존 미인증 계정 로그인 후 인증 대기를 분리. `ShowEmailVerify(email, origin)`로 실제 입력 이메일을 명시 전달해 `[이메일 주소]`/stale 표시 문제를 보정. 신규 가입 인증 대기에서 뒤로가기를 누르면 확인 팝업 후 현재 미인증 Firebase 사용자를 삭제하고, 기존 미인증 로그인 경로에서는 계정 삭제 없이 로그아웃 후 이전 화면으로 복귀하도록 구현. 앱 종료/강제 종료는 가입 취소로 보지 않으며, 재실행 자동 로그인 경로에서 미인증 계정은 인증 화면으로, 인증 완료됐지만 닉네임이 없는 계정은 닉네임 설정 화면으로 복귀하도록 게이트를 보강. SplashOverlay는 Login 씬 내부 패널 이동 시 fade out되도록 수정하고 Lobby 씬 전환에서만 `skipFade`를 유지. 사용자 실기 PASS: 실제 이메일 표시, 가입 취소 팝업, Firebase 미인증 계정 삭제, 인증 계속하기 유지, 인증 화면 재실행 복귀, 닉네임 설정 화면 재실행 복귀. task: `_Tasks/2026-07-16/14_20_email-verification-flow-cleanup/`. |
