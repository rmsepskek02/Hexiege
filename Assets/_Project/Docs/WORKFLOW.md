# 작업 사이클 운영 규칙

이 파일은 **작업 사이클 메카닉의 단일 권위 소스**입니다.
에이전트 역할은 `CLAUDE.md` 규칙 3, 문서 인덱스는 루트의 `AGENTS.md` 참조.

---

## 작업 시작 전 확인

`CLAUDE.md` 작업 시작 전 체크리스트 [1]이 이 문서를 읽도록 지시하므로, **아래 두 가지는 사이클 [1]에 들어가기 전에 확인한다.**
둘 다 **시스템 프롬프트에 자동 주입되지 않는다** — 직접 열지 않으면 도달하지 못한다.

| 확인 대상 | 무엇을 보나 | 왜 |
|----------|-----------|-----|
| `AGENTS.md` — 문서 인덱스 | 이번 작업에 필요한 문서가 어디 있는지 | 프로젝트 전체 문서의 단일 인덱스다. 여기를 거치지 않으면 관련 규칙 문서를 빠뜨린 채 착수하게 된다 |
| `.claude/mistakes.md` — AI 실수 기록 | 이번 작업과 성격이 비슷한 과거 실수가 있는지 | 같은 실수가 반복되는 것을 막기 위한 누적 기록이다. **목차가 한 건당 한 줄**이라 훑는 비용이 작고, 걸리는 항목이 있을 때만 그 본문을 펼쳐 읽는다 |

- **두 문서 모두 전문을 읽지 않는다.** `AGENTS.md`는 인덱스에서 필요한 문서만 골라 열고, `.claude/mistakes.md`는 목차를 훑은 뒤 해당 항목만 펼친다.
- 「성격이 비슷하다」의 기준은 기술 분야가 아니라 **실수의 모양**이다 — 수치를 다음 사람에게 넘기는 작업이면 인계 수치 항목을, 문서·메모리를 정리하거나 옮기는 작업이면 소실 항목을 본다.
- **작업 중 실수를 인지하면 그 자리에서 `.claude/mistakes.md`에 덧붙인다.** [7]~[11] 문서 반영 단계까지 미루지 않는다 — 미루면 경위가 흐려져 교훈을 만들 수 없다. **사건은 지우지 않는다.**

---

## 폴더 구조

```
Assets/_Project/Docs/_Tasks/         ← 사용자용 작업 문서
└── YYYY-MM-DD/
  └── HH_MM_[작업명]/
    ├── Research.md    ← 코드 파악, 영향 범위, 현재 상태
    ├── Plan.md        ← 구현 접근법, 파일별 변경 내용, 위험 요소
    └── Testcase.md    ← 테스트 시나리오 + 사용자 실기 결과 기록

Assets/_Project/Docs/_Logs/          ← 런타임 로그 + QA-Fix 로그 (상세 규칙: LogRules.md 참조)
```

예시:
```
Assets/_Project/Docs/_Tasks/
└── 2026-03-05/
  └── 18_05_network-input-fix/
    ├── Research.md
    ├── Plan.md
    └── Testcase.md
```

> **시간(HH_MM) 확인 방법**: 폴더 생성 시 현재 시간을 모르는 경우, Bash 도구로 `date +%H_%M` 명령을 실행하여 확인한다. 시간 접두사 생략은 규칙 위반이다.

---

## 전체 작업 사이클

```
[1] 사용자 작업 요청 → 계획 설명
      ↓
[2] 사용자 승인 ("진행해") → "task 문서 작성을 진행할까요?" 반드시 확인
      ↓
[3] Research.md 작성
      ↓
[4] Plan.md 작성 → 사용자에게 공유 → 명시적 승인 후에만 구현 시작
      ↓
[5] 에이전트 위임 결정 → 컨텍스트 공유 → 구현
      ↓ ← 구현 완료 후 바로 [6]으로 이동 (TC/QA는 사용자가 명시적으로 지시한 경우에만 진행)
[5-1] Testcase.md 작성  ← 사용자가 명시적으로 요청한 경우에만 진행
      ↓
[5-2] Inspector 작업이 필요한 경우: Editor 1회성 스크립트 작성 → 사용자에게 실행 요청 → 사용자 확인
(별도의 Inspector 작업이 없는 경우 생략하고 다음 단계 바로 진행)
      ↓
[5-3] qa-tester 에이전트에게 테스트 요청 및 테스트 진행하여 문서에 반영  ← [5-1] 완료 즉시 진행 (이미 사용자 승인 완료)
      ↓
[6] 구현 내용을 사용자가 테스트하여 작업 완료 여부 확인
(버그 발견 또는 부족한 구현의 경우 재작업)
      ↓
[7] 테스트 완료 확인 즉시 → "문서/메모리 업데이트를 진행할까요?" 반드시 확인
(사용자가 다음 작업으로 넘어가려 해도 이 확인 단계를 생략하지 않음)
      ↓
[8] Testcase.md 결과 업데이트
      ↓
[9] 에이전트 MEMORY.md 업데이트
      ↓
[10] 업데이트 내용 검토
      ↓
[11] 문서 전체 검토 및 반영
      ↓
[12] 변경 파일 리스트업
```

### ⚠️ 절대 금지 사항
- **[2] 승인 후 바로 Edit/Write/Agent 호출 금지** — 반드시 "task 문서 작성할까요?" 먼저 확인
- **[5-1~5-3] TC 작성 및 QA 테스트는 사용자 명시적 지시 없이 진행 금지** — 사용자가 먼저 요청하지 않는 한 TC/QA 제안·진행 불가
- **[6] 테스트 완료 후 다음 작업 전 [7] 업데이트 확인 생략 금지** — "나중에", "다음 세션에" 불가
- **규모/복잡도 무관 — 한 줄 수정도 포함 — 예외 없음**

---

## 단계별 상세 운영 규칙

### [1] 작업 요청 및 계획 설명
- 착수 전 확인 대상은 상단 **「작업 시작 전 확인」** 참조 (`AGENTS.md` 인덱스 · `.claude/mistakes.md` 목차)
- 사용자 요청 분석 후 작업 범위·접근법 설명
- 승인 전 Edit/Write/Agent 호출 일체 금지

### [2] 사용자 승인 + 문서 작성 여부 확인
- 사용자가 "진행해" 등으로 승인 → 즉시 **"task 문서(Research.md / Plan.md) 작성을 진행할까요?"** 질문
- 사용자가 명시적으로 "문서 생략해도 돼"라고 말하지 않는 한 반드시 확인

### [3] Research.md 작성
- 관련 코드 읽기, 영향 범위 파악
- 현재 버그/상태 분석
- 작업 중 발견한 부가 이슈도 여기에 기록

### [4] Plan.md 작성 및 승인
- **`Assets/_Project/Docs/GameSystemRules.md` 읽기 필수 — 예외 없음. Plan.md 작성 전 반드시 읽을 것.**
  - 세부 규칙은 `Assets/_Project/Docs/GameSystemRules/` 하위 파일에 있다. 인덱스를 먼저 읽고 작업과 관련된 파일을 추가로 읽는다.
- **Plan의 각 수정 항목은 GameSystemRules의 어떤 규칙에 근거하는지 명시할 것.**
- 구체적인 구현 방법, 수정할 파일 목록
- 예상 위험 요소, 아키텍처 제약 사항
- Plan.md 내용을 사용자에게 공유 → **명시적 승인 확인 후에만** 구현 시작
- 계획 변경 시 변경 이유도 추가 기록

#### ⚠️ 기존 로직 제거 규칙 (예외 없음)
Plan에 "기존 로직 제거"를 명시할 때는 반드시 제거해도 안전한 근거를 함께 기술해야 하며, 검증 전까지는 "제거" 대신 "비활성화(주석 처리)" 방식을 기본으로 한다. 또한, 사용자가 바로 파악할 수 있도록 **Plan 문서 최상단에 기술한다**.
주석 처리된 기존 로직의 **최종 삭제는 [6] 사용자 테스트 통과 후, [7] 문서/메모리 업데이트 전**에 수행한다.

### [5] 에이전트 위임 및 구현

어느 에이전트를 부르는지는 `CLAUDE.md` 규칙 3 참조. 전달할 컨텍스트는 아래 표를 따른다.

| 에이전트 | 담당 | 전달 필수 컨텍스트 |
|---------|------|-----------------|
| **game-programmer** | 코드 구현 / 버그 수정 | 관련 파일 경로, 증상, 아키텍처 규칙 |
| **game-design-lead** | 게임플레이 설계 / 밸런스 결정 | 현재 구현 상태, 관련 수치 |
| **qa-tester** | 구현 검증 / 버그 체크 | 변경된 파일 목록, 예상 동작 |
| **asset-prompt-crafter** | 3D 모델 / UI 에셋 생성 | 에셋 스펙, FBX 파이프라인 요구사항 |
| **document-manager** | 모든 문서 생성·수정·동기화 | 변경 내용, 관련 문서 경로 |

> **작업 분해·에이전트 조율은 위 표에 없다 — 메인 세션이 직접 한다.** 서브에이전트는 다른 에이전트를 호출할 수 없으므로(하네스 제약, 설정으로 변경 불가) 조율을 에이전트에게 위임할 수 없다. 종전에 이 표에 있던 `project-orchestrator` 행은 그래서 2026-09-02에 제거됐다. 메인 세션은 최상위라 위 5개 에이전트를 직접 호출할 수 있다.

#### 조율이 필요한 작업의 기준

아래에 해당하면 **메인 세션이 조율·단계 분할·결과 검토를 직접 수행한다.** 개별 실무는 위 표의 전문 에이전트에게 위임하되, 무엇을 어떤 순서로 맡길지와 돌아온 결과의 검토는 메인 세션의 몫이다.

| 상황 | 메인 세션이 직접 조율·검토 |
|------|----------|
| 단일 파일 버그 수정 | 선택 (game-programmer 직접 위임 가능) |
| 설계 결정 + 코드 구현이 함께 필요 | **필수** |
| 3개 이상 파일에 걸친 기능 추가 | **필수** |
| 에이전트 결과 검토 필요 | **필수** |
| 전체 현황 파악 및 다음 작업 결정 | **필수** |

- 구현 완료 후 [6] 사용자 테스트로 바로 이동
- **TC 작성([5-1]) 및 QA 테스트([5-3])는 사용자가 명시적으로 지시한 경우에만 진행** — 먼저 제안하거나 묻는 것도 금지

#### 조율 진행 절차 (7단계)

위 기준에 해당해 메인 세션이 직접 조율할 때는 아래 순서를 밟는다.

1. **이해** — 사용자가 실제로 달성하려는 것이 무엇인지 먼저 확정한다. 모호하면 추측하지 말고 되묻는다.
2. **분해** — 더 쪼갤 수 없는 하위 작업 단위로 나누고, 작업 간 선후 의존을 함께 적는다.
3. **배정** — 각 하위 작업을 담당 에이전트에 배정한다. 어느 에이전트가 무엇을 맡는지는 `CLAUDE.md` 규칙 3과 위 위임 표를 따른다. **이 자리에 에이전트 목록을 옮겨 적지 않는다** — 사본은 에이전트가 늘거나 줄면 조용히 거짓이 된다.
4. **순서 결정** — 의존 관계를 기준으로 순차/병렬 실행 순서를 정한다. 착수 전 분해 결과를 사용자에게 공유하고 승인을 받는다([4]와 같이 승인 전 구현 착수 금지).
5. **실행** — 메인 세션이 전문 에이전트를 **직접 호출**한다. 각 호출은 아래 위임 포맷을 쓰고, 위 표의 「전달 필수 컨텍스트」 열을 빠짐없이 채운다.
6. **검증** — 돌아온 결과를 검토한다. 기대와 다르면 무엇이 어긋났는지 붙여 다시 위임한다. **검토 자체는 위임 대상이 아니다.**
7. **종합** — 결과를 합쳐 사용자에게 보고하고, 남은 항목을 함께 알린다.

- 하위 작업 하나로 끝나는 일을 억지로 여러 단계로 쪼개지 않는다 — 단일 에이전트로 충분하면 바로 위임한다.
- 맞는 에이전트가 없으면 임의로 대신 처리하지 말고 사용자에게 알린다.

#### 위임 포맷

에이전트를 호출할 때의 지시는 아래 5칸을 채워 전달한다.

```
[작업 목표]: 구체적인 목표
[컨텍스트]: 필요한 배경 정보
[범위]: 정확히 무엇을 해야 하는지
[제약사항]: 지켜야 할 규칙이나 제한
[기대 결과물]: 어떤 형태의 결과를 기대하는지
```

### [5-1] Testcase.md 작성
- 구현 완료 후 `Testcase.md`를 작업 폴더에 작성
- 설계 스펙 기반으로 테스트 케이스 목록 작성 (Inspector 작업 완료 여부와 무관)

#### TC 본문 형식
각 TC는 아래 4개 항목으로 구성하며, **반드시 자연어로 작성** (코드 스니펫, 메서드명, 변수명 사용 금지):

```
### TC-N: 테스트 이름

**전제:** 테스트 시작 전 시스템 상태

**동작:**
1. 사용자가 수행하는 조작 순서

**기댓값:**
- 예상되는 결과

**결과:** (사용자 실기 테스트 후 기록 — AI와 문서로 소통하는 채널)
```

- 예시: ~~`unitProductionUseCase.StartProduction()`을 호출하면~~ → "생산 버튼을 누르면 생산이 시작된다"
- TC ID 접두사로 컨텍스트 구분:
  | 접두사 | 대상 |
  |--------|------|
  | `SINGLE-` | 싱글플레이 / 에디터 단독 |
  | `MULTI-` | 멀티플레이 (Host + Client 구성) |
  | `PERF-` | 성능 (프레임, 이벤트 빈도, 대량 오브젝트 등) |

#### QA 섹션 형식
TC 목록과 **별도 섹션**으로 문서 하단에 작성. qa-tester 에이전트 전용 공간:
- 코드, 근거, 버그 ID 등 구체적인 표현 사용
- 정적 분석 결과 + 실기 테스트 결과(가능한 경우) 모두 포함

#### 판정 기준 (3단계)
| 판정 | 의미 |
|------|------|
| `PASS` | 기댓값과 완전히 일치 |
| `FAIL` | 기댓값 미충족, 버그 존재 |
| `CONDITIONAL PASS` | 최종 수렴은 보장되나 조건부 확인 필요 (실기 재확인 요구) |

### [5-2] Inspector 에디터 스크립트 (Inspector 작업이 있는 경우)
- Inspector에서 수동으로 해야 하는 작업이 있을 경우 Editor에서 실행 가능한 1회성 스크립트 작성
- 메뉴 경로(`Hexiege/...`) 형태로 작성, 사용자에게 실행 요청
- 사용자가 실행 완료를 확인한 후 다음 단계 진행
- 스크립트 실행 완료 후 해당 파일 삭제해도 무방 (1회성)
- 별도의 Inspector 작업이 없는 경우 생략하고 다음 단계 바로 진행

#### 에디터 스크립트를 두는 폴더 (1회성 vs 영구 보존)

```
Assets/Editor/Setup/   → 1회성 스크립트. 실행 후 삭제 가능
Assets/Editor/Tools/   → 영구 보존 도구. 삭제 금지
```

- 이것은 새 관습이 아니라 **이미 있는 관습을 적어 둔 것**이다 — `Setup/` 의 14개는 전부 헤더에 「1회성 셋업」이 명시돼 있고, `Tools/LogcatCapture.cs` 는 「상시 사용 도구」로 명시돼 있다(2026-08-26 실측).
- **반복해서 다시 쓰는 도구는 `Tools/` 에 둔다.** 무작위 맵 **폴백 템플릿 제작 도구**가 여기 해당한다 — 저장 포맷이 바뀌면 템플릿을 재생성해야 하는데 도구를 지우면 재생성할 수 없다(`GameSystemRules/GameSystemRules_RandomMap.md` 규칙 12).
- 파일을 옮길 때는 **`.cs.meta` 를 반드시 동반**하고 `Hexiege.EditorTools` 네임스페이스 영향을 확인한다.

**후속 판단 대기 (이번에 옮기지 않았다 — 이동은 별도 승인 대상)**

| 파일 | 판정 |
|---|---|
| `Assets/_Project/Scripts/Editor/AndroidBuildAssetOptimizer.cs` | 확실한 상시 도구 → `Assets/Editor/Tools/` 로 이동 대상 |
| `CombatHitEventInjector.cs` · `CombatHitEventValidator.cs` · `SpiritAttackVfxTestSpawner.cs` | **판단 필요** — 헤더는 「1회성」이나 반복 사용 성격 |

### [5-3] QA 요청
- qa-tester 에이전트에게 Testcase.md와 함께 테스트 요청
- QA 에이전트는 **정적 분석 + 실기 테스트(가능한 경우)** 모두 수행하여 결과를 문서에 반영
- 판정은 `PASS` / `FAIL` / `CONDITIONAL PASS` 3단계 사용
- **멀티플레이 TC**: 에디터(Host) + 빌드(Client) 구성이 필요하여 QA 에이전트 단독 실기 불가 → 해당 TC에 "에이전트 실기 불가 — 사용자 확인 필요" 표기 후 정적 분석만 수행
- 발견된 버그는 BUG-ID로 명시하여 QA 섹션에 기록
- **FAIL 발견 시**: `Assets/_Project/Docs/_Logs/` 하위 동일 경로에 Log.md 생성(없는 경우) 후 해당 Round [QA] 섹션에 버그 기록 → game-programmer에게 Log.md 경로와 함께 수정 요청
- **재QA 시**: Log.md에 다음 Round 섹션 추가 후 동일 절차 반복

---

## Log.md 운영 규칙

→ `Assets/_Project/Docs/LogRules.md` **2. QA-Fix 로그** 섹션 참조

### [6] 사용자 테스트
- 구현 내용을 사용자가 직접 테스트하여 완료 여부 확인
- 버그 발견 또는 부족한 구현의 경우 재작업

### [7] 문서/메모리 업데이트 확인 ← 절대 생략 불가
- 테스트 완료 확인 즉시 → **"문서/메모리 업데이트를 진행할까요?"** 반드시 질문
- 사용자가 다음 작업을 언급해도 이 확인을 먼저 한 뒤 넘어감

### [8] Testcase.md 업데이트
- 사용자 테스트 최종 결과를 `Testcase.md`에 반영 (PASS/FAIL)
- Plan.md 별도 체크리스트 불필요 — Testcase.md로 통합

### [9] 에이전트 MEMORY.md 업데이트

작업 완료 시 해당하는 항목을 모두 업데이트한다.

| 조건 | 업데이트 대상 |
|------|-------------|
| 항상 | `PROJECT_STATUS.md`, `ROADMAP.md`, `WORK_HISTORY.md` |
| 코드 변경 | game-programmer MEMORY.md |
| 게임플레이/밸런스 변경 | game-design-lead MEMORY.md |
| 버그 수정 / 취약점 발견 | qa-tester MEMORY.md |
| 3D 에셋 작업 | asset-prompt-crafter MEMORY.md |
| 문서 구조 변경 / 신규 에이전트 추가 | document-manager MEMORY.md |
| 모든 작업 완료 후 | `.claude/MEMORY.md` (에이전트 공용) |

갱신 방법(도구·삭제 조건·크기 기준)은 `.claude/MEMORY.md` 「🔴 Agent Memory Management Rules」.

### [10] 업데이트 내용 검토
- 각 에이전트가 작성한 MEMORY.md 변경 내용을 확인
- 잘못 기록된 내용, 누락된 항목 수정

### [11] 문서 반영
①② 두 범위의 문서를 작업 내용 기준으로 검토하여 빠짐없이 업데이트한 뒤, ③ 정합성 검사로 마무리한다.
특정 문서만 업데이트하고 나머지를 방치하는 것은 규칙 위반.

**① 현재 작업의 _Tasks 문서**
- 현재 작업 폴더의 `Research.md` / `Plan.md`: 실제 구현과 달라진 부분 수정
  (Testcase.md는 [8]에서 처리 완료)

**② Docs/ 상시 참조 문서 전체** (`_Tasks/`, `_Logs/` 제외)
- `PROJECT_STATUS.md`: 완료된 시스템/버그 수정 항목 갱신
- `ROADMAP.md`: 완료된 항목 제거, 다음 우선순위 조정
- `WORK_HISTORY.md`: 완료된 작업 마일스톤 추가
- `GameDesignDocument.md` / `TechnicalDesignDocument.md` / `GameSystemRules/` 등: 작업 내용과 불일치하거나 누락된 부분 수정
- 그 외 Docs 하위 문서: 연관성 검토 후 필요 시 업데이트

**③ 문서 정합성 검사 (문서 수정을 마친 뒤 반드시 실행)**
```
python3 Tools/check_docs.py
```
- 리포지토리 루트에서 실행한다. **읽기 전용**이라 문서를 고치지 않고 문제 목록만 출력한다.
- 검사 항목 **7종**: `[1]` 규칙 번호 결번 / `[2]` 깨진 파일 링크 / `[3]` 실재하지 않는 규칙 번호 참조 / `[4]` 섹션명이 없어 특정 불가한 참조 / `[5]` 병기 내용과 규칙 제목 불일치 / `[6]` 인덱스에서 링크되지 않은 에이전트 메모리 토픽 파일(고아 토픽) / `[7]` 에이전트 메모리 폴더의 총합 행수 감소.
  - `[6]`·`[7]`은 **에이전트 메모리 보호용**이다. `[6]`은 인덱스 링크가 빠져 아무도 찾지 못하게 된 토픽 파일을, `[7]`은 "토픽으로 옮겼다"면서 실제로는 지운 경우를 잡는다 — 이동이라면 폴더 총합 행수가 줄지 않는다(`.claude/MEMORY.md` 「🔴 에이전트 메모리 갱신 규칙」 5·6).
  - `[7]`의 기준값은 `.claude/agent-memory/_baseline.json`에 있다. **이 파일을 직접 편집하지 않는다.** 갱신은 `python3 Tools/check_docs.py --update-baseline`으로만 한다.
    - **증가 방향**(정상적인 성장): `--update-baseline` 만으로 갱신하며 사용자 승인이 필요 없다. **미루지 않는다** — 미루면 이후에 발생한 감소폭이 실제보다 작게 보인다.
    - **감소가 하나라도 포함된 갱신**: `--update-baseline --reason "왜 줄었는지"` 가 **필수**다. `--reason` 없이 실행하면 도구가 **거부**한다. 준 사유는 `_baseline.json` 의 `change_log` 에 **자동 기록**된다. 감소 반영은 "이 삭제는 의도된 것"이라는 판단이므로 **사용자 승인은 여전히 필요하다.**

#### 검사 범위

**검사기가 매 실행 시 `[검사 범위]` 블록으로 규칙 정의 원본 문서 수·참조 검색 대상 `.md` 수·`.claude` 하위 문서 수를 출력한다. 그 출력이 권위 소스이며, 문서가 늘거나 줄면 값이 바뀐다 — 이 자리에 숫자를 옮겨 적지 않는다.**

```
규칙 정의 원본 : Assets/_Project/Docs/GameSystemRules 하위
참조 검색 대상 : Assets/_Project/Docs 하위 + 루트 AGENTS.md · CLAUDE.md + .claude 하위 (skills/ · plugins/ 제외)
제외           : _Tasks/ · _Logs/ (이력 기록이라 소급 수정하지 않는다)
```

#### 🔴 규칙 정의는 반드시 `**규칙 N. 제목**` 형식으로 쓴다

검사기는 규칙 정의를 **굵은 글씨 `**규칙 N. 제목**` 형식으로만** 인식한다(`check_docs.py` 의 `parse_rule_docs()`).
**`## 규칙 N. 제목` H2 로 쓰면 그 줄은 규칙이 아니라 「섹션명」으로 파싱된다.** 결과는 규칙 하나가 누락되는 정도가 아니라 **그 문서의 규칙이 통째로 0개 등록**되는 것이고, 그러면 그 문서를 가리키는 규칙 번호 참조는 실재하지 않는 번호를 적어도 **검사 [3]·[4]·[5]를 공허하게 통과**한다. 즉 검사 결과가 0건이어도 그 문서에 대해서는 아무것도 검증되지 않은 상태가 된다.

- 규칙을 **새로 쓰거나 추가할 때는 예외 없이 굵은 글씨 형식**을 쓴다. H3(`### 하위 제목`)은 규칙으로 읽히지 않으므로 규칙 본문 안의 소제목으로 자유롭게 쓸 수 있다.
- **번호 뒤에는 `.` 또는 공백이 와야 한다** — `규칙 11-1` 처럼 하이픈을 붙이면 정규식에 매치되지 않아 역시 등록되지 않는다.
- 2026-08-25, `GameSystemRules_Map.md` · `_RandomMap.md` · `_Upgrade.md` 3개 문서를 이 형식으로 통일해 검사 대상에 넣었다(규칙 정의 원본 **7개 → 10개**).

- **0건을 확인한 뒤 [12]로 넘어간다.** 남은 항목이 있으면 참조하는 쪽을 고쳐 해소하고, 고칠 수 없는 항목은 그 이유를 사용자에게 보고한다.
- **규칙 번호 자체는 고치지 않는다** — 코드 주석과 과거 Task 문서가 그 번호를 참조하고 있어 재배열하면 연결이 끊긴다. 고칠 대상은 항상 참조하는 쪽이다.
- `_Tasks/` · `_Logs/`는 이력 기록이라 검사 대상에서 제외되며, 소급 수정하지 않는다.

### [12] 변경 파일 리스트업
작업 중 추가 또는 변경된 파일 전체를 아래 형식으로 정리하여 사용자에게 공유.

```
[추가]
- Assets/_Project/Scripts/.../NewFile.cs

[수정]
- Assets/_Project/Scripts/.../ModifiedFile.cs
- Assets/_Project/Prefabs/.../SomePrefab.prefab
```

- 코드, 프리팹, 씬, 에셋 등 모든 파일 유형 포함
- 작업 문서(_Tasks/, _Logs/)는 별도 항목으로 구분
- 누락 없이 작성 (git diff 또는 작업 이력 기반으로 확인)

---

## 규칙

- 작업 폴더는 삭제하지 않음 (히스토리로 보존)
- **계획 단계(Research/Plan 작성 시)**: 에이전트(Agent) 호출 허용 — 코드 파악, 설계 검토, 의견 수렴에 활용
- **메인 세션**은 단일 에이전트 작업이든 복합 작업이든 계획·조율·검토를 직접 수행한다 (조율을 대신할 에이전트는 없다 — [5] 「조율이 필요한 작업의 기준」 참조)
