# 무작위 맵 3단계 — 구현 계획 (멀티플레이 맵 전송 · 해시 대조 · Client 검증)

작성일: 2026-09-09

> 조사 기록은 같은 폴더 `Research.md`. **실측한 것과 못 잰 것의 구분은 그 문서가 단일 소스**이며, 이 계획은 그 구분을 그대로 물려받는다.
> 규칙의 단일 소스는 `Assets/_Project/Docs/GameSystemRules/GameSystemRules_RandomMap.md`(이하 **규칙 문서**),
> UI 규칙은 `Assets/_Project/Docs/GameSystemRules/GameSystemRules_UI.md` 「공통 UI 규칙」,
> 기술 계약은 `Assets/_Project/Docs/TechnicalDesignDocument.md`(이하 **TDD**) 「무작위 맵 시작 동기화」 절이다.
> **두 곳이 어긋나면 규칙 문서가 옳다**(TDD 자신이 그렇게 적고 있다).
> 선행: 1단계 `_Tasks/2026-09-01/19_49_random-map-phase1-tilekind/` · 2단계 `_Tasks/2026-09-03/03_14_random-map-phase2-generator/`

---

## 0. 이 작업이 무엇이고 왜 하는지 (자연어 설명 — CLAUDE.md 규칙 13)

지금 이 게임은 **혼자 하는 경기에서만** 매 판 다른 전장이 나옵니다. 2단계에서 맵을 만들어내는 기계와 그것을 실제 전장으로 옮기는 일까지 끝냈고, 사용자가 직접 플레이해 확인했습니다.

**둘이서 하는 경기는 아직 그렇지 않습니다.** 두 사람이 각자 자기 기기에서 맵을 만들면 서로 다른 전장에서 싸우게 되므로, 지금은 임시로 **양쪽 모두 똑같은 고정된 값으로 맵을 만들게** 해 두었습니다. 그 대가로 **멀티플레이는 지금 매 판 완전히 같은 맵**이 나옵니다.

이번 작업은 그 임시 조치를 걷어내고 제대로 된 방식으로 바꿉니다. 하는 일은 네 가지입니다.

1. **방을 만든 사람(Host)이 맵을 만드는 유일한 주인이 됩니다.** 상대는 맵을 만들지 않습니다.
2. **Host가 만든 맵을 통째로 상대에게 보내 줍니다.** 상대는 받은 것을 그대로 씁니다.
3. **보낸 맵과 받은 맵이 한 글자도 다르지 않은지 지문(해시)을 맞춰 봅니다.** 그리고 상대는 한 걸음 더 나아가, **받은 맵이 "정말 그 씨앗값에서 나올 수 있는 맵인지"를 스스로 다시 만들어 보며 대조합니다.** 이렇게 하면 누군가 중간에 맵을 손으로 뜯어고쳐도 곧바로 들통납니다.
4. **지문이 같을 때만 전투 화면으로 넘어갑니다.** 보내다 실패하거나 지문이 다르면 **전투 화면으로 넘어가지 않고 대기실에 그대로 머무릅니다.**

**이번 단계가 끝났다고 말할 수 있는 조건은 하나입니다 — 두 사람이 멀티 경기에 들어갔을 때 매 판 다른 맵이 나오고, 두 화면에 같은 맵이 뜨고, 그 맵에서 경기가 정상적으로 돌아가는 것.** 화면을 보고 확인할 수 있습니다.

**이번에 하지 않는 것이 하나 있습니다 — "실패했을 때 화면에 무엇을 띄울지"입니다.** 실패하면 전투 화면으로 넘어가지 않고 대기실에 머문다는 **동작 자체는 이번에 만들지만**, 그때 뜨는 팝업의 생김새·문구·버튼은 다음 작업으로 미룹니다. 왜 이렇게 나눌 수 있는지는 §2-3에 적었습니다. 한 판 더 할 때 "같은 맵 / 새 맵"을 고르는 일도 다음 작업입니다.

---

## 🔴 1. 기존 로직 제거·대체 — 먼저 읽을 것 (`WORKFLOW.md` [4] 「기존 로직 제거 규칙」)

이번 작업은 **멀티플레이의 임시 고정 seed 조치를 걷어내고 Host 권위 경로로 대체**한다. 없애는 코드가 있으므로 규정대로 여기 최상단에 적는다.

> 🔴 **원칙: 이번 단계에서 지우는 것은 하나도 없다. 전부 「주석 비활성화」다.**
> 대체 경로가 **멀티 실기로 검증되기 전까지는 되돌릴 수단을 남겨야** 한다. 특히 이번 대체 경로는
> **이 환경에서 컴파일조차 확인할 수 없고(Unity·NGO 참조) 사용자 실기 2대로만 확인된다** — 2단계까지와 달리
> 「돌려 보고 아니면 되돌린다」의 되돌리기 비용이 가장 큰 단계다.
> **최종 삭제는 `WORKFLOW.md` [6] 사용자 실기 통과 후 · [7] 문서 갱신 전**에 수행한다.

| # | 대상 | 처리 | 안전한 근거 |
|---|---|---|---|
| **제거-1** | `GameBootstrapper.Map.cs:633~634` 의 멀티 분기 `if (IsNetworkMode()) return NetworkInterimRootSeed;` 와 그 위 주석 블록(`:627~632`), 그리고 상수 `GameBootstrapper.cs:240` `private const ulong NetworkInterimRootSeed = 11UL;` | 🔴 **삭제하지 않고 주석 비활성화.** `// [3단계 대체 대기]` 표식을 단다 | 대체 경로(Host 권위 seed → 전송 → 대조)가 **멀티 실기로 검증되기 전까지** 되돌릴 수단이 필요하다. ⚠️ **현재 값은 `1` 이 아니라 `11UL` 이다**(커밋 `b515995` — 2단계 §14-6 의 테스트용 임시 변경). 되돌릴 때 `1` 로 쓰면 그 회차 실기와 다른 상태가 된다 |
| **제거-2** | `GameBootstrapper.Map.cs:433` `PrepareAndProjectMap()` — **준비와 투영을 한 함수에서 연달아 하는 구조** | **분할이며 제거가 아니다.** `PrepareMap()` + `ProjectMap(정의)` 로 가르고, 기존 이름의 래퍼가 둘을 연달아 부르게 남긴다 | 싱글플레이 경로가 **한 줄도 달라지지 않는 것**이 이 분할의 조건이다. 래퍼를 남기면 호출부(`:110`)가 무변경이라 회귀 면적이 0이 된다. 규칙 근거는 Research §7-3·§7-4 |
| **제거-3** | `BattleViewModel.cs:305` 의 `_networkManager.LoadGameScene()` **직접 호출** | **호출 대상 교체**(제거 아님). 옛 호출문은 주석 비활성화 + `// [3단계 대체 대기]` 표식 | 규칙 16 이 *"Host와 Client의 최종 맵 데이터 해시가 같을 때만 전투 씬 전환을 시작한다"* 고 정한다. 실측상 이 한 줄이 **현재 전투 씬 전환의 유일한 트리거**이며(Research §4-5), 게이트는 이 호출 앞에 선다 |
| **제거-4** | `GameBootstrapper.Map.cs:453~454` 주석 *"다른 코드가 `GetLastMapPreparation()` 으로 같은 판의 결과(최종 맵 바이트·해시 등)를 다시 읽는다"* | **문장 교체**(코드 변경 아님) | Research §12-4 가 *"틀린 주석이 아니라 앞선 주석"* 이라 판정했으나, **이 계획의 설계에서는 앞서지도 않는다** — 인계는 `GetLastMapPreparation()` 이 아니라 §5-3 의 정적 홀더를 거치므로 **3단계가 끝나도 이 접근자의 호출자는 0건이다.** 근거는 §5-3. 코드가 아니라 사실 서술을 고치는 항목이므로 주석 비활성화 대상이 아니다 |

> **표식을 `[3단계 대체 대기]` 로 통일한다.** 2단계가 `[2단계 대체 대기]` 를 써서 최종 삭제 시 `grep` 한 번으로 찾았고, 삭제 후 0건이 되는 것이 완료의 기계적 증거였다. 같은 방식을 쓴다.
> ⚠️ **2단계의 `[2단계 대체 대기]` 표식은 건드리지 않는다** — 그쪽의 최종 삭제 여부는 이 작업의 범위가 아니다.

---

## 2. 완료 판정

### 2-1. 판정표

| | 내용 |
|---|---|
| **주 판정** | 두 기기로 멀티 경기에 들어가면 **매 판 다른 맵**이 나오고, **양쪽 화면의 맵이 같으며**, 그 맵에서 유닛 생산·이동·전투·건설·철거가 정상 동작한다 |
| 부 판정 1 | Client 가 받은 맵으로 **복원 대조에 성공**하고 `MapDefinitionValidator.Validate()` 를 통과한다 (로그로 확인 — §7-H) |
| 부 판정 2 | **해시가 다르면 전투 씬으로 넘어가지 않고 로비에 머문다.** 로그에 불일치가 남는다 (규칙 16) |
| 부 판정 3 | 규칙 12 「로그 필수 항목」 14가지가 **전부 값을 갖는다.** 2단계에서 11가지가 채워졌고, 3단계가 나머지 **2가지(전송/재전송 횟수 · Host/Client 해시 비교 결과)** 를 채운다 |
| 부 판정 4 | 임시 고정 seed 가 비활성화된 뒤에도 **싱글플레이가 회귀 없이** 매 판 다른 맵으로 정상 동작한다 |

### 2-2. 범위 밖 (사용자가 이번 범위에서 뺀 것 — 결함이 아니라 확정된 범위 결정)

- 재경기 `SameMap`/`NewMap` 일체 (규칙 14) → §10
- **실패 UI 일체** — 팝업 여부·문구·타입·countdown → §10
- 초기 골드 적용 · 경로 완전 차단 대응 · TDD 「약 274바이트」 정정 → §11

### 2-3. 🔴 「진행 차단」과 「실패 표시」를 왜 나눌 수 있는가 (사용자 지시로 근거를 남긴다)

**나눌 수 있는 이유는 규칙 16 자신이 둘을 서로 다른 불릿으로 적고 있고, 앞 불릿이 뒤 불릿 없이도 판정 가능하기 때문이다.**

| 규칙 16 의 문장 | 성격 | 이번 범위 |
|---|---|---|
| *"어떤 실패에서도 전투 씬으로 전환하지 않는다."* · *"맵 준비가 실패하거나 해시가 다르면 전투 씬으로 이동하지 않고 기존 로비를 유지하며 불일치 내용을 기록한다."* | **상태 기계의 판정** — 관측 가능한 결과가 「씬이 안 넘어간다 + 로그가 남는다」이며, 이 둘만으로 통과/실패를 가릴 수 있다 | ✅ **이번** |
| *"최초 멀티 준비 실패는 로딩을 닫고 … `맵 준비에 실패했습니다` 문구의 모달 (Modal) 타입 팝업과 `Retry`/`Leave Match`를 표시한다."* | **표현** — 위 판정 결과를 사람에게 알리는 층이며, 게이트의 판정에 되먹임하지 않는다 | ⏸ 다음 |

**의존 방향이 한쪽뿐이라는 것이 핵심이다.** 게이트는 팝업 없이도 완결되지만(로그로 관측한다), 팝업은 게이트 없이는 **의미가 없다** — 게이트가 없으면 팝업이 뜬 뒤에도 씬이 넘어가 버린다. 그래서 **순서는 반드시 게이트가 먼저**이고, 이 순서를 지키는 한 분리는 안전하다.

⚠️ **분리의 대가도 적어 둔다.** 이번 범위만 끝난 상태에서 실패가 나면 **플레이어는 아무 안내도 받지 못한 채 로딩 화면에 남는다.** 이것은 규칙 16 의 UI 규정이 **미충족 상태로 남는다**는 뜻이며, 「구현이 잘못됐다」가 아니라 **「이번 범위가 거기까지가 아니다」**로 읽어야 한다. 다음 범위(§10)에서 닫힌다.

또한 규칙 16 의 **`Retry` 경로도 이번 범위가 아니다** — *"어느 플레이어든 `Retry`를 누르면 Host에 요청한다"* 인데 누를 버튼이 이번에 없으므로, 요청 RPC 와 idempotency 처리는 **부를 곳이 없는 죽은 코드**가 된다. 팝업과 한 묶음으로 §10 에 둔다.

---

## 3. 작업 순서 — 의존 관계

```
A. NGO 씬 재로드 생존 조건 확인          ← 코드 0줄. B 의 전제를 실측으로 확정한다
        ↓  (여기서 갈린다 — §4-3)
B. NetworkMapTransfer 신설 + 3메시지 골격 + 전송 한도 실측
        ↓
C. 확정 맵 인계 통로(MapHandoff) + 낡은 주석 정정   ← 순수 C#. B 와 독립이라 순서를 바꿔도 된다
        ↓
D. 준비/투영 분리 (동작 무변경 리팩터)   ← 싱글 회귀로만 검증된다. 아직 아무것도 뒤집지 않는다
        ↓
E. 로그 키 신설                          ← F 이후의 실패를 관측 가능하게 만든다. 반드시 F 앞
        ↓
F. Host 권위 준비·전송 (로비) + timeout 10초 + 1회 재전송
        ↓
G. Client 수신·재조립·해시 대조 + MapReady ACK
        ↓
H. Client D 방식 재생성 검증 (Validate)  ← G 의 ACK 조건에 한 단계를 더 얹는다
        ↓
I. 씬 전환 게이트 + 임시 고정 seed 비활성화 + 실패 시 로비 유지   🔴 여기서 처음 동작이 뒤집힌다
```

**A~H 까지는 멀티 동작이 지금과 똑같다.** 임시 고정 seed 가 아직 살아 있고 게이트도 열려 있으므로 양쪽이 각자 같은 고정 seed 로 맵을 만든다. **I 커밋 하나가 그 전부를 뒤집는다** — 2단계에서 H 단계(투영)가 그랬던 것과 같은 모양이다.

> **왜 E(로그)를 2단계처럼 맨 끝에 두지 않는가.** 2단계의 로그 키(K)는 **이미 화면으로 확인되던 것을 기록으로 옮기는** 작업이라 마지막이 맞았다. 이번 F~I 의 실패는 **로그 말고는 관측 수단이 없다**(실패 UI 가 §10 으로 빠졌으므로 — §2-3). 로그가 없으면 F 를 켠 순간부터 「왜 안 넘어가는지」를 아무도 알 수 없다. 그래서 **F 앞으로 당긴다.**

> **왜 G 와 H 를 나누는가.** G 만으로도 규칙 16 의 *"해시가 같을 때만"* 은 성립한다(바이트 동일성 확인). H 는 그 위에 *"이 맵이 정말 그 seed 에서 나올 수 있는가"* 를 더한다(TDD 「Client 검증 순서」 4번). **G 가 끝난 상태도 그 자체로 완결된 커밋**이고, H 가 실패하면 G 까지 되돌리는 것이 아니라 H 만 되돌린다.

**단계마다 독립 커밋으로 끊는다.** 2단계가 그렇게 해서 중간에 세션이 끊겨도 이어갈 수 있었고, 실제로 실행 순서를 I↔J 로 바꿀 수도 있었다.

---

## 4. 🔴 전송 객체를 무엇으로 할 것인가 — 선택과 근거

### 4-1. 선택: **후보 A — 신규 `NetworkObject` 프리팹을 Host 가 동적 스폰한다** (`NetworkMapTransfer`)

Research §4-4 의 세 후보 중 **A 를 고른다.** 근거는 다른 둘이 각각 확정적으로 탈락하기 때문이다.

| 후보 | 판정 | 근거 |
|---|---|---|
| **A. 동적 스폰 `NetworkObject`** | ✅ **채택** | 규칙 16 의 *"씬에 종속되지 않는 공용 전송 경로"* 를 **문자 그대로** 만족하는 유일한 후보. 최초 경기와 `NewMap` 재경기가 **같은 객체·같은 프로토콜**을 쓴다는 요구도 이것만 담는다 |
| **B. 로비 씬에 `NetworkObject` 배치** | ❌ **탈락(확정)** | 로비 씬 오브젝트는 **전투 씬으로 넘어가면 사라진다.** `NewMap` 재경기는 전투 씬(결과 화면)에서 시작하므로 그 경로를 담을 수 없고, 이는 규칙 16 의 *"최초 경기와 `NewMap` 재경기는 모두 … 공용 전송 경로"* **정면 위반**이다. 「지금은 재경기를 안 하니까 괜찮다」는 성립하지 않는다 — 다음 범위(§10)에서 통째로 다시 만들게 된다 |
| **C. `NetworkGameManager` 를 `NetworkBehaviour` 로 승격** | ❌ **탈락** | 그 클래스는 **959행짜리 매니저이고 로비 UI 가 직접 참조**한다(Research §4-3·§13-8). `NetworkBehaviour` 가 되면 스폰 전에는 RPC 를 못 쓰므로 **초기화 순서 전체가 영향권**에 들어간다. 이번 단계의 되돌리기 비용이 이미 최대인데(§1) 거기에 가장 큰 회귀 위험을 얹는 선택이다 |

### 4-2. ⚠️ 이 선택의 전제는 **아직 확인되지 않았다**

> **후보 A 는 「NGO 2.9.2 에서 동적 스폰한 `NetworkObject` 가 `LoadSceneMode.Single` 씬 재로드를 넘어 생존하는 조건」에 걸려 있고, 그 조건은 Research 가 확인하지 못했다**(§13-1 — 패키지 소스가 이 환경에 없다: `Library/PackageCache` 부재. 프로젝트 안 선례도 0건이며 `DefaultNetworkPrefabs.asset` 에 매니저류 프리팹 등록 사례가 없다).

그래서 **그 확인을 계획의 첫 단계(A)로 둔다.** 추정으로 채우지 않는다(CLAUDE.md 규칙 10).
⚠️ **A 단계 결과가 나오기 전에는 B 단계에 착수하지 않는다.**

### 4-3. A 단계 결과에 따라 갈리는 두 갈래 — 미리 정해 두되 확정하지 않는다

| A 의 결과 | 진행 |
|---|---|
| **생존한다** (조건을 찾았다) | 계획대로 진행. 찾은 조건과 근거를 **TDD 「`NetworkMapTransfer` 전송 프로토콜」 절에 기록**한다(규칙 16 이 전송 기법의 단일 소스를 그쪽으로 지정) |
| **생존하지 않는다** | **대안 A′ — 씬 재로드 때마다 Host 가 다시 스폰하고, 씬을 넘어야 하는 상태는 `MapHandoff`(§5-3, Application 정적 홀더)가 들고 있게 한다.** ⚠️ **이 대안이 규칙 16 의 「씬에 종속되지 않는 공용 전송 경로」를 만족한다고 지금 단정하지 않는다** — 객체는 재생성되지만 클래스·프로토콜·상태는 같으므로 만족한다고 볼 여지가 있으나, 규칙 문언이 「객체가 살아 있어야 한다」를 요구하는지 「경로가 씬에 묶이지 않아야 한다」를 요구하는지는 문맥만으로 확정할 수 없다. **A 단계 결과를 손에 쥔 뒤 사용자에게 확인한다**(CLAUDE.md 규칙 12) |

### 4-4. 🔴 조각 전송 — 구조는 만들되, 실전에서 조각은 항상 1개다

**실측(Research §5-1)**: canonical 바이트는 **335~343바이트**이고 공식은 `319 + 4N + 20D`(N=중립 광산 수, D=장식 수)다. 장식은 규칙 3·12 가 *"최초 구현의 장식 목록은 항상 비어 있다"* 고 확정했으므로 D=0 이고, N 은 1~6 이라 **payload 는 `323 ~ 343바이트` 범위에 갇혀 있다.**
**설정 한도(Research §5-2)**: `UnityTransport.m_MaxPayloadSize` = **6144**(`Lobby.unity:7475` · `Game.unity:45469`). 343 ÷ 6144 = **5.6%**.

| 판단 | 내용 |
|---|---|
| **구조를 만든다** | 규칙 16 이 *"맵 데이터는 조각으로 나눠 신뢰성 있게 보내고, 모든 조각이 모여 선언된 크기와 일치한 뒤에야 해시 검증 → 역직렬화 → 공정성 검증"* 이라고 정한다. **`MapPrepareBegin` → `MapChunk` → `MapReady` 3메시지 상태 기계는 조각 수가 1이어도 그대로 성립**한다(Research §5-4) |
| **재조립·중복 무시·순서 뒤바뀜 처리도 넣는다** | TDD 가 명시한 계약이며(중복 chunk 무시 · `index` 순 재조립 · stale nonce 무시 · 부분 데이터 사용 금지), 장식이 도입되면 조각 수가 자연히 는다 |
| 🔴 **그 코드가 죽은 코드로 남는 위험을 이렇게 다룬다** | **①** 재조립기를 **`NetworkBehaviour` 밖의 순수 C# 클래스**로 분리한다 — 그러면 이 환경에서 `mcs`/`mono` 로 **조각 2개 이상인 입력을 만들어 실제로 돌려 볼 수 있다.** 실전에서 안 도는 것과 **한 번도 실행된 적 없는 것**은 다르다. **②** 그 실행 결과(조각 N개 재조립·중복·역순 입력)를 B 단계 실행 기록에 수치로 남긴다. **③** 실전 조각 수가 1이라는 사실 자체를 `TechnicalDesignDocument.md` 에 기록한다(그 문서가 이미 같은 취지를 적고 있으므로 **수치만 실측값으로 갱신**하는 형태 — 다만 그 갱신은 §11 참조) |

⚠️ **한도 숫자를 이 계획에 확정해 적지 않는다.** 규칙 16 이 *"조각 크기와 전체 한도의 숫자는 이 문서에 적지 않는다 … 값은 구현 시 NGO 실측으로 확정하고 근거와 함께 `TechnicalDesignDocument.md` 에 기록한다"* 고 지시한다. Research §5-3 이 **NGO RPC 한 번의 실효 페이로드 상한**과 **Relay 경유 실효 MTU** 를 못 쟀다고 밝혔으므로, **B 단계에 실측 항목으로 넣는다**(§7-B). **`m_MaxPayloadSize` 6144 는 설정값이지 실효 상한이 아니다** — NGO 헤더와 `FastBufferWriter` 오버헤드가 그 안에서 소비된다.

---

## 5. 🔴 아키텍처 소유권 — 어느 레이어가 무엇을 갖는가

### 5-1. 충돌의 모양 (Research §11 요약)

맵을 **만드는** 주체는 Application(`MapPreparationUseCase`, 일부러 `UnityEngine` 미참조 순수 C#)이고, 맵을 **보내는** 주체는 Infrastructure 여야 한다(RPC 는 `NetworkBehaviour` 에서만 나가고 그것은 Infrastructure 전용). 그런데 규칙 16 은 「Host 가 유일한 권위자」라는 **하나의 책임**으로 생성·검증·전송·재전송·timeout·해시 대조를 묶는다.

### 5-2. 결정 — **비동기 왕복의 주인을 Infrastructure 에 두고, Application 은 동기 함수 두 개만 제공한다**

| 레이어 | 이번에 소유하는 것 | 신설/변경 |
|---|---|---|
| **Domain** | canonical 인코딩·해시·검증기 — `MapDefinitionCodec.Encode/Decode/ComputeHash/HashEquals`, `MapDefinitionValidator.Validate`, 생성기 5종 | **변경 없음** (이미 전부 있다 — Research §6-1) |
| **Application** | ① **준비** `MapPreparationUseCase.Prepare(rootSeed, testMode)` — **무변경** ② **검증 재생성** `MapVerificationUseCase.Verify(받은 바이트)` — **신설, 순수 C#** ③ **확정 맵 인계 홀더** `MapHandoff` — **신설, 순수 C#** | 신설 2 |
| **Infrastructure** | **비동기 왕복 전부** — `NetworkMapTransfer : NetworkBehaviour` 가 조각화·RPC 3종·nonce·timeout 10초·1회 재전송·ACK 대기·해시 대조·씬 전환 게이트를 소유 | 신설 1 (+ `NetworkGameManager` 에 위임 진입점 1개) |
| **Bootstrap** | 배선과 투영 — `GameBootstrapper` 가 `MapHandoff` 에서 확정 정의를 받아 `ProjectMap` | 변경 |
| **Presentation** | **이번 범위 없음** — `BattleViewModel` 의 호출 대상 한 줄 교체(제거-3)뿐 | 변경 1줄 |

### 5-3. 🔴 근거 — 왜 「Application 인터페이스 ← Infrastructure 구현」 선례를 이번에는 쓰지 않는가

Research §11-3 은 같은 맵 계통의 `IMapFallbackTemplateSource`(Application 인터페이스 · Infrastructure 구현)를 선례로 들면서, **그것이 동기 조회인 반면 전송은 비동기 왕복이라 같은 형태가 성립하는지는 미확인**이라고 남겼다(§13-4).

> 🔴 **이 계획은 그 미확인에 답하지 않는다 — 답이 필요 없는 자리로 옮겼기 때문이다.**
> 의존성 역전 인터페이스는 **Application 이 Infrastructure 의 기능을 필요로 할 때**의 해법이다. 그런데 이번에는 **Application 이 전송을 필요로 하지 않는다.** 전송을 시작·재시도·완료 판정하는 주체가 Infrastructure 이고, 그쪽이 Application 의 순수 함수를 **불러 쓴다.** 방향이 `Infrastructure → Application` 이므로 **역참조가 아니고, 뒤집을 것도 없다.**

이 배치가 지키는 것 세 가지 (전부 `.claude/MEMORY.md` 의 제약이며 Research §11-1 이 현재 지켜지고 있음을 실측했다):

1. **Application → `Unity.Netcode` 직접 참조 0건 유지** — 신설 `MapVerificationUseCase`·`MapHandoff` 는 순수 C# 이라 이 환경에서 `mcs`/`mono` 로 **실제 컴파일·실행 검증이 가능**하다. 2단계가 A~G 를 그렇게 검증했다.
2. **Application → Infrastructure 역참조 0건 유지.**
3. **`NetworkBehaviour` 는 Infrastructure 에만.**

### 5-4. 확정 맵 인계 통로 — `MapHandoff`(Application 정적 홀더)

**문제**: 맵은 **로비에서** 확정되는데 투영은 **전투 씬에서** 일어난다. 그 사이에 `LoadSceneMode.Single` 재로드가 있다.

**해법**: 선례 `NetworkContext`(`Application/NetworkContext.cs:23` — Infrastructure 가 `Set()`, Application 이 읽는 정적 홀더)와 **같은 모양**으로 `MapHandoff` 를 둔다. 전송 객체(Infrastructure)가 확정된 `MapPreparationResult` 를 심고, `GameBootstrapper`(Bootstrap)가 전투 씬에서 읽어 `ProjectMap` 한다.

> Research §11-3 이 *"정적 홀더는 상태 조회에는 맞지만 「조각을 보내고 ACK를 기다린다」 같은 비동기 대화에는 맞지 않는다"* 고 적었다. **여기 쓰임은 정확히 「상태 조회」다** — 비동기 대화는 Infrastructure 안에서 끝나고, 홀더에는 **끝난 결과**만 들어간다. 그래서 이 지적에 저촉되지 않는다.

**그 결과 `IGameServices` 는 확장하지 않는다.** Research §11-4 는 `MapPreparationResult` 가 Application 타입이라 `IGameServices` 에 `GetLastMapPreparation()` 를 추가해도 **역참조 위반이 아니라고** 확인했지만, **뚫을 필요 자체가 없어졌다** — 필요한 방향은 「Infrastructure 가 준비 결과를 읽는다」가 아니라 「Bootstrap 이 확정 결과를 읽는다」이고, 그것은 홀더 하나로 끝난다. **인터페이스 멤버 15개를 16개로 늘리지 않는다.**

> 🔴 **그래서 `GameBootstrapper.GetLastMapPreparation()`(`GameBootstrapper.cs:478`)의 호출자는 3단계가 끝나도 0건이다.** Research §12-4 는 그 자리의 주석을 *"틀린 주석이 아니라 앞선 주석"* 이라 판정했으나, **이 설계에서는 앞서지도 않는다.** 그래서 제거-4 로 **주석 문장을 사실에 맞게 고친다** — 「재현을 위해 보관한다」는 유효한 이유는 남기고, 「다른 코드가 다시 읽는다」는 없어질 예정이 없는 주장은 지운다. **접근자 자체는 지우지 않는다**(재현 목적의 진입점이며, 지우는 것은 이번 범위가 아니다).

---

## 6. 🔴 Client 검증 — 「D 방식: 받은 맵을 스스로 다시 만들어 대조한다」 (사용자 확정)

### 6-1. 무엇이 문제였나 (Research §6-3)

`MapDefinitionValidator.Validate(definition, constraints, generator)` 는 **`constraints` 를 필수 인자로 받는다.** 그 값은 생성 시도마다 `TryApplyTerrain(builder, terrainStream, out constraints)` 가 지형 PRNG 스트림에서 만들어 내고, 그 스트림은 `(MapVersion, RootSeed, Terrain, AttemptIndex)` 로 파생된다. **그런데 `AttemptIndex` 도 `StartingMineSide` 도 canonical 바이트에 없다.** 그래서 Client 는 받은 바이트만으로는 `Validate()` 를 부를 수 없었다.

### 6-2. D 방식이 그것을 푸는 방법

Client 는 받은 canonical 바이트에서 **`MapVersion` · `RootSeed` · `MapType` · `NeutralMineCount` · `TestModeFlag` · `InitialGold`** 를 읽어 낼 수 있다(`Decode`). 그 값으로 **생성기를 돌려 바이트가 완전히 같아지는 `(AttemptIndex, StartingMineSide)` 조합을 찾는다.** 찾으면 그 시도의 `Constraints` 를 손에 넣게 되고, 그것으로 `Validate()` 를 부른다.

**미지수는 둘뿐이다** — `AttemptIndex`(0~99, 규칙 12 의 최대 100회) × `StartingMineSide`(A/B) = **최대 200조합**. 나머지 값은 전부 받은 바이트에서 읽는다.

### 6-3. ✅ 실측 근거 — 메인 세션이 실제로 실행해 확인한 수치

```
시드 1~40 전부 복원 성공 (실패 0건)
평균 생성 시도 1.4회 · 최대 2회
복원 뒤 MapDefinitionValidator.Validate() 까지 전부 통과
```

> 🔴 **구현 요령 — 이것을 빠뜨리면 어떤 조합도 일치하지 않는다.**
> **재생성한 정의에 `TestModeFlag` 와 `InitialGold` 를 「받은 바이트에서 읽은 값」으로 심어 준 뒤 인코딩해야 바이트가 일치한다.**
> 준비 조정자(`MapPreparationUseCase`)가 생성 **뒤에** 그 두 값을 심고, `Encode` 가 그것을 canonical 입력에 포함하기 때문이다.
> **메인 세션이 실제로 그 함정에 한 번 빠졌다가 고쳤다.** 2단계 G 단계 실행 기록도 같은 이유로 *"폴백에서는 값 교체 뒤 해시를 다시 계산한다"* 를 🔴 로 적어 두었다 — **같은 함정이다.**

### 6-4. D 를 고른 근거 (사용자 확정 — 대안이 왜 폐기됐는지 포함)

| | 내용 |
|---|---|
| **형식을 건드리지 않는다** | canonical 바이트 형식이 그대로이므로 **`MapVersion` 상승이 없고, 폴백 템플릿 5개(`Resources/MapTemplates/*.bytes`)를 다시 만들 필요도 없다.** |
| **검증에 쓰는 값을 해시가 보호한다** | 검증에 들어가는 값이 **전부 canonical 바이트에서 나오므로**, 그 바이트를 지키는 SHA-256 이 검증 재료까지 그대로 보호한다 |
| **「공정한가」에 더해 「그 seed 에서 나올 수 있는 맵인가」까지 증명한다** | Host 가 맵을 손으로 뜯어고치면 **어떤 `(AttemptIndex, StartingMineSide)` 조합으로도 바이트가 재현되지 않아 즉시 걸린다.** 해시 대조만으로는 「Host 가 보낸 것과 Client 가 받은 것이 같다」까지만 알 수 있고, 그 맵이 정당하게 생성된 것인지는 알 수 없다 |
| ❌ **폐기: 「필요한 값을 canonical 바깥에 실어 보내기」** | `AttemptIndex`·`StartingMineSide` 를 package 헤더에 함께 보내는 안. **검증에 쓰는 값 자체를 해시가 안 지키므로 사실상 검증이 성립하지 않는다** — 위변조된 `AttemptIndex` 를 그대로 믿고 `Validate()` 를 부르는 꼴이 된다 |
| ❌ **폐기: 「canonical 바이트에 두 필드 추가」** | 해시는 보호하지만 **`MapVersion` 을 올려야 하고 폴백 `.bytes` 5개를 전부 다시 만들어야 한다** |
| ❌ **폐기: 「Client 검증 범위를 좁힌다」** | TDD 「Client 검증 순서」 4번을 만족하지 못한다 — 사양 축소다 |

### 6-5. 🔴 규칙 16 문언과의 긴장 — 다음 사람이 규칙 위반으로 오해하지 않게 여기 못 박는다

> 🔴 **규칙 16 은 「Client 는 전달받은 최종 맵을 그대로 로드하며 별도로 추첨·재생성·폴백하지 않는다」고 정한다. 이 계획의 D 방식 재생성은 그 문장과 충돌하지 않는다 — 실제로 로드해서 쓰는 맵은 받은 바이트를 `Decode` 한 것 하나뿐이고, 재생성한 맵은 `Constraints` 를 얻기 위한 대조용으로만 쓰고 그 자리에서 버리기 때문이다.** 규칙이 금지한 것은 **Client 가 자기 맵을 만들어 그것을 쓰는 것**이며(그러면 두 사람이 서로 다른 전장에서 싸운다), 여기서 재생성한 맵은 **받은 바이트와 한 바이트라도 다르면 그 순간 실패로 판정되는 대조 대상**이다 — 즉 이 절차는 Client 가 맵을 고를 자유를 **늘리는 것이 아니라 없애는 쪽으로만 작동한다.** 「추첨」도 하지 않는다 — `MapType`·`NeutralMineCount`·`RootSeed`·`TestModeFlag`·`InitialGold` 는 전부 받은 바이트에서 읽고, Client 가 스스로 정하는 값은 하나도 없다(탐색하는 `AttemptIndex`·`StartingMineSide` 조차 「고르는」 것이 아니라 **받은 바이트가 이미 정해 둔 답을 찾아내는 것**이며, 답이 없으면 실패다). 「폴백」도 하지 않는다 — 복원에 실패하면 폴백 템플릿으로 넘어가는 것이 아니라 **맵 준비 실패**다. 🔴 **규칙 문서는 고치지 않는다. 이 문단이 그 해석의 기록이며, 규칙 문언과 구현이 어긋난 것이 아니라 문언이 금지한 행위를 하지 않는다는 판정이다.**

⚠️ **이 해석에 기대어 범위를 넓히지 말 것.** 위 문단이 허용하는 것은 **대조 목적의 재생성 하나뿐**이다. 「어차피 Client 도 생성기를 돌리니까 전송 없이 seed 만 보내면 된다」는 정반대의 결론이며, 규칙 16 이 명시적으로 금지한다(*"최종 맵 전체 데이터를 … 전달한다"*). Research §7-3 이 *"이 구분이 흐려지면 「Client 도 생성기를 돌린다」는 설계로 잘못 미끄러진다"* 고 경고한 그 지점이다.

### 6-6. D 방식이 부수적으로 해소하는 것 — Research §12-2 의 어긋남

Research §12-2 는 **규칙 16 이 「전달한다」고 정한 항목 중 `StartingMineSide` 가 현재 package 정의로는 전달되지 않는다**는 어긋남을 기록했다. **D 방식은 이것을 해소한다** — Client 가 복원 탐색으로 `StartingMineSide` 를 **알아내기** 때문이다. 값을 실어 보내는 것이 아니라 **받은 바이트에서 유도**하므로 해시 보호 밖으로 새는 값도 생기지 않는다. **이 해소는 D 방식의 부수 효과이며 이번 범위 안에서 닫힌다.**

---

## 7. 단계별 계획 (A~I — 각 단계가 독립 커밋)

> **검증 수단의 한계를 먼저 못 박는다.** 이 환경에서 **순수 C# 은 `mcs`/`mono` 로 실제 컴파일·실행된다.**
> 그러나 **`UnityEngine`·`Unity.Netcode` 를 참조하는 파일은 컴파일조차 되지 않는다.**
> **멀티플레이 동작은 사용자 실기 2대로만 확인된다.** 각 단계의 「검증」 칸은 이 구분을 지켜 적었다.

### A. NGO 씬 재로드 생존 조건 확인 (코드 0줄 또는 최소 프로브)

**한 줄 요약**: 후보 A 의 전제 — 동적 스폰 `NetworkObject` 가 `LoadSceneMode.Single` 재로드를 넘어 생존하는 조건 — 을 실측으로 확정한다.
**의존**: 없음 (첫 단계)
**규칙 근거**: 규칙 16 *"씬에 종속되지 않는 공용 전송 경로"*
**Research 근거**: §13-1(미확인 1번) · §4-4 후보 A

| 항목 | 내용 |
|---|---|
| 할 일 | ① NGO 2.9.2 문서에서 씬 재로드 시 동적 스폰 오브젝트의 파괴 조건을 확인한다 ② 프로젝트에 **최소 프로브 프리팹 1개**(빈 `NetworkBehaviour`, 스폰 시각과 재로드 후 생존 여부만 로그)를 두고 에디터 2인 구성으로 재경기 재로드를 통과시켜 본다 ③ 결과와 근거를 §14 실행 기록에 적는다 |
| **검증** | **사용자 실기 2인 구성** — 이 항목은 다른 방법이 없다. `mcs` 로는 `Unity.Netcode` 를 컴파일할 수 없다 |
| 결과 처리 | §4-3 의 두 갈래대로. **생존하지 않으면 대안 A′ 채택 여부를 사용자에게 확인한 뒤 B 로 넘어간다** |
| ⚠️ | 프로브 코드는 **B 착수 시 제거한다.** 남기면 아무도 안 쓰는 `NetworkObject` 가 프리팹 목록에 영구히 남는다 |

---

### B. `NetworkMapTransfer` 신설 + 3메시지 골격 + 전송 한도 실측

**한 줄 요약**: 전송 객체와 3메시지 상태 기계의 뼈대를 세우고, **못 쟀던 두 숫자를 실측**한다. 아직 아무 맵도 보내지 않는다.
**의존**: A (전제 확정 후에만)
**규칙 근거**: 규칙 16 (씬 비종속 경로 · 조각 전송 · 부분 데이터 사용 금지) · TDD 「`NetworkMapTransfer` 전송 프로토콜」
**Research 근거**: §4-2(로비 `NetworkObject` 0개) · §5-1~§5-4 · §13-2

| 항목 | 내용 |
|---|---|
| 신설 | `Infrastructure/Network/NetworkMapTransfer.cs` (`NetworkBehaviour`) — RPC 3종 `MapPrepareBeginClientRpc` / `MapChunkClientRpc` / `MapReadyServerRpc`. **NGO 규약대로 메서드명이 `ServerRpc`/`ClientRpc` 로 끝난다** |
| 신설 | `Infrastructure/Network/MapChunkAssembler.cs` — **순수 C#**(`NetworkBehaviour` 밖). 조각 분할·재조립·중복 무시·`index` 순 정렬·`totalBytes` 일치 확인·**부분 데이터 반환 금지** |
| 신설 | `Assets/_Project/Prefabs/Network/NetworkMapTransfer.prefab` + `DefaultNetworkPrefabs.asset` 등재 |
| 🔴 **실측 항목** | ① **NGO RPC 한 번의 실효 페이로드 상한** — payload 를 키워 가며 실패 지점을 찾는다. `m_MaxPayloadSize`(6144)는 설정값이지 실효 상한이 아니다 ② **Relay 경유 실효 MTU** — Relay 세션(실기 2대)에서 같은 방식으로 잰다. **로컬(127.0.0.1) 측정값을 Relay 값으로 삼지 않는다** ③ 두 값과 **측정 방법**을 `TechnicalDesignDocument.md` 「전송 프로토콜」 절에 기록한다(규칙 16 이 그렇게 지시) |
| **검증** | ⓐ **`mcs`/`mono` 로 `MapChunkAssembler` 단독 컴파일·실행** — 조각 1개 / 여러 개 / 중복 / 역순 / 하나 빠짐 / 길이 불일치 6가지 입력을 실제로 돌려 기대 결과와 대조하고 **그 수치를 §14 에 적는다**(§4-4 의 「죽은 코드로 남는 위험」 대응) ⓑ `NetworkMapTransfer.cs` 는 **컴파일 검증 불가** — Unity 에서만 확인된다 ⓒ 실효 상한·MTU 는 **사용자 실기** |
| ⚠️ | 이 단계에서는 **맵을 보내지 않는다.** 스폰·수명·RPC 왕복과 한도만 확인한다. 맵을 얹는 것은 F |

---

### C. 확정 맵 인계 통로 `MapHandoff` + 낡은 주석 정정

**한 줄 요약**: 로비에서 확정한 맵을 전투 씬으로 넘기는 정적 홀더를 만들고, 사실이 아닌 주석 한 곳을 고친다.
**의존**: 없음 (B 와 독립 — 순서를 바꿔도 된다)
**규칙 근거**: 규칙 16 *"전투 씬에서는 로비에서 확정한 맵 데이터만 사용해 맵을 구성한다"*
**Research 근거**: §6-2 · §11-4 · §12-4

| 항목 | 내용 |
|---|---|
| 신설 | `Application/MapHandoff.cs` — **순수 C# 정적 홀더**. 선례는 같은 폴더의 `NetworkContext.cs:23`. 멤버는 `Set(MapPreparationResult)` · `TryTake(out MapPreparationResult)` · `Clear()` 세 개 |
| 🔴 설계 | **`TryTake` 는 「읽고 비운다」**로 둔다. 남겨 두면 다음 판에 **지난 판 맵이 조용히 재사용될** 수 있고, 그것은 규칙 16 의 「로비에서 확정한 맵 데이터만」을 어기는 가장 조용한 방식이다. 로비 복귀·연결 종료 시 `Clear()`(규칙 14 의 *"로비 복귀 또는 연결 종료 시 폐기"* 와 같은 취지) |
| 변경 | `GameBootstrapper.Map.cs:453~454` **주석 문장 교체**(제거-4). 「재현을 위해 보관한다」는 남기고 「다른 코드가 `GetLastMapPreparation()` 으로 다시 읽는다」는 지운다 — 3단계가 끝나도 그 호출자는 0건이다(§5-3) |
| **비변경** | 🔴 **`IGameServices` 는 손대지 않는다**(멤버 15개 유지). 근거는 §5-3 |
| **검증** | **`mcs`/`mono` 로 `MapHandoff` 단독 컴파일·실행** — `Set`→`TryTake` 1회 성공 / 2회째 실패 / `Clear` 후 실패를 실제로 확인. 주석 정정은 `grep` 으로 확인 |

---

### D. 준비/투영 분리 (동작 무변경 리팩터)

**한 줄 요약**: `PrepareAndProjectMap()` 을 `PrepareMap()` + `ProjectMap(정의)` 로 가른다. **동작은 한 줄도 바뀌지 않는다.**
**의존**: C (`ProjectMap` 이 받을 타입이 정해져야 한다)
**규칙 근거**: 규칙 16 (해시 대조가 씬 전환보다 먼저 → 준비와 투영이 다른 시점·다른 주체가 된다)
**Research 근거**: §7-3 · §7-4 (제거-2)

| 항목 | 내용 |
|---|---|
| 변경 | `GameBootstrapper.Map.cs` — `PrepareAndProjectMap()`(`:433`) 을 둘로 가르고 **같은 이름의 래퍼**가 둘을 연달아 부르게 남긴다. 호출부(`:110`)는 무변경 |
| 🔴 이 단계가 하지 **않는** 것 | 임시 고정 seed 분기(`:633~634`)는 **그대로 살려 둔다.** 멀티도 여전히 로컬 준비다. 뒤집는 것은 I |
| **검증** | ⓐ `GameBootstrapper.Map.cs` 는 **`UnityEngine` 참조라 컴파일 검증 불가** ⓑ **사용자 실기 — 싱글 회귀**: 매 판 다른 맵이 나오고 경기가 정상 동작하는가. 로그의 `MapPreparationSucceeded` 가 경기당 **여전히 한 줄만** 나오는가(2단계 K 실기 항목과 같은 확인) |
| ⚠️ | **리팩터의 성공 기준은 「아무것도 달라지지 않는 것」이다.** 여기서 동작이 조금이라도 달라지면 그것은 이 단계의 버그다 |

---

### E. 로그 키 신설

**한 줄 요약**: F 이후의 실패를 관측 가능하게 만든다. 규칙 12 의 로그 필수 항목 중 **3단계에서 값이 생기는 2가지**를 채운다.
**의존**: 없음 (그러나 **반드시 F 앞**)
**규칙 근거**: 규칙 12 「로그 필수 항목」 · 규칙 16 *"불일치 내용을 기록한다"* · `LogRules.md` **1.5**
**Research 근거**: §8-4 (맵 관련 키 4개뿐, 전송·해시 관련 0건)

**현재 `LogEvent` 멤버는 41개다**(2026-09-09 실측 — `Application/Interfaces/ILogSink.cs` 의 enum 본문을 파싱해 셈. 2단계 K 가 37→41 로 늘린 값과 일치).

**신설 키(안) — `LogRules.md` 1.5 의 신설 기준 두 개(① 집계가 섞이면 안 된다 ② 조치가 다르다)를 각 키가 둘 다 충족하는지로 판정한다.**

| 키(안) | 축 A 심각도 | 축 B 존속 | 기준 ① 집계 | 기준 ② 조치 |
|---|---|---|---|---|
| `MapTransferSucceeded` | Info | 운영 | 성공 경기 수 — 아래 실패 지표들의 **분모**가 된다. 실패 키에 섞으면 발생률을 못 낸다 | 조치 없음(정상) |
| `MapTransferRetried` | **Warn** | 운영 | *"재전송으로 복구된 건수"* — 성공에도 실패에도 섞이면 **네트워크 품질 지표가 사라진다** | 조치 = 조각 크기·timeout 재검토 |
| `MapTransferFailed` | **Error** | 운영 | timeout·불완전 수신으로 **끝내 실패**한 건수 | 조치 = 전송 경로·한도 |
| `MapHashMismatch` | **Error** | 운영 | **전송은 됐는데 내용이 다른** 경우. 위 `MapTransferFailed`(전송 자체 실패)와 섞으면 **원인이 정반대인 두 사건이 한 지표**가 된다 | 조치 = 직렬화·해시 계약 점검(전송 재시도는 규칙 16 상 금지) |
| `MapClientVerificationFailed` | **Error** | 운영 | Client 가 **복원 실패 또는 `Validate()` 실패**. 해시는 맞는데 맵이 부당한 경우이며, 위 둘과 원인·조치가 전부 다르다 | 조치 = 생성기·검증기 |

- **축 B 판정**: 다섯 키 모두 **① 플레이어 기기(Relay 경유 실기)에서만 벌어지고 ② 이 로그가 없으면 추적 수단이 없다**(실패 UI 는 §10 이라 이번엔 존재조차 하지 않는다) → **운영**.
- **`Error`는 항상 `운영`**(LogRules 1.3 원칙 3)과도 일치한다.
- 🔴 **키는 enum 맨 뒤에 붙인다.** 2단계 J 가 토스트 키에서 확인한 대로, 중간에 끼우면 뒤 값이 통째로 밀린다.
- 남길 항목: **전송/재전송 횟수 · Host/Client 해시 비교 결과**(규칙 12 의 14항목 중 3단계 몫) + 실패 단계 · 내부 error code.
- 🔴 **해시는 원본 32바이트로 대조하고, 로그에는 앞 16자만 싣는다.** 2단계 K 가 이미 그렇게 정하고 코드 주석에 적어 두었다(`GameBootstrapper.Map.cs` 의 `ToMapHashField()`) — **그 결정을 3단계가 이어받는다.**

| 항목 | 내용 |
|---|---|
| 변경 | `Application/Interfaces/ILogSink.cs` (`LogEvent` enum 맨 뒤에 5개 추가 → **41 → 46개 예상**) |
| 변경 | `Assets/_Project/Docs/LogRules.md` **1.5 에 신설 블록 추가** — 2026-08-19·2026-09-08 블록과 같은 형태로 **개수 변화와 나눈 이유**를 적는다 |
| **검증** | ⓐ **`mcs` 로 `ILogSink.cs` 단독 컴파일** + 파이썬으로 enum 본문 파싱해 **개수 실측** ⓑ `LogRules.md` **1.14 금지 항목 전수 점검** — 특히 「한 사건에 두 줄」이 나지 않는가(성공/재전송/실패/해시불일치/검증실패가 **배타적**인지 확인) ⓒ 실제 출력은 F~I 실기에서 |
| ⚠️ | **키 개수와 이름은 위 표가 「안」이다.** F~I 를 구현하며 실제로 필요한 갈래가 달라지면 그때 조정하고, **조정했다는 사실을 §14 에 적는다** |

---

### F. Host 권위 준비·전송 (로비) + timeout 10초 + 1회 재전송

**한 줄 요약**: Host 가 로비에서 맵을 준비해 Client 에게 조각으로 보낸다. **아직 게이트는 열려 있다.**
**의존**: B · C · E
**규칙 근거**: 규칙 12 *"Host가 유일한 맵 생성 권위자"* · 규칙 16 (전달 항목 · 조각 전송 · timeout 10초 · **1회만** 재전송)
**Research 근거**: §4-5 · §6-1 · §7-4

| 항목 | 내용 |
|---|---|
| Host 흐름 | 로비에서 `MapPreparationUseCase.Prepare(rootSeed, testMode)` 실행 → `MapPreparationResult` 의 `CanonicalBytes`·`Hash` 를 그대로 씀(이미 있다 — Research §6-1) → `MapPrepareBegin(matchNonce, mapVersion, totalBytes, chunkCount, SHA256)` → `MapChunk` × N → `MapReady` 대기 |
| 🔴 재전송 규정 | **timeout 또는 조각 불완전 수신일 때만** 같은 nonce·같은 package 를 **1회** 재전송. 두 번째 10초에도 실패하면 종료. **미지원 `MapVersion` · 용량 한도 초과 · 해시 불일치 · 역직렬화 실패 · 공정성 검증 실패 · 연결 끊김은 즉시 실패**(재전송 없음) |
| 🔴 헤더/본문 `mapVersion` 이중화 | TDD 는 package 헤더에도 `mapVersion` 을 두고 *"canonical `MapDefinition.MapVersion`과 일치해야 한다"* 고 정한다. **값이 두 군데가 되므로 「불일치 시 실패」 판정이 새로 필요하다**(Research §9 — 지금은 값이 한 곳뿐이라 그 판정이 없다) |
| Host 가 **하지 않는** 것 | 이 단계에서는 **씬 전환을 막지 않는다.** 게이트는 I |
| **검증** | ⓐ 조각 분할부는 B 의 `MapChunkAssembler` 로 이미 검증됨 ⓑ **사용자 실기 2대** — `MapPrepareBegin` 이 나가고 `MapReady` 가 돌아오는가, 재전송이 0회인가, 로그에 전송/재전송 횟수가 남는가 |

---

### G. Client 수신·재조립·해시 대조 + `MapReady` ACK

**한 줄 요약**: Client 가 조각을 모아 길이·버전·SHA-256 을 확인하고 결과를 되돌려 준다.
**의존**: F
**규칙 근거**: 규칙 16 *"모든 조각이 모여 선언된 크기와 일치한 뒤에야 해시 검증 → 역직렬화 → 공정성 검증"* · *"부분 데이터는 어떤 검증이나 맵 구성에도 쓰지 않는다"* · TDD 「Client 검증 순서」 1~3
**Research 근거**: §6-1 (`ComputeHash`·`HashEquals` 이미 존재)

| 항목 | 내용 |
|---|---|
| Client 순서 | ① package 전체 길이 · `canonicalLength` · 지원 `MapVersion` 확인 ② `MapDefinitionCodec.ComputeHash(받은 바이트)` 재계산 후 `HashEquals` 로 전달된 digest 와 비교 ③ 맞을 때만 `Decode` ④ `MapReady(success, clientSHA, errorCode)` 전송 |
| 🔴 | **대조는 원본 32바이트로 한다**(로그의 16자 문자열이 아니다 — 2단계 K 가 코드 주석에 명시해 둔 결정) |
| 이 단계가 하지 **않는** 것 | `Validate()` 는 아직 안 부른다(H). 그리고 받은 정의를 **아직 쓰지 않는다**(I) |
| **검증** | **사용자 실기 2대** — Host/Client 해시 비교 결과가 로그에 남고 일치하는가. 추가로 **인위적 불일치 시험**(§9-라 참조) |

---

### H. Client D 방식 재생성 검증 (`Validate`)

**한 줄 요약**: 받은 맵을 스스로 다시 만들어 바이트가 같아지는 조합을 찾고, 그 `Constraints` 로 공정성 검증을 통과시킨다.
**의존**: G
**규칙 근거**: TDD 「Client 검증 순서」 **4번**(semantic fairness validator 실행) · 규칙 16 *"공정성 검증 실패는 즉시 실패"*
**Research 근거**: §6-3 · 사용자 확정 D 방식(§6)

| 항목 | 내용 |
|---|---|
| 신설 | `Application/UseCases/MapVerificationUseCase.cs` — **순수 C#**. 입력은 받은 canonical 바이트 하나, 출력은 `(성공 여부, 복원된 AttemptIndex·StartingMineSide, Constraints, 실패 사유)` |
| 절차 | ① `Decode` 로 `MapVersion`·`RootSeed`·`MapType`·`NeutralMineCount`·`TestModeFlag`·`InitialGold` 추출 ② `(AttemptIndex 0~99) × (StartingMineSide A/B)` 를 돌며 생성 → 🔴 **재생성 정의에 `TestModeFlag`·`InitialGold` 를 받은 값으로 심고** `Encode` → 바이트 완전 일치 탐색 ③ 찾은 시도의 `Constraints` 로 `MapDefinitionValidator.Validate()` ④ 어느 단계든 실패하면 **즉시 실패**(재전송 없음) |
| 상한 | 탐색 상한은 **`AttemptIndex` 0~99**(규칙 12 의 최대 100회와 같은 값) × 2 = 최대 200조합. 넘으면 실패 |
| 실측 근거 | **시드 1~40 전부 복원 성공(실패 0건) · 평균 생성 시도 1.4회 · 최대 2회 · `Validate()` 전부 통과** (§6-3) |
| **검증** | 🔴 ⓐ **`mcs`/`mono` 로 실제 컴파일·실행 가능하다** — 순수 C# 이므로 **시드를 대량으로 돌려 복원 성공률·평균 시도 수를 재측정**하고 §14 에 적는다. 40개보다 훨씬 넓게 돌린다(§9-다) ⓑ **사용자 실기** — 실제 멀티 경기에서 Client 검증이 통과하고 로그에 남는가 |

---

### I. 씬 전환 게이트 + 임시 고정 seed 비활성화 + 실패 시 로비 유지 🔴

**한 줄 요약**: **여기서 처음 동작이 뒤집힌다.** 해시가 같을 때만 전투 씬으로 넘어가고, Client 는 받은 맵을 투영한다.
**의존**: D · F · G · H 전부
**규칙 근거**: 규칙 16 *"해시가 같을 때만 전투 씬 전환을 시작한다"* · *"맵 준비가 실패하거나 해시가 다르면 전투 씬으로 이동하지 않고 기존 로비를 유지하며 불일치 내용을 기록한다"* · *"어떤 실패에서도 전투 씬으로 전환하지 않는다"*
**Research 근거**: §4-5(게이트 위치) · §7-1~§7-4(제거-1·제거-3)

| 항목 | 내용 |
|---|---|
| 게이트 위치 | `BattleViewModel.cs:305` 의 `_networkManager.LoadGameScene()` 직접 호출을 **「맵 준비 요청」으로 교체**(제거-3). 성공 시 `NetworkMapTransfer` 가 `NetworkGameManager.LoadGameScene()`(`:770`)을 부른다 |
| 🔴 소유권 | **게이트의 주인은 `NetworkMapTransfer`(Infrastructure)다.** `NetworkGameManager` 는 `NetworkBehaviour` 로 **승격하지 않고**(후보 C 탈락 — §4-1) **위임 진입점 하나만** 받는다. 이미 `DontDestroyOnLoad` 인 순수 `MonoBehaviour`(`:37`·`:104`)라 로비에서 살아 있고, ViewModel 이 이미 이 매니저를 참조하므로 회귀 면적이 가장 작다 |
| 뒤집기 | 제거-1(임시 고정 seed 분기 + 상수) **주석 비활성화**. 멀티에서는 `CreateRootSeed()` 가 **애초에 호출되지 않는다** — Host 는 로비에서 이미 뽑았고 Client 는 뽑지 않는다(Research §7-4) |
| Client 투영 | `MapHandoff.TryTake()` 로 확정 정의를 받아 `ProjectMap` 만 수행. **준비하지 않는다** |
| 실패 동작 | 로비 유지 + 로그. **팝업·`Retry` 는 §10**(근거는 §2-3) |
| **검증** | **사용자 실기 2대 — 이것이 유일한 검증이다.** ① 매 판 다른 맵 ② 양쪽 화면이 같은 맵 ③ 경기 정상 동작 ④ 인위적 불일치 시 씬이 안 넘어가고 로그가 남는가 ⑤ **싱글 회귀** — 싱글이 여전히 매 판 다른 맵으로 정상 동작하는가 |
| ⚠️ | **이 커밋 하나가 멀티 경로 전체를 갈아끼운다.** 문제가 생기면 「전송이 잘못됐나 / 게이트가 잘못됐나 / 투영이 잘못됐나」가 섞인다 → 완화는 §9-가 |

---

## 8. 규칙 근거 매핑 (`WORKFLOW.md` [4] — 각 항목이 어느 규칙에 근거하는지)

| 단계 | 규칙 문서 (`GameSystemRules_RandomMap.md`) | 그 밖의 규칙 | TDD |
|---|---|---|---|
| A NGO 생존 조건 | 규칙 16 (씬 비종속 경로) | — | `NetworkMapTransfer` 전송 프로토콜 |
| B 전송 골격 · 한도 실측 | 규칙 16 (조각 전송 · 부분 데이터 금지 · **숫자는 실측으로 확정**) | — | 전송 프로토콜 · timeout/retry state machine |
| C `MapHandoff` | 규칙 16 (*"전투 씬에서는 로비에서 확정한 맵 데이터만 사용"*) · 규칙 14 (*"로비 복귀 또는 연결 종료 시 폐기"*) | — | — |
| D 준비/투영 분리 | 규칙 16 (해시 대조가 씬 전환보다 먼저) | — | 생성·검증 실행 모델 |
| E 로그 키 | 규칙 12 「로그 필수 항목」 · 규칙 16 (*"불일치 내용을 기록한다"*) | — | — (`LogRules.md` **1.5**) |
| F Host 전송 | 규칙 12 (Host 유일 권위자) · 규칙 16 (전달 항목 · timeout 10초 · 1회 재전송 · 즉시 실패 6종) | — | 전송 프로토콜 · timeout/retry state machine |
| G 해시 대조 | 규칙 16 (조각 완결 후 해시→역직렬화→공정성 순서 · 부분 데이터 금지) | — | Client 검증 순서 1~3 |
| H D 방식 검증 | 규칙 16 (공정성 검증 실패 = 즉시 실패) · 규칙 13 (생성 완료 검증) | `GameSystemRules_Map.md` 규칙 1~5 (검증기가 확인하는 공정성) | **Client 검증 순서 4** |
| I 게이트 · 뒤집기 | 규칙 16 (*"해시가 같을 때만"* · *"어떤 실패에서도 전투 씬으로 전환하지 않는다"*) · 규칙 12 (Host 권위 seed) | — | 복구 상태 (진행 차단 부분만 — 표시는 §10) |

**이번 범위에서 근거로 삼지 않은 규칙**: 규칙 14(재경기 맵 선택) 전체 · `GameSystemRules_UI.md` 「공통 UI 규칙」 규칙 M-1~M-4 전체 · `GameSystemRules_Units.md` 규칙 45. 각각 §10·§11 로 넘긴다.

---

## 9. 위험 요소

**가. 🔴 I 단계 하나가 멀티 경로 전체를 갈아끼운다 — 되돌리기 비용이 가장 크다**
2단계의 H 단계와 같은 모양이지만, 그때와 달리 **이 환경에서 컴파일조차 확인할 수 없고 사용자 실기 2대가 유일한 관측 수단**이다.
→ **완화 ①**: 제거-1·제거-3 을 삭제가 아니라 **주석 비활성화**로 두어, 임시 고정 seed 경로를 되살리는 것을 **한 줄 토글**로 만든다(§1).
→ **완화 ②**: A~H 를 전부 「동작 무변경」으로 유지해 **I 이전 어느 커밋에서 멈춰도 게임이 지금과 똑같이 돈다**(§3).

**나. 조각이 실전에서 항상 1개라 재조립 로직이 죽은 코드가 된다**
실측상 payload 는 323~343바이트고 한도 설정은 6144다(Research §5). `TechnicalDesignDocument.md` 도 이미 같은 취지를 적었다.
→ **완화**: 재조립기를 `NetworkBehaviour` 밖 순수 C# 으로 분리해 **`mono` 로 조각 여러 개 입력을 실제로 실행**하고 그 수치를 §14 에 남긴다(§4-4). **실전에서 안 도는 것과 한 번도 실행된 적 없는 것은 다르다.**
⚠️ **인위적으로 조각 크기를 줄여 여러 조각을 만드는 「시험 전용 경로」는 넣지 않는다** — TDD 가 *"코드를 억지로 실행시키려고 조각 크기를 줄이는 방안도 폐기했다"* 고 이미 정했다. 필요하다고 판단되면 **별도 승인 사항**이다(Research §5-3 도 *"시험 전용 경로로 둘지 여부 자체가 기획 결정"* 이라 적었다).

**다. Client 복원 탐색이 실패하는 seed 가 있을 수 있다 — 잰 것은 40개뿐이다**
「시드 1~40 전부 성공」은 실측이지만 **40개는 전수가 아니다.** 실패하는 seed 가 하나라도 있으면 그 판은 **정상 맵인데도 준비 실패**가 된다.
→ **완화**: H 단계에서 **`mono` 로 훨씬 넓은 시드 구간을 돌려** 복원 성공률·평균 시도 수·최대 시도 수를 재측정하고 §14 에 적는다. 2단계 G 가 시드 2000개로 분포를 확인한 선례가 있다.
→ ⚠️ **실패 seed 가 나오면 그 자리에서 원인을 파악하기 전까지 I 로 넘어가지 않는다.** 「거의 다 되니까」로 넘기면 실기에서 **재현되지 않는 간헐 실패**가 된다.

**라. 「해시가 다를 때」를 실기에서 어떻게 재현하는가 — 관측 수단이 없다**
부 판정 2 는 **일부러 불일치를 만들어야** 확인된다. 그런데 정상 경로에서는 절대 일어나지 않는다.
→ **미결이며 추정하지 않는다.** 후보는 ① 임시 진단 코드로 Client 쪽 수신 바이트 1개를 뒤집어 보기(2단계 진단 로그처럼 **확인 후 전량 제거**) ② 아예 확인하지 않고 「코드 경로만 있다」로 남기기. **①은 코드에 임시 조작을 넣는 일이므로 사용자 승인 사항**이다 → G 착수 시 꺼낸다.
⚠️ **②를 고르면 부 판정 2 는 「⚠️ 실기 미검증」으로 남는다** — 완료로 적지 않는다.

**마. 로비 씬에는 `GameBootstrapper` 가 없다 — 준비를 누가 실행하는가**
맵 준비는 **로비에서** 끝나야 하는데(규칙 12·16) `GameBootstrapper` 는 전투 씬 쪽 조합 루트다.
→ **해법은 이미 손에 있다**: `MapPreparationUseCase` 는 순수 C# 이라 어디서든 `new` 할 수 있고, 그 유일한 의존 `IMapFallbackTemplateSource` 의 구현(`Infrastructure/Config/ResourcesMapFallbackTemplateSource.cs:47`)은 `Resources` 를 읽으므로 **씬과 무관하다.** 따라서 `NetworkMapTransfer` 가 직접 조립해 실행할 수 있다.
→ ⚠️ **그러나 「`GameBootstrapper` 가 유일한 조합 루트」라는 제약과 마찰이 있다.** 로비에는 그 루트가 없으므로 예외가 필요한지, 아니면 전송 객체가 로비 한정 조합 루트 역할을 겸하는 것으로 볼지 — **B 착수 시 사용자에게 확인한다**(CLAUDE.md 규칙 12).

**바. NGO RPC 실효 상한이 canonical 크기보다 작을 가능성**
가능성은 낮다(343바이트 대 설정 6144). 그러나 **재지 않은 값이다.**
→ **완화**: B 단계 실측을 **F 착수의 선행 조건**으로 둔다. 상한이 예상보다 작으면 조각 수가 1이 아니게 되고, 그때는 §9-나 의 「죽은 코드」 위험이 **저절로 사라진다**(나쁜 소식이 아니다).

**사. 규칙 14 의 「자동 시작 금지」가 이번에 손대지 않은 채 남는다**
현재 `NetworkGameEndController.cs:375` 는 양측 동시 요청 시 조건 개념 없이 곧바로 `StartRematch()` 한다(Research §3-1·§12-3).
→ **이번에 고치지 않는다**(재경기는 §10). **다만 §10 에 「조건을 도입하는 순간 이 분기를 반드시 함께 손봐야 한다」를 못 박아 둔다** — 안 그러면 조건이 다른데도 자동 시작되는 상태가 새로 생긴다.

---

## 10. 〈다음 범위〉 — 사용자가 이미 정해 둔 답을 함께 적는다 (다시 묻지 않기 위해)

> **아래는 이번 범위가 아니다.** 그러나 **사용자가 이미 답을 정해 둔 항목**이 섞여 있으므로, 다음 작업에서 같은 질문을 반복하지 않도록 여기에 그 답을 함께 남긴다.

| # | 항목 | 사용자가 정해 둔 답 | 남은 미정 |
|---|---|---|---|
| 1 | 재경기 `SameMap` / `NewMap` (규칙 14 전체) | — | 규칙 14 전부. **UI 자산이 아직 담지 못한다** — `GameEndUI` 버튼 2개(3개 필요) · `RematchRequestPopup` 텍스트 필드 없음(조건 표시 필요) · 싱글 재경기는 이미 `NewMap` 고정이고 `SameMap` 경로가 코드에 없다(Research §3-2·§3-3) |
| 2 | **재경기 새 맵 실패 시 표시** (규칙 M-3 · M-4 의 「팝업 여부 미정」) | ✅ **팝업을 띄운다. 규칙 8 분류로 「모달 (Modal)」.** | ⚠️ **모달이 떠 있는 동안 자동 로비 복귀 countdown 을 어떻게 할지는 미정이다** — 멈춤 / 계속 / 안 돌림 중 논의하다 사용자가 UI 전체를 뒤로 미뤘다. 규칙 M-3 은 *"자동 로비 복귀 countdown 을 전체 길이로 다시 시작한다"* 고만 정하고 모달과의 관계를 정하지 않는다. **전체 길이 = 30초**(`GameEndUI.cs:73` `_autoReturnSeconds = 30f` 실측) |
| 3 | **싱글 최초 실패의 표시 문구** (규칙 M-4 미정 3번) | ✅ **멀티와 동일 — `맵 준비에 실패했습니다`** | 없음 |
| 4 | **싱글 최초 실패의 loading UI 처리** (규칙 M-4 미정 4번) | ✅ **멀티처럼 닫는다** | 없음 |
| 5 | 싱글 최초 실패의 버튼 | ✅ **`Retry` / `Lobby`** (규칙 M-4 가 이미 정한 값 — 멀티의 `Retry`/`Leave Match` 와 **버튼만 다르다**) | 없음 |
| 6 | **맵 투영 실패(`MapProjectionFailed`) 시의 UI·진행** (Research §13-6 — 어느 규칙 문서에도 없다) | ✅ **준비 실패와 같게 취급한다.** 원인 구분은 **이미 나뉘어 있는 로그 키**(`MapPreparationFailed` vs `MapProjectionFailed`)로 한다 | 없음 |
| 7 | 멀티 최초 실패 팝업 · `Retry` 요청 RPC · idempotency | 규칙 16·M-2 가 이미 전부 확정(모달 · `맵 준비에 실패했습니다` · `Retry`/`Leave Match` · 중복 요청 한 번만 · **새 root seed 부터 재시작**) | 없음 — 구현만 남았다 |

🔴 **다음 범위 착수 시 반드시 함께 처리할 것 (이번에 고치지 않고 남기는 어긋남)**
- **규칙 14 의 「자동 시작 금지」와 현재 코드의 어긋남**(Research §12-3). `NetworkGameEndController.cs:375` 가 두 번째 요청을 받는 즉시 `StartRematch()` 한다. **`RematchMapMode` 를 도입하는 순간 이 분기를 함께 손보지 않으면 「조건이 다른데도 자동 시작」 상태가 새로 생긴다.**
- **규칙 14 문언의 미확정 하나**(Research §13-5): *"양측이 서로 다른 mode로 동시에 요청해도 자동 시작하지 않는다"* 가 **같은 mode 동시 요청에도 적용되는지** 문언으로 확정할 수 없다. **추정하지 말고 사용자에게 확인**한다(CLAUDE.md 규칙 12).
- `RematchRequestPopup` 에 **조건 표시용 텍스트 슬롯**이 없고 `GameEndUI` 에 **세 번째 버튼**이 없다 — 자산 작업이 함께 필요하다.

---

## 11. 사용자가 3단계 범위로 고르지 않은 것 (임의로 넣지 않는다 — 근거와 함께 남긴다)

> **아래 3건은 「미해결 결함」이 아니라 「사용자가 이번 범위로 고르지 않은 확정 항목」이다.** 다만 그중 하나는 **규칙이 미충족 상태로 남는다**는 사실을 반드시 함께 적어야 한다.

### 11-1. 🔴 초기 골드 적용 — **넣지 않으면 규칙 16 이 미충족으로 남는다**

**실기로 드러난 사실**(2단계 §14-5): 골드가 **5135** 인데 그 맵(중립 광산 3개)이 정한 값은 **500** 이고, 실제로 쓰인 것은 `GameConfig.StartingGold`(**5000**)다.
**코드 실측**(Research §10-2): `MapPreparationResult.InitialGold` 를 읽는 곳은 **로그 문자열 한 군데뿐**이고(`GameBootstrapper.Map.cs:573`), 실제 시작 골드는 여전히 `new ResourceUseCase(_config.StartingGold)`(`GameBootstrapper.Setup.cs:444`)다.

> 🔴 **규칙 16 은 *"Host가 확정한 … 실제 초기 골드를 canonical `MapDefinition`에 넣어 전송하고, 양쪽은 그 값을 그대로 적용한다"* 고 정한다. 즉 이 항목을 넣지 않으면 3단계가 끝나도 규칙 16 이 미충족 상태로 남는다.** 전송·해시·검증은 그 값을 **실어 나르기만 하고**, 「그대로 적용한다」는 뒷부분이 비어 있게 된다.

**그럼에도 이번 범위에 넣지 않는다** — 사용자가 범위를 「멀티에서 맵이 제대로 도는 것까지」로 확정했고, 이 항목은 **맵이 아니라 경제**를 건드리므로 실패 시 원인이 섞인다. **범위 확대는 사용자 결정 사항이다**(CLAUDE.md 규칙 6).

### 11-2. 경로 완전 차단 대응 (`GameSystemRules_Units.md` 규칙 45)

ROADMAP 3단계 행의 ③에 묶여 있으나 **맵 전송과 별개 작업**이다. 규칙 문서 자신이 *"그것은 맵의 성질이 아니라 유닛의 행동이므로 유닛 문서가 단일 소스다"* 라고 넘겼다(규칙 11). **Research 도 조사 범위에 넣지 않았다**(Research §2-3·§13-7) — 즉 **이 항목은 조사조차 되지 않은 상태**이므로, 넣기로 한다면 Research 부터 필요하다.

### 11-3. `TechnicalDesignDocument.md` 의 canonical payload 「약 274바이트」 정정

| 출처 | 값 |
|---|---:|
| TDD 전송 프로토콜 절 (2026-08-26 기록) | **약 274바이트** (*"타일 231 + 헤더 19 + 성·광산 24 + 장식 0"*) |
| **이번 실측** (Research §5-1) | **335~343바이트** (헤더 **40** + 타일 231 + 성 20 + 시작 광산 20 + 중립 광산 `4+4N` + 장식 4) |

**어긋난 이유 두 가지**(코드 대조로 특정): ① 헤더가 19가 아니라 **40바이트**다(`int32` 8개 + `uint64` 1개) ② **4개 목록의 「개수」 필드 4×4 = 16바이트가 누락**됐다.
그 문서 자신이 *"파이썬 계산이며 Unity·NGO 실기 측정이 아니다"* 라고 밝히므로 **거짓 주장이 아니라 계산 착오**이며, ⚠️ **그 수치가 근거로 쓰인 「조각이 1개뿐」이라는 결론은 실측값 343 으로도 그대로 성립한다**(한도 6144 의 5.6%). **결론은 유지되고 근거 수치만 정정 대상이다.**

⚠️ **다만 B 단계에서 NGO 실효 상한·Relay MTU 를 그 문서의 **같은 절**에 기록하게 되므로, 그때 이 수치도 함께 정정할지 사용자에게 확인할 가치가 있다** — 같은 절을 두 번 여는 일을 피할 수 있다. **지금은 정정하지 않는다.**

---

## 12. Research 의 미확인 9건 · 어긋남 4건을 어떻게 처리하는가

### 12-1. Research §13 미확인 9건

| # | 항목 | 이번 처리 |
|---|---|---|
| 1 | NGO 동적 스폰 오브젝트의 씬 재로드 생존 조건 | ✅ **A 단계로 흡수** — 계획의 첫 단계 |
| 2 | NGO RPC 실효 페이로드 상한 · Relay 실효 MTU | ✅ **B 단계 실측 항목으로 흡수.** F 착수의 선행 조건 |
| 3 | `constraints` 가 정말 `AttemptIndex` 에 의존하는가 (생성기 5종 본문 미확인) | ✅ **답이 필요 없어졌다** — D 방식 실측이 **시드 1~40 전부에서 복원에 성공**했으므로, 「5종 중 몇 개가 의존하는가」와 무관하게 **복원 절차가 전부에 대해 성립**한다. H 단계의 대량 시드 실행이 이것을 더 넓게 확인한다 |
| 4 | 인터페이스 역전이 비동기 왕복에도 성립하는가 | ✅ **문제가 성립하지 않는 자리로 옮겼다**(§5-3) — 왕복을 Application 이 소유하지 않으므로 역전할 것이 없다. ⚠️ **원래 질문 자체는 여전히 미확인**이며, 다른 비동기 기능에서 다시 나올 수 있다 |
| 5 | 규칙 14 의 「같은 mode 동시 요청」 해석 | ⏸ **§10 으로 이월** — 재경기가 다음 범위다. **사용자 확인 필요** |
| 6 | 투영 실패 시 UI·진행 규정 | ✅ **사용자가 확정**: 준비 실패와 같게 취급, 원인 구분은 로그 키로(§10-6). **표현은 §10** |
| 7 | 경로 완전 차단 대응(규칙 45) | ⏸ **§11-2** — 범위 밖 확정 |
| 8 | `NetworkGameManager` 959행 전문 | ✅ **불필요해졌다** — 후보 C 를 채택하지 않으므로 전문 통독이 필요 없다(§4-1). I 단계에서 손대는 것은 **위임 진입점 하나**뿐이다 |
| 9 | 2단계 결과의 멀티플레이 실기 동작 | ✅ **이미 해소됐다** — 2026-09-09 에 수행됐고 결과는 2단계 `Plan.md` §14 에 있다(고정 seed `1` → `11` 2회차). **3단계 착수 전 조건이던 항목이므로 여기서 닫는다** |

### 12-2. Research §12 어긋남 4건 — 이번에 고칠 것과 안 고칠 것

| # | 어긋남 | 이번에 고치는가 |
|---|---|---|
| 12-1 | TDD canonical 크기 「약 274바이트」 vs 실측 335~343 | ❌ **고치지 않는다** — §11-3 (B 단계에서 같은 절을 열 때 함께 할지 사용자 확인) |
| 12-2 | 규칙 16 전달 항목 vs package 정의 (`StartingMineSide` 미전달) | ✅ **이번에 해소된다** — D 방식이 복원 탐색으로 값을 유도한다(§6-6). **문서를 고치는 것이 아니라 구현이 요구를 만족시키는 형태**다 |
| 12-3 | 규칙 14 「자동 시작 금지」 vs 현재 코드 | ❌ **고치지 않는다** — 재경기가 §10. **다만 §10 에 「조건 도입 시 반드시 함께」를 못 박았다** |
| 12-4 | `GameBootstrapper.Map.cs:453~454` 주석이 사실이 아니다 | ✅ **이번에 고친다** — C 단계·제거-4. ⚠️ **Research 는 「앞선 주석」이라 했으나 이 설계에서는 앞서지도 않는다**(§5-3) |

---

## 13. 변경·신설 파일 목록 (예상)

⚠️ **「예상」이다.** 실제 목록은 각 단계 실행 후 §14 에 실측으로 적는다(`WORKFLOW.md` [12]).

**신설**
```
Assets/_Project/Scripts/Infrastructure/Network/NetworkMapTransfer.cs        (B — NetworkBehaviour)
Assets/_Project/Scripts/Infrastructure/Network/MapChunkAssembler.cs         (B — 순수 C#, mono 검증 대상)
Assets/_Project/Scripts/Application/MapHandoff.cs                           (C — 순수 C# 정적 홀더)
Assets/_Project/Scripts/Application/UseCases/MapVerificationUseCase.cs      (H — 순수 C#, mono 검증 대상)
Assets/_Project/Prefabs/Network/NetworkMapTransfer.prefab                   (B)
```

**수정**
```
Assets/_Project/Scripts/Bootstrap/GameBootstrapper.Map.cs        (C 주석 정정 · D 준비/투영 분리 · I 제거-1)
Assets/_Project/Scripts/Bootstrap/GameBootstrapper.cs            (I 제거-1 — 상수 NetworkInterimRootSeed)
Assets/_Project/Scripts/Bootstrap/GameBootstrapper.Network.cs    (I — Client 투영 경로)
Assets/_Project/Scripts/Infrastructure/Network/NetworkGameManager.cs  (I — 위임 진입점 1개. 🔴 NetworkBehaviour 승격 아님)
Assets/_Project/Scripts/Presentation/UI/ViewModels/BattleViewModel.cs (I 제거-3 — 호출 대상 1줄)
Assets/_Project/Scripts/Application/Interfaces/ILogSink.cs       (E — LogEvent 키 5개 추가 예상)
Assets/_Project/Resources/DefaultNetworkPrefabs.asset            (B — 프리팹 등재)
Assets/_Project/Docs/LogRules.md                                 (E — 1.5 신설 블록)
Assets/_Project/Docs/TechnicalDesignDocument.md                  (B — NGO 실측값과 근거 기록. 규칙 16 지시)
```

**손대지 않는 것 (명시)**
```
Assets/_Project/Docs/GameSystemRules/**                  ← 규칙 문서는 이번에 한 줄도 고치지 않는다
Assets/_Project/Scripts/Application/Interfaces/IGameServices.cs   ← 확장하지 않는다 (§5-3)
Assets/_Project/Scripts/Domain/Map/**                    ← 코덱·해시·검증기는 이미 다 있다 (Research §6-1)
Assets/_Project/Resources/MapTemplates/*.bytes           ← 형식 무변경이므로 재생성 불필요 (§6-4)
Assets/_Project/Scripts/Infrastructure/Network/NetworkGameEndController.cs  ← 재경기는 §10
```

---

## 14. 실행 기록

| 단계 | 상태 | 커밋 | 비고 |
|---|---|---|---|
| A NGO 씬 재로드 생존 조건 확인 | ⬜ 미착수 | — | 🔴 **착수하지 않았고, 「차단 요인」에서 「관찰 항목」으로 강등됐다.** 이번 범위에는 **생존 조건이 걸리지 않는다** — 전송이 **씬 로드가 시작되기 전 로비에서 전부 끝나고**, 확정된 맵은 객체가 아니라 `MapHandoff`(정적 홀더)로 씬을 건넌다. 생존이 실제로 물리는 곳은 **재경기(§10 표 1번 · 다음 범위)** 이며, 그것이 이번에 실기로 드러났다(아래 §16-가). 상세는 아래 「A 단계 상세」 |
| B `NetworkMapTransfer` 골격 + 한도 실측 | ✅ 구현 완료 · **실기 검증 완료(사용자 테스트)** · ⚠️ **부분 검증 — 한도 실측 2건이 남았다** | `df392da` | 골격·재조립기는 실기 2판에서 실제로 돌았다(`ChunkCount=1` · 재조립 성공). 🔴 **그러나 이 단계가 스스로 「실측 항목」으로 내건 두 숫자는 아직 재지 않았다** — 조각 크기는 `IsChunkSizeMeasured = false` · `ProvisionalChunkSizeBytes = 1024`(문서 작업 중 코드 직접 실측). `MapChunkAssembler` 는 **`mono` 로 6가지 입력 79 단언 전원 PASS**. 상세는 아래 「B 단계 상세」 |
| C `MapHandoff` + 낡은 주석 정정 | ✅ 구현 완료 (**mono 로 실제 컴파일·실행 검증**) · **실기 검증 완료(사용자 테스트)** | `771e5e4` | 로비→전투 씬 인계가 실기 2판에서 실제로 성립했다. 🔴 **「읽고 비운다」도 실기로 드러났다** — 재경기에서 `Reason=MapHandoffEmpty` 가 남았다(§16-가). 상세는 아래 「C 단계 상세」 |
| D 준비/투영 분리 | ✅ 구현 완료 · **실기 검증 완료(사용자 테스트)** | `771e5e4` | **성공 기준이 「아무것도 달라지지 않는 것」**이었고 그대로였다 — 싱글 3판이 각기 다른 seed 로 정상 동작(`20:00:04`·`20:00:07`·`20:00:10`). 상세는 아래 「D 단계 상세」 |
| E 로그 키 신설 | ✅ 구현 완료 (**mcs 로 개수 실측**) · ⚠️ **부분 검증 — 실기에서 발화한 것은 5종 중 1종뿐** | `df392da` | `LogEvent` **41 → 46개**(문서 작업 중 파이썬 파싱으로 직접 실측). 🔴 **나머지 4종이 안 찍힌 것은 검증 누락이 아니다** — 전부 실패 키인데 실기 2판이 모두 성공이었다. 상세는 아래 「E 단계 상세」 |
| F Host 권위 준비·전송 | ✅ 구현 완료 · **실기 검증 완료(사용자 테스트)** | `1ffe0f2` | 2판 모두 `SendCount=1` · **`ResendCount=0`** · 준비 `AttemptCount=1`(27ms · 1ms). ⚠️ **재전송·timeout 경로는 실기에서 한 번도 발화하지 않았다.** 상세는 아래 「F 단계 상세」 |
| G Client 수신·해시 대조 | ✅ 구현 완료 · **실기 검증 완료(사용자 테스트)** · ⚠️ **불일치 경로 미발화** | `1ffe0f2` | 2판 모두 `HashMatch=True`. **인위적 불일치 시험(§15-3)은 하지 않았다** — 사용자가 임시 조작 코드를 넣을지 확정하지 않았다. 상세는 아래 「G 단계 상세」 |
| H Client D 방식 재생성 검증 | ✅ 구현 완료 (**mono 로 실제 컴파일·실행 검증**) · **실기 검증 완료(사용자 테스트)** | `1ffe0f2` | 실기에서 Client 가 `AttemptIndex=0, SearchedCombinations=2, VerifyStage=None` 으로 통과. **시드 1~5000 재측정에서 복원 100.00%**(2단계의 시드 40개 기록을 **125배 넓게** 다시 잰 값 — 옛 기록은 지우지 않는다). 상세는 아래 「H 단계 상세」 |
| I 씬 전환 게이트 + 뒤집기 | ✅ 구현 완료 · **실기 검증 완료(사용자 테스트)** · ⚠️ **게이트가 막는 경로는 실기 미발화** | `cc3a14b` | 🔴 **여기서 처음 동작이 뒤집혔다.** 매 판 다른 seed 가 나왔고(`3905793782056422338` · `10943744334431763949`) 게이트를 통과했으며 **싱글 회귀도 없다.** ⚠️ **「실패하면 로비에 머무는가」는 성공만 나서 확인되지 않았다.** 상세는 아래 「I 단계 상세」 |

**상태 라벨**: ⬜ 미착수 / 🔄 진행 중 / ✅ 구현 완료 (컴파일 미검증) / ✅ 구현 완료 (**mono 로 실제 컴파일·실행 검증**) / ✅ 구현 완료 · **실기 검증 완료(사용자 테스트)** / ⚠️ 부분 검증 — 무엇이 남았는지 함께 적는다

> **각 단계 완료 시 이 표의 행을 채우고, 상세는 `### <단계> 단계 상세 (날짜)` 로 이 문서 끝에 append 한다.** 2단계 Plan 과 같은 형태다.
> **계획과 실행 순서가 달라지면 표의 알파벳 순서는 그대로 두고 비고에 「실행 순서를 바꿨다」와 그 이유를 적는다**(2단계가 I↔J 를 그렇게 처리했다).

---

## 15. 이 계획이 확정하지 않은 것 (착수 시 사용자 확인이 필요한 자리)

**추정으로 채우지 않았다**(CLAUDE.md 규칙 10·12). 아래는 **해당 단계에 착수할 때 꺼낸다.**

| # | 무엇 | 언제 | 왜 지금 못 정하나 |
|---|---|---|---|
| 1 | A 가 「생존하지 않는다」로 나왔을 때 **대안 A′ 가 규칙 16 을 만족한다고 볼 것인가** | A 완료 직후 | 규칙 문언이 「객체가 살아 있어야」인지 「경로가 씬에 묶이지 않아야」인지 문맥으로 확정 불가 (§4-3) |
| 2 | **로비에서 `MapPreparationUseCase` 를 조립·실행하는 것이 「`GameBootstrapper` 가 유일한 조합 루트」와 충돌하는가** | B 착수 시 | 로비에 그 루트가 없다. 예외를 두는 것인지 전송 객체가 로비 한정 루트를 겸하는 것인지 (§9-마) |
| 3 | **해시 불일치를 실기에서 재현하기 위해 임시 조작 코드를 넣을 것인가** | G 착수 시 | 코드에 임시 조작을 넣는 일이며, 안 넣으면 부 판정 2 가 「실기 미검증」으로 남는다 (§9-라) |
| 4 | **TDD 「약 274바이트」를 B 단계에서 함께 정정할 것인가** | B 완료 시 | 같은 절을 여는 김에 할지, 별도 작업으로 둘지 (§11-3) |
| 5 | **`LogEvent` 신설 키 5개의 이름·개수** | E 착수 시 | §7-E 의 표는 「안」이다. F~I 구현에서 실제 갈래가 달라질 수 있다 |

---

> **이 계획은 사용자 승인 전까지 코드를 한 줄도 바꾸지 않는다**(CLAUDE.md 규칙 1·2·11).
> 승인 후에도 **A 단계 결과가 나오기 전에는 B 에 착수하지 않는다**(§4-2).

---

### A 단계 상세 (2026-09-14) — 착수하지 않았다 · 차단 요인에서 관찰 항목으로 강등

**한 줄로**: **검증 누락이 아니라 이번 범위에 조건이 걸리지 않은 것**이다. A 는 *"동적 스폰 `NetworkObject` 가 `LoadSceneMode.Single` 재로드를 넘어 생존하는가"* 를 묻는 단계였고, §4-2 는 이것을 **후보 A 의 전제**이자 B 착수의 선행 조건으로 두었다. 실제로 만들어 놓고 보니 **그 전제가 이번 범위에서는 쓰이지 않는다.**

**강등 근거 (설계가 그렇게 굳었다)**
- 전송은 **씬 로드가 시작되기 전에 로비에서 전부 끝난다.** Host 가 로비에서 준비 → 조각 전송 → `MapReady` → 해시 대조 → **그 뒤에야** `NetworkGameManager.LoadGameScene()` 이 불린다(I 단계 게이트). 즉 **전송 객체가 씬 전환을 넘어 살아 있을 필요가 없다.**
- 확정된 맵이 씬을 건너는 통로는 **객체가 아니라 `MapHandoff`(Application 정적 홀더)** 다(C 단계). 정적 홀더는 씬 수명과 무관하다.
- 따라서 A 가 「생존하지 않는다」로 나왔더라도 §4-3 의 두 갈래(대안 A′ 채택 여부)를 꺼낼 일이 없었다. **§15 표 1번(A 결과에 따른 사용자 확인)도 함께 불발이다 — 물을 일이 생기지 않았다.**

🔴 **그러나 이 질문이 사라진 것은 아니다 — 생존이 실제로 물리는 곳은 재경기다.** 재경기는 **로비로 돌아가지 않고 Game 씬만 재로드**하며 그 과정에서 `NetworkMapTransfer` 를 despawn 한다(§16-가에 실기 로그 지문이 있다). 재경기에서 맵을 다시 구성하려면 **그 재로드를 넘어 전송 경로가 살아 있어야 하거나, 살아 있지 않아도 되도록 흐름을 바꿔야** 한다. **§10 표 1번(재경기 `SameMap`/`NewMap`)에 착수할 때 A 를 그때 실측한다.**

**남은 것**: 프로브 프리팹은 만들지 않았으므로 「B 착수 시 제거」(계획의 ⚠️) 할 대상도 없다.

---

### B 단계 상세 (2026-09-14) — 전송 골격과 재조립기 (커밋 `df392da`, E 와 같은 커밋)

**신설 2파일 + 프리팹 1개.**

| 파일 | 성격 | 이번 회차 검증 수단 |
|---|---|---|
| `Infrastructure/Network/MapChunkAssembler.cs` (**369행** — 문서 작업 중 `wc -l` 실측) | 🔴 **일부러 순수 C#** (`using System;` 하나뿐) | **`mono` 로 실제 실행** |
| `Infrastructure/Network/NetworkMapTransfer.cs` (**최종 1607행** — 문서 작업 중 `wc -l` 실측. B 시점에는 골격뿐이고 F~I 에서 알맹이가 붙었다) | `NetworkBehaviour` | **헤드리스 컴파일 불가 — Unity·실기에서만** |
| `Prefabs/Misc/NetworkMapTransfer.prefab` + `Resources/Config/DefaultNetworkPrefabs.asset` 등재 | Unity 에셋 | **실기 스폰 로그로 확인** |

**✅ `MapChunkAssembler` — `mono` 실행 결과**: 6가지 입력(조각 1개 / 여러 개 / 중복 / 역순 / 하나 빠짐 / 길이 불일치)에 경계값을 더해 **79 단언 전원 PASS**. 🔴 **그리고 모든 `Accept` 직전마다 `TryGetAssembled` 를 불러 「불완전 상태에서 바이트가 나오는 일」이 0건임을 확인**했다 — 규칙 16 의 *"부분 데이터는 어떤 검증이나 맵 구성에도 쓰지 않는다"* 를 **API 모양이 아니라 실행으로** 확인한 것이다.

**✅ 실기**: 2판 모두 `ChunkCount=1` 로 보내고 Client 가 재조립에 성공했다. **로비 스폰도 성공**했다 — `맵 전송 객체 스폰 완료 | Role=Host/Client, NetworkObjectId=1`.

> 🔴 **이 프로젝트에서 로비 씬에 네트워크 오브젝트가 존재한 것은 이번이 처음이다.** `Lobby.unity` 의 `NetworkObject` 개수는 종전 **0개**였고, 기존 `NetworkBehaviour` **14개는 전부 Game 씬**이었다(메인 세션 실측 — Research §14-5 가 이 사실로 후보를 골랐다). 그래서 **「로비에서 NGO 가 실제로 도는가」 자체가 이번에 처음 확인된 것**이며, 그 확인이 실기 스폰 로그다.

🔴 **⚠️ 이 단계가 스스로 내건 「실측 항목」 2건은 아직 재지 않았다 — 완료로 적으면 거짓이 된다.**

| 못 잰 숫자 | 지금 코드에 있는 값 | 왜 못 쟀나 |
|---|---|---|
| ① NGO RPC 한 번의 **실효** 페이로드 상한 | — (씬의 `UnityTransport.m_MaxPayloadSize` = 6144 는 **설정값이지 실효 상한이 아니다**) | 사용자 실기 2대로만 잴 수 있다 |
| ② **Relay 경유** 실효 MTU | — (로컬 127.0.0.1 값을 Relay 값으로 삼을 수 없다) | 같음 |
| 그래서 조각 크기가 | `IsChunkSizeMeasured = false` · `ProvisionalChunkSizeBytes = 1024` (**근거 없음을 주석에 명시**) | 위 두 값이 있어야 확정된다 |

- 위 세 줄은 **문서 작업 중 `NetworkMapTransfer.cs` 를 직접 읽어 실측한 값**이다.
- 재는 수단은 코드에 이미 있다 — `RunTransferProbe(probeBytes, chunkSize)` + 인스펙터 컨텍스트 메뉴. **실전과 같은 RPC 경로**로 더미 바이트를 보낸다.
- 🔴 **규칙 16 이 *"값은 구현 시 NGO 실측으로 확정하고 근거와 함께 TDD 에 기록한다"* 고 지시하므로, 이 2건이 남아 있는 한 그 조항은 미충족이다.** 그래서 §16-바에 보류 항목으로 세웠다.
- ⚠️ **실전에서 조각이 항상 1개인 것과는 별개의 문제다.** 이번 실측으로 바이트 수 공식이 확인됐지만(아래 F 단계 상세), 그것은 *"어떤 잠정값을 넣어도 한 조각에 들어간다"* 를 말할 뿐 **잠정값을 확정값으로 만들어 주지는 않는다.**

`TransferTimeoutSeconds = 10` · `MaxResendCount = 1` 은 **규칙 16 이 정한 값이라 잠정이 아니다.**

---

### C 단계 상세 (2026-09-14 기록 · 커밋 `771e5e4`, D 와 같은 커밋) — 확정 맵 인계 통로

**신설**: `Application/MapHandoff.cs` (**99행** — 문서 작업 중 실측). 순수 C# 정적 홀더, 멤버 `Set` / `TryTake` / `Clear` 세 개.

- **왜 홀더인가**: 맵이 **확정되는 곳**(로비)과 **격자에 새겨지는 곳**(전투 씬) 사이에 `LoadSceneMode.Single` 재로드가 있다. 그 사이를 건너는 유일한 통로다 — **그리고 이것이 A 단계를 불필요하게 만든 설계이기도 하다**(위 A 단계 상세).
- **`TryTake` 는 「읽고 비운다」**. 남겨 두면 다음 판에 지난 판 맵이 조용히 재사용되고, 그것이 규칙 16 의 「로비에서 확정한 맵 데이터만」을 어기는 가장 조용한 방식이기 때문이다.
- **낡은 주석 정정(제거-4)**: `GameBootstrapper.Map.cs` 의 「다른 코드가 `GetLastMapPreparation()` 으로 다시 읽는다」를 교체했다. 실측상 `GetLastMapPreparation` 은 **선언 1건 · 호출자 0건**이고 3단계가 끝난 지금도 0건이다.
- **`IGameServices` 는 손대지 않았다**(§5-3 대로).

**✅ `mono` 실행**: 초기 false / `Set`→1회째 true / **2회째 false(읽고 비운다)** / `Clear` 후 false / `Clear` 뒤 재`Set` 정상 = **5개 단정 전원 PASS**.

**✅ 실기**: 2판 모두 로비에서 확정된 맵이 전투 씬에 그대로 인계돼 경기가 정상 동작했다.
🔴 **「읽고 비운다」가 실기에서도 실제로 그렇게 동작한다는 직접 증거가 나왔다** — 재경기에서 `Reason=MapHandoffEmpty` 가 남았다. **이것은 결함이 아니라 이 설계가 의도한 동작이다**(§16-가).

**폐기(Clear) 배선**은 I 단계에서 붙었다 — `MapHandoff.Clear()` 호출자 **0건 → 2건**(`NetworkGameManager.DisconnectAsync` · `BackToLobby`). 규칙 14 의 *"로비 복귀 또는 연결 종료 시 폐기"*.

---

### D 단계 상세 (2026-09-14 기록 · 커밋 `771e5e4`) — 준비/투영 분리 (동작 무변경 리팩터)

`GameBootstrapper.Map.cs` 한 파일. `PrepareAndProjectMap()` 을 **셋으로 갈랐고 문장 순서는 그대로**다.

| 함수 | 하는 일 |
|---|---|
| `PrepareAndProjectMap()` (기존 이름 · 래퍼) | `_mapPreparation`/`_mapProjection` 비우기 → 가드 → `PrepareMap()` → 실패면 return → `ProjectMap()` |
| `PrepareMap()` → `MapPreparationResult` | seed 생성 · 준비 · 보관 · 규칙 12 로그 1줄. 실패면 null 반환 |
| `ProjectMap(MapPreparationResult)` | `MapProjectionUseCase.Project` · 보관 · 실패 로그 |

🔴 **가르면서 반드시 지킨 것 3가지** (다음에 이 자리를 만질 때 되풀이해서 걸릴 지점)
1. **리셋 두 줄은 래퍼에 남는다.** `_mapProjection` 비우기를 `ProjectMap` 안으로 옮기면 **준비 실패로 일찍 돌아간 판에서 실행되지 않아** 지난 판의 투영 결과가 남고 성·시작 채굴소가 옛 자리에 배치된다.
2. **가드도 래퍼에 남는다.** `_config` 만 보고 `PrepareMap` 을 먼저 부르면 `_grid == null` 인 판에서 **원래는 안 나가던 준비 로그가 새로 한 줄 나간다** — 규칙 12 의 「경기당 1줄」 전제가 깨진다.
3. `ProjectMap` 의 인자는 `MapDefinition` 이 아니라 **`MapPreparationResult`** 다. 투영 실패 로그가 `MapVersion`/`Seed`/`Hash` 를 쓰고, `MapHandoff` 가 넘겨주는 타입과도 같아 I 에서 그대로 이어진다.

**이 단계가 하지 않은 것(계획대로)**: 임시 고정 seed 분기는 **그대로 살려 두었다.** 뒤집은 것은 I 다.

**✅ 실기 — 싱글 회귀**: `20:00:04` · `20:00:07` · `20:00:10` 세 판이 **각각 다른 seed** 로 정상 동작했다(유형 FullyOpen / ThreeLane / Outer). **리팩터의 성공 기준은 「아무것도 달라지지 않는 것」이었고 그대로였다.**
⚠️ **컴파일 검증은 불가**했다 — `GameBootstrapper.Map.cs` 는 `UnityEngine` 의존이라 이 환경에서 빌드할 수 없다. 확인한 것은 중괄호 균형 · 문장 순서 대조뿐이며, **컴파일 통과는 Unity 에서만 확인된다.**

---

### E 단계 상세 (2026-09-14) — 로그 키 5종 (커밋 `df392da`, B 와 같은 커밋)

**`LogEvent` 멤버 41개 → 46개.** 문서 작업 중 `Application/Interfaces/ILogSink.cs` 의 enum 본문을 파이썬으로 파싱해 **직접 실측**했고, 마지막 5개가 아래 5종임을 확인했다. **키 개수와 이름은 §7-E 의 「안」 그대로 확정됐다 — 구현하며 갈래가 달라지지 않았다**(계획의 ⚠️ 가 요구한 「조정했다면 적는다」에 해당할 조정이 없었다).

| 키 | 축 A | 축 B | 실기 2판에서 발화했는가 |
|---|---|---|---|
| `MapTransferSucceeded` | Info | 운영 | ✅ **2회** |
| `MapTransferRetried` | **Warn** | 운영 | ❌ (`ResendCount=0`) |
| `MapTransferFailed` | **Error** | 운영 | ❌ |
| `MapHashMismatch` | **Error** | 운영 | ❌ (`HashMatch=True`) |
| `MapClientVerificationFailed` | **Error** | 운영 | ❌ |

🔴 **4종이 안 찍힌 것은 검증 누락이 아니다.** 넷 다 **실패 키**인데 실기 2판이 **모두 성공**이었다. 발화시키려면 인위적 불일치·전송 실패를 만들어야 하고 그것은 §15-3(사용자 미확정)에 걸려 있다.

**배타성 확인(계획의 검증 ⓑ)**: 결말 4개(`Succeeded`/`Failed`/`HashMismatch`/`ClientVerificationFailed`)는 **서로 배타적**이고 `MapTransferRetried` 만 **중간 전이**다. 따라서 **한 판에 최대 2줄**이며, 「한 사건에 두 줄」(`LogRules.md` 1.14 금지 8)에 걸리지 않는다. 코드 쪽 강제는 `_outcomeLogged` 깃발이 맡고, 두 번째 결말은 **개발 축 Warn 으로만** 남긴다(조용히 삼키면 「결말이 두 번 났다」는 버그를 못 찾는다).

**문서**: `LogRules.md` **1.5** 에 개정 블록을 추가했다 — `> **맵 전송 키 5종 신설 — 멤버 ~~41개~~ → 46개 (2026-09-14 · 3단계 E)**`. 🔴 **커밋 전 작업 트리라 해시가 없어 단계 표기로 대신했다**(규칙 5 로 git 을 실행할 수 없고, 규칙 10 상 해시를 지어낼 수 없다).

---

### F 단계 상세 (2026-09-14) — Host 권위 준비·전송 (커밋 `1ffe0f2`, G·H 와 같은 커밋)

**Host 흐름**: 로비에서 `BeginHostMapTransfer(rootSeed, mapTestModeEnabled)` → `MapPreparationUseCase` 실행 → 준비 결과에 **이미 들어 있는** `CanonicalBytes`/`Hash` 를 그대로 package 로 → `MapPrepareBegin` → `MapChunk` × N → `MapReady` 대기.
🔴 **여기서 바이트를 새로 만들거나 해시를 다시 계산하지 않는다.** 두 번 계산하는 순간 「어느 쪽이 진짜인가」가 생기고 해시 대조가 의미를 잃는다.

**🔴 실기 실측 (2026-09-14, 에디터 1 + 실기기 1 · 최초 경기 2판 모두 PASS)**

| | 1판 (19:59:16) | 2판 (19:59:34) |
|---|---|---|
| Seed | `3905793782056422338` | `10943744334431763949` |
| 맵 유형 / 중립 광산 | FullyOpen / 3 | ObstacleOpen / 2 |
| TotalBytes | **331** | **327** |
| ChunkCount | **1** | **1** |
| HashMatch | **True** | **True** |
| SendCount / ResendCount | 1 / **0** | 1 / **0** |
| 준비 시간 (AttemptCount) | 27ms (1) | 1ms (1) |
| 씬 전환 게이트 | 통과 | 통과 |

🔴 **바이트 수 공식이 실측으로 확인됐다 — `319 + 4 × (중립 광산 수)`.** 광산 3개 → 331, 2개 → 327. 장식은 0개다. 이것은 Research §5-1 이 코덱 필드 순서로 유도한 공식 `319 + 4N + 20D` 와 **일치**하며, **이번에 처음으로 실기 전송 바이트로 확인**됐다(종전에는 `.bytes` 폴백 템플릿 파일 크기로만 확인됐다).
- 따라서 **조각은 항상 1개**다(`ProvisionalChunkSizeBytes = 1024`). ⚠️ **그렇다고 조각 크기가 확정된 것은 아니다** — 위 B 단계 상세 참조.

⚠️ **실기에서 발화하지 않은 경로**: timeout 10초 감시 · 1회 재전송 · 즉시 실패 6종. **2판 모두 첫 전송에 성공했기 때문이며, 코드가 없다는 뜻이 아니다.**

🔴 **함께 드러난 아키텍처 마찰**: `NetworkMapTransfer` 가 `MapPreparationUseCase` 를 **스스로 조립한다**(로비에 `GameBootstrapper` 가 없다). 이것이 §15 표 2번의 그 질문이며 **아직 사용자 확인 전**이다. 이번에 같은 부류가 하나 더 생겼다 — §16-라-3.

---

### G 단계 상세 (2026-09-14) — Client 수신·재조립·해시 대조 (커밋 `1ffe0f2`)

**`VerifyAndAnswer` 의 순서가 계약이다** — **해시 대조 → (프로브면 여기서 끝) → 역직렬화 → 헤더/본문 형식 버전 대조 → D 방식 검증**.
🔴 **대조는 원본 32바이트로 한다.** 로그용 16자 문자열로 비교하면 앞 8바이트만 같아도 통과한다 — 2단계 K 가 코드 주석에 못 박아 둔 결정을 그대로 이어받았다.
🔴 **Host 는 Client 의 「성공」 신고를 그대로 믿지 않는다.** `MapReadyServerRpc` 가 받은 `clientHash` 를 `_hostHash` 와 **한 번 더** 대조한다. 안 그러면 「Host/Client 해시 비교」가 Client 의 자기 신고가 된다.

**✅ 실기**: 2판 모두 `HashMatch=True`.
⚠️ **인위적 불일치 시험은 하지 않았다.** §15 표 3번(임시 조작 코드를 넣을 것인가)이 **사용자 미확정**이라, §2-1 의 부 판정 중 「불일치 시 씬이 안 넘어가는가」는 **실기 미검증으로 남는다.** **검증 누락이 아니라 확인을 받지 못해 시험 자체를 만들지 않은 것**이다.

---

### H 단계 상세 (2026-09-14) — Client D 방식 재생성 검증 (커밋 `1ffe0f2`)

**신설**: `Application/UseCases/MapVerificationUseCase.cs` (**583행** — 문서 작업 중 실측). 순수 C#. `Verify(payload)` 가 `Decode` 까지 **전부** 한다 — 호출부에서 따로 `Decode` 하지 않는 이유는 두 번 해석하면 「검증한 정의」와 「실제로 쓰는 정의」가 다른 객체가 되어 어긋날 길이 생기기 때문이다.

**탐색 상한**: `AttemptIndex` 0~99 × `StartingMineSide` A/B = **최대 200조합**(코드 실측 — `MaxSearchCombinationCount`). 넘으면 실패이고 **재전송하지 않는다**(규칙 16 — 공정성 검증 실패는 즉시 실패).

**🔴 `mono` 대량 재측정 (시드 1~5000)**

| 항목 | 값 |
|---|---|
| 준비 성공 | **5000 / 5000** (폴백 0건) |
| **D 방식 복원 성공** | **5000 / 5000 = 100.00%** |
| 평균 생성 시도 | **1.51회** (최대 3) |
| 탐색 조합 | 평균 **1.51개** (최대 3 / 상한 200) |
| 복원값 불일치 | seed · 유형 · 광산 수 · 시작 광산 쪽 **전부 0건** |
| 총 소요 | 1.7초 |

> 🔴 **이것은 2단계 §6-3 의 「시드 1~40 · 평균 1.4회」를 125배 넓게 다시 잰 값이다.** 그 기록은 **지우지 않는다** — 40개 시점에는 그것이 실측이었다. 두 값은 모순이 아니라 표본 크기가 다른 두 측정이다.

**✅ 실기**: Client 쪽(실기기 logcat)에서 **D 방식 재생성 검증 통과**를 확인했다 — `AttemptIndex=0, SearchedCombinations=2, VerifyStage=None, VerifyError=None`.

**부수 효과(§6-6 대로)**: `StartingMineSide` 가 package 에 실려 오지 않아도 **복원 탐색이 값을 유도**하므로, Research §12-2 의 어긋남(규칙 16 전달 항목 vs package 정의)이 **문서를 고치는 것이 아니라 구현이 요구를 만족시키는 형태**로 해소됐다.

---

### I 단계 상세 (2026-09-14) — 씬 전환 게이트 + 고정 seed 해제 (커밋 `cc3a14b`) 🔴 여기서 처음 동작이 뒤집혔다

**바뀐 파일**: `Domain/Map/MapRootSeed.cs`(신설 · **86행** 실측) · `GameBootstrapper.cs` · `GameBootstrapper.Map.cs` · `NetworkGameManager.cs` · `NetworkMapTransfer.cs` · `BattleViewModel.cs`.

**게이트의 모양**
```
BattleViewModel.OnClientConnected (2명)
  └─ NetworkGameManager.BeginMapTransferAndLoadGameScene()   ← 종전엔 여기서 LoadGameScene() 직행
       ├─ 프리팹 동적 Spawn → NetworkMapTransfer
       ├─ transfer.OnHostTransferSucceeded → NetworkGameManager.LoadGameScene()
       ├─ transfer.OnHostTransferFailed    → OnMapTransferFailed 발행(로비 유지)
       └─ transfer.BeginHostMapTransfer(MapRootSeed.Create(), GameConfig.MapTestModeEnabled)
```
- 🔴 **`NetworkGameManager` 를 `NetworkBehaviour` 로 승격하지 않았다**(§4-1 후보 C 탈락). **위임 진입점 하나만** 받는다.
- 🔴 **결말 통보는 「한 번만」과 「반드시 한 번은」을 동시에 지켜야 해서 깃발이 두 겹이다** — `NetworkMapTransfer._hostOutcomeNotified`(결말 자리 4곳이 수렴) · `NetworkGameManager._mapTransferGateSettled`. **실패 통보 뒤 성공 통보가 따라오면 「실패한 판인데 전투 씬으로 넘어간다」**(규칙 16 정면 위반). 반대로 아무 통보도 안 가면 **로비가 로딩 화면에서 영영 멈춘다**(실패 팝업이 §10 으로 빠졌으므로 로딩을 내리는 유일한 통로가 이 이벤트다).

**고정 seed 해제(제거-1)**: 임시 고정 seed 분기와 상수 `NetworkInterimRootSeed = 11UL` 를 **주석 비활성화**했다(삭제가 아니다 — 되돌리기가 「주석 두 군데를 푸는 것」으로 끝나게 하려는 의도). 🔴 **이 비활성화는 동작 변경이 아니라 죽은 코드 정리다** — 멀티에서는 `CreateRootSeed()` 자체가 불리지 않는다(Host 는 로비에서 뽑고 Client 는 받는다). 싱글 경로 계산식은 `Domain/Map/MapRootSeed.Create()` 로 **옮겨졌고 계산식은 무변경**이다(로비에도 같은 계산이 필요한데 복사하면 한쪽만 고쳐져 갈라진다).

**🔴 ✅ 실기 — 3단계의 주 판정이 닫혔다**
- **매 판 다른 seed 가 나왔다** — `3905793782056422338`(FullyOpen) / `10943744334431763949`(ObstacleOpen). **2단계 이래 「멀티에서 매 판 다른 맵이 나오는가」가 확인 불가였던 것이 여기서 처음 확인됐다.**
- **양쪽이 같은 맵**임을 해시로 확인했다(`HashMatch=True`) — 🔴 **2026-09-09 회차의 「눈으로 본 것이지 칸 단위 대조가 아니다」가 이번에 해소됐다.** 해시 대조는 **바이트 단위 대조**이므로 눈 대조보다 강하다.
- **게이트 통과** 2판 · **전송 관련 예외·경고 0건**.
- **싱글 회귀 없음** — `20:00:04`·`20:00:07`·`20:00:10` 세 판이 각기 다른 seed(FullyOpen / ThreeLane / Outer)로 정상 동작.

⚠️ **실기에서 확인되지 않은 것**: **「실패하면 전투 씬으로 안 넘어가고 로비에 머무는가」**. 2판 모두 성공이라 게이트가 **막는 쪽으로는 한 번도 발화하지 않았다.** §15 표 3번이 미확정인 것과 같은 뿌리다.

🔴 **범위 밖 수정 1줄을 함께 했다 — §16-라-2 에 적었다.** 게이트에 직결돼 불가피하다고 판단했으나 **범위를 넘은 것은 사실**이다.

---

## 16. ⏸ 3단계에서 새로 드러난 보류·미해결 항목 (2026-09-14 추가)

> **이 절을 2단계 Plan §13 에 붙이지 않고 여기에 새로 만든 이유**: 2단계 §13 은 제목 자체가 **「2026-09-08 시점 — 사용자가 이번에 처리하지 않기로 보류한 항목」**이라 **특정 시점의 스냅숏**이다. 3단계에서 드러난 것을 그 표에 섞으면 **제목이 시간에 대해 거짓말을 하게 된다.** 대신 2단계 §13 의 1·2번(이번 회차에 상태가 실제로 바뀐 두 항목)에는 그 자리에 갱신 표시를 덧붙였고, 표 끝에 이 절로 오는 포인터를 달았다.
>
> **아래는 「미해결 결함 목록」이 아니다.** (가)는 **설계대로 드러난 것**, (마)와 (바)는 **규칙이 미충족으로 남는다는 사실 기록**, (라)는 **사용자 확인 대기**, (나)·(다)가 실제로 손봐야 할 성격이다.
>
> **[🔴 2026-09-14 같은 날 추가 — 원문은 그대로 두고 덧붙인다]** 초기 골드와 맵 테스트 모드에 관해 **사용자가 답을 확정했고 메인 세션이 코드·에셋을 직접 실측**했다. 그 결과 **(마)의 성격이 바뀌었고**(「미적용」의 원인이 특정됐다) **(아)·(자) 두 항목이 늘었다.** (아)는 **사용자가 확정한 결정**이고 (자)는 **확인 결과 문제없음**이다. **번호는 재배열하지 않는다.**

| # | 항목 | 성격 |
|---|---|---|
| 가 | 재경기 맵이 구성되지 않는다 | **결함 아님 — 설계대로** · 다음 범위(§10-1) |
| 나 | 로비 씬에서 로그 파일이 안 써진다 | 🔴 **손봐야 함** — 맵 전송은 로비에서 일어난다 |
| 다 | 인게임 패널 백그라운드가 에디터에서만 안 나타난다 | **사용자 보고 · 미조사** |
| 라 | 구현 중 나온 판단 3건 | **사용자 확인 대기** (🔴 **2026-09-14: 3번 중 1건은 (아) 착수 시 자동 해소된다** — 아래) |
| 마 | 초기 골드 여전히 미적용 | **규칙 16 일부 미충족** (§11-1 그대로) · 🔴 **2026-09-14 원인 특정 — 아래 정정 블록** |
| 바 | B 단계 한도 실측 2건 미완 | **규칙 16 일부 미충족** (위 B 단계 상세) |
| **아** (2026-09-14 신설) | **맵 테스트 모드를 삭제한다** | 🔴 **사용자 확정 결정 · 다음 작업** — 이번 회차에는 **삭제하지 않고 기록만** 한다 |
| **자** (2026-09-14 신설) | 멀티 초기 골드가 Host/Client 로컬 설정으로 각각 만들어진다 | ✅ **확인 완료 · 문제없음** — 결함이 아니다 |

> **(사)의 자리에 대하여**: **`16-사. ※ 근거 구분` 은 §16 전체에 대한 메타 항목**이라 계속 **마지막에 가깝게** 둔다. 다만 **2026-09-14 에 추가된 (아)·(자)는 그 뒤에 온다** — 가나다 순서를 유지하면서 기존 항목의 글자를 바꾸지 않는 유일한 방법이기 때문이다. **(아)·(자)의 근거 구분은 각 항목 안에 따로 적었다.**

---

### 16-가. 재경기(Rematch) 맵이 구성되지 않는다 — 🔴 **설계대로 드러난 것이지 결함이 아니다**

**실기 로그 지문 (`19:59:44~45`)**
```
StartRematch: 동적 NetworkObject Despawn | ObjectName=NetworkMapTransfer(Clone)
StartRematch: Game 씬 재로드
[ERROR] 멀티플레이인데 로비에서 확정된 맵이 전투 씬으로 인계되지 않았다
        Event=MapPreparationFailed, Reason=MapHandoffEmpty
```

**왜 이렇게 되는가** — `NetworkGameEndController.StartRematch()`(`Infrastructure/Network/NetworkGameEndController.cs` **447~497행** — 문서 작업 중 직접 실측)는 **로비로 돌아가지 않고 Game 씬만 재로드**하며, 그 과정에서 동적 스폰 `NetworkObject` 를 전부 despawn 한다 — 거기에 `NetworkMapTransfer` 가 포함된다. 그리고 `MapHandoff` 는 **첫 경기에서 이미 비워졌다**(`TryTake` 는 읽고 비운다 — C 단계). 그래서 인계할 것이 없다.

🔴 **`GameBootstrapper` 가 지난 판 맵으로 조용히 때우지 않고 실패로 남긴 것이 규칙 16 이 의도한 동작이다.** 이 가드가 없었다면 **양쪽이 서로 다른 맵으로 재경기를 시작했을 것**이다 — 규칙 16 의 「로비에서 확정한 맵 데이터만 사용한다」가 막으려던 바로 그 사고다. **다음 사람이 버그로 접수하지 않도록 여기에 못 박아 둔다.**

**어디서 처리되는가**: **§10 표 1번(재경기 `SameMap`/`NewMap`) — 다음 범위**다. 그 항목에 **「실기에서 실제로 이 형태로 드러난다」는 사실과 위 로그 지문**을 근거로 덧붙인다. 🔴 **그리고 A 단계(NGO 씬 재로드 생존 조건)를 그때 실측해야 한다** — 위 「A 단계 상세」가 강등 사유로 든 것이 바로 이 자리다.

---

### 16-나. 🔴 로비 씬에서는 로그 파일이 써지지 않는다

`LogSessionOwner.EnsureInitialized()` 의 호출부는 **`LoginBootstrapper.Awake`(`Bootstrap/LoginBootstrapper.cs:121`) 와 `GameBootstrapper.Awake`(`Bootstrap/GameBootstrapper.cs:529`) 둘뿐**이다(문서 작업 중 전수 grep 으로 직접 실측). **로비 씬에는 부르는 곳이 없다.**

- 그래서 **`Lobby.unity` 에서 직접 Play 하면** sink 가 0개라 `GameLog` 가 **콘솔 폴백**(`Application/GameLog.cs:493` `if (_sinks.Count == 0) FallbackToConsole(...)` — 직접 실측)을 타고 **파일에는 아무것도 안 남는다.**
- 실제로 그 상태에서 `CloudSaveService` 초기화 예외가 **콘솔에만** 찍혔고, **맵 전송 로그도 파일에서 통째로 누락**됐다.
- **Login 씬부터 시작하면 정상**임이 재테스트로 확인됐다.

🔴 **그럼에도 이것이 문제인 이유: 맵 전송은 로비에서 일어나는 일이다.** 규칙 12(로그 필수 항목)와 규칙 16(*"불일치 내용을 기록한다"*)이 요구하는 기록이 **「로비 직접 Play」에서는 남지 않는 구조**다. 개발 중 로비만 띄워 전송을 시험하는 것이 가장 흔한 사용법이므로, **가장 필요한 회차에 로그가 없다.**

⚠️ **이 항목은 원인이 특정된 상태이고 코드 수정이 필요하다 — 이번 문서 작업의 범위가 아니라 보류로 둔다**(CLAUDE.md 규칙 6·11).

---

### 16-다. 인게임 패널 백그라운드가 에디터에서만 나타나지 않는다 — **사용자 보고 · 미조사**

**사용자 보고 그대로**: **실기기에서는 패널 백그라운드를 터치하면 UI 가 닫히는데, 에디터에서는 백그라운드가 나타나지 않는다.**

🔴 **재현 조건도 원인도 조사하지 않았다.** 맵 전송과 관계있는지조차 확인되지 않았다 — **추정하지 않고 보고 내용만 남긴다**(CLAUDE.md 규칙 10). 다음에 이 항목을 집을 때는 **조사부터** 필요하다.

---

### 16-라. 구현 중 나온 판단 3건 — **사용자 확인 대기**

**1. `MapRootSeed` 를 `Domain/Map/` 에 둔 것**
Domain 이 시계(`UtcNow`)와 `Guid` 를 읽는 것이 **「Domain 은 결정적이어야 한다」는 통념과 부딪힌다.** 채택 근거는 *결정적이어야 하는 것은 seed **이후**이고 이 파일이 그 경계다* 라는 것이며 **파일 머리말에 적혀 있다.** ⚠️ **`Application` 으로 옮기자는 판단도 성립한다** — 확정이 필요하다.

**2. 🔴 범위 밖 수정 1줄 — `NetworkMapTransfer.StartHostRound` 의 `PackageTooLarge` 분기에 `_hostIsProbe = isProbe;` 추가**
그 분기가 `_hostIsProbe` 를 갱신하지 않아 **지난 회차 값이 남았다.** 직전이 프로브였다면 **진짜 맵의 실패가 진단용(probe)으로 오인돼 게이트 통보가 가지 않고 로비가 로딩 화면에서 멈춘다**(프로브 회차는 일부러 게이트에 통보하지 않는다). **게이트에 직결돼 불가피하다고 판단했으나 범위를 넘은 것은 사실이다**(CLAUDE.md 규칙 6).

**3. 조합 루트 마찰이 1건 → 2건으로 늘었다**
`GameBootstrapper` 가 **유일한 의존성 조합 루트**라는 아키텍처 제약과 부딪히는 자리가 둘이 됐다.

| # | 자리 | 언제 생겼나 |
|---|---|---|
| 기존 | `NetworkMapTransfer` 가 `MapPreparationUseCase` 를 스스로 조립한다 | F 단계 (§15 표 2번으로 이미 예고돼 있었다) |
| **신규** | `NetworkGameManager.ReadMapTestModeEnabled()` 가 `Resources.Load<GameConfig>("Config/GameConfig")` 를 **직접 읽는다**(`NetworkGameManager.cs:1026~1028` — 문서 작업 중 직접 실측) | I 단계 |

**둘 다 원인은 같다 — 로비에는 `GameBootstrapper` 가 없다.** 예외를 두는 것인지, 전송 객체가 로비 한정 조합 루트를 겸하는 것인지 **사용자 확정이 필요하다**(§15 표 2번이 이미 세워 둔 질문이며, 이번에 **자리가 하나 더 늘었다**).

> **[✅ 2026-09-14 같은 날 추가 — 원문은 그대로 두고 덧붙인다]** 🔴 **위 표의 「신규」 1건은 아래 (아) 맵 테스트 모드 삭제를 착수하면 자동으로 해소된다.** `NetworkGameManager.ReadMapTestModeEnabled()` **자체가 사라지므로** 그 `Resources.Load<GameConfig>` 직접 읽기도 함께 사라진다. **즉 이 항목의 마찰은 2건에서 다시 1건으로 줄어든다.**
>
> ⚠️ **그러나 「기존」 1건(`NetworkMapTransfer` 가 `MapPreparationUseCase` 를 스스로 조립한다)은 그대로 남는다** — 원인인 *"로비에는 `GameBootstrapper` 가 없다"* 가 해소되는 것이 아니기 때문이다. **사용자 확정이 필요하다는 질문 자체는 살아 있다.**

---

### 16-마. 초기 골드는 여전히 미적용 — **규칙 16 이 일부 미충족으로 남는다**

로그에서 `InitialGold` 가 매 판 다르게 찍히지만(실기 실측 **500 · 600 · 200 · 300**) 실제 게임의 초기 골드는 **`BlueGold=5000, RedGold=5000` 으로 고정**이다(`19:59:25.110` 실측).

🔴 **§11-1 이 「넣지 않으면 규칙 16 이 미충족으로 남는다」고 적어 둔 그대로다.** 전송·해시·검증은 그 값을 **실어 나르기만 하고**, 규칙 16 의 *"양쪽은 그 값을 그대로 적용한다"* 는 뒷부분이 **비어 있다.** 3단계가 끝난 지금 **그 상태가 확정됐다.**

⚠️ **사용자가 3단계 범위 선택에서 이 항목을 고르지 않았고 「하지 말라」인지 「이번엔 아니다」인지 여전히 확정되지 않았다 — 범위에 넣지 않고 사실만 적는다**(CLAUDE.md 규칙 12).

> **[🔴 2026-09-14 같은 날 정정·보강 — 위 원문은 그대로 두고 덧붙인다]**
>
> 위 원문은 **「매 판 다른 `InitialGold` 가 찍히는데 실제는 5000 고정」**이라고만 적어, **5000 이 맵 테스트 모드 때문에 나온 값처럼 읽힐 수 있다. 그렇지 않다.** 아래 세 가지가 이번에 실측·확정됐다.
>
> **① 5000 은 버그가 아니라 확정된 개발 설정이다 — 출처는 `Economy.StartingGold` 이며 맵 테스트 모드가 아니다.**
>
> - 🔴 **사용자 확정(그대로 옮긴다)**: *"테스트 단계이기 때문에 5000 골드로 설정되어 있고, **출시에는 초기 골드를 조정할 것**이다. **현재 예정은 500이지만 밸런스에 따라 조정될 여지가 있다.** 현재 **맵 테스트 모드는 따로 사용하고 있지 않다.**"*
> - **실측 근거**(문서 작업 중 직접 재확인): `Assets/_Project/Resources/Config/GameConfig.asset` **29행 `_startingGold: 5000`** · **34행 `_mapTestModeEnabled: 0`** · **35행 `_testStartingGold: 5000`**. 소비 지점은 `Bootstrap/GameBootstrapper.Setup.cs` **444행 `_resource = new ResourceUseCase(_config.StartingGold);`** 하나다.
> - 실기 로그의 `MapPreparationSucceeded` 에도 **`TestMode=False`** 로 찍혔다. **즉 맵 테스트 모드는 꺼져 있었고, 5000 은 `Economy.StartingGold` 값 그대로다.**
> - 그러므로 이 항목은 **「5000 이 잘못 나온다」가 아니라 「맵이 정한 값을 쓰는 배선이 없다」**가 정확한 서술이다.
>
> **② 🔴 5000 이 서로 다른 두 필드에 우연히 같아서 생기는 함정이 있다.**
>
> `Economy.StartingGold`(=5000) 와 `TestStartingGold`(=5000) 는 **서로 다른 필드인데 값이 우연히 같다.** `Infrastructure/Config/GameConfig.cs` **160~162행** 주석이 이미 그렇게 경고하고 있다(문서 작업 중 직접 실측) — *"위쪽 Economy 의 StartingGold(현재 값 5000)와는 **다른 필드**다. **값이 우연히 같을 뿐**이며 … 한쪽을 고칠 때 다른 쪽이 따라 바뀐다고 가정하지 말 것."*
> - **그 결과**: **맵 테스트 모드를 켜도 화면상 골드는 5000 그대로**라 **효과가 관측되지 않는다.** 다음 사람이 **「테스트 모드가 동작하지 않는다」로 오해하기 가장 쉬운 자리**다.
> - 🔴 **그리고 출시 때 `Economy.StartingGold` 를 500 으로 바꾸는 순간 이 우연이 깨져 두 값이 갈라진다.** 그때부터는 테스트 모드를 켜고 끄는 것이 화면에서 보이게 된다. **지금 안 보이는 것은 값이 같아서일 뿐이다.**
>
> **③ 🔴 광산 수 ↔ 초기 골드 연동은 미구현이며, 추후 밸런싱과 함께 작업한다(사용자 결정).**
>
> - 🔴 **사용자 확정**: *"광산 수에 따른 초기 골드 연동은 추후 밸런싱과 함께 조정할 것이고 다시 작업하도록 하겠다."*
> - **실측 근거**: `MapPreparationResult.InitialGold` 를 **실제 게임 초기 골드로 소비하는 곳은 0곳**이다. `InitialGold` 가 등장하는 곳은 **생성기·검증기·코덱·폴백 템플릿·로그 문자열뿐**이며, `Bootstrap/` 안의 유일한 등장은 `GameBootstrapper.Map.cs` **713행 로그 문자열**(`", InitialGold=" + prepared.InitialGold +`)이다(문서 작업 중 전수 grep 으로 직접 재확인).
> - 규칙 3 의 표를 강제하는 것은 `Domain/Map/MapDefinitionValidator.cs` **270행 `GetExpectedInitialGold(int testModeFlag, int neutralMineCount)`** 다 — 정상 모드는 `NormalModeInitialGoldValues[광산 수]`(**광산 1개 700 · 2개 600 · 3개 500 · 4개 400 · 5개 300 · 6개 200**), 테스트 모드는 `TestModeInitialGold`(**5000**) 고정.
> - 즉 맵은 이 값을 **계산·검증·인코딩·전송·해시 대조까지 전부 마친 뒤 아무도 읽지 않는다.**
>
> 🔴 **다음 사람이 가장 오해하기 쉬운 지점 — 반드시 함께 읽을 것**
> > **출시 때 `Economy.StartingGold` 를 500 으로 바꾸는 것만으로는 규칙 3 이 지켜지지 않는다.** 그렇게 하면 광산이 1개든 6개든 **매 판 500 고정**이 된다. 규칙 3 의 표는 **광산 수에 따라 700~200 으로 달라지는 값**이므로, **광산 수 연동은 상수 하나를 바꾸는 일이 아니라 별개의 배선 작업**이다.
>
> **그래서 규칙 3 과 규칙 16 이 이 항목 때문에 미충족으로 남는다** — 규칙 16 의 *"양쪽은 그 값을 그대로 적용한다"* 와 규칙 3 의 **광산 수별 표**가 둘 다 비어 있다. **「범위에 넣지 않고 사실만 적는다」는 위 원문의 판단은 유효하나, 「하지 말라인지 이번엔 아니다인지 확정되지 않았다」는 부분은 해소됐다 — 사용자가 「추후 밸런싱과 함께 다시 작업한다」로 확정했다.**
>
> **※ 근거 구분(규칙 10)**: 사용자 확정 문장 2건은 **사용자 발언 그대로**이고, `GameConfig.asset` 세 필드 · `GameBootstrapper.Setup.cs:444` · `GameConfig.cs:160~162` 주석 · `GameBootstrapper.Map.cs:713` · `MapDefinitionValidator.cs:270` 과 표 값은 **문서 작업 중 직접 실측**, 실기 로그의 `TestMode=False` 는 **메인 세션이 로그 원본에서 읽어 전달한 값**이다.

---

### 16-바. B 단계의 한도 실측 2건이 남았다 — **규칙 16 이 일부 미충족으로 남는다**

① NGO RPC 한 번의 **실효** 페이로드 상한 ② **Relay 경유** 실효 MTU. 둘 다 **사용자 실기 2대로만** 잴 수 있고, 재는 수단(`RunTransferProbe` + 인스펙터 컨텍스트 메뉴)은 **코드에 이미 있다.**
- 그래서 조각 크기가 아직 **`IsChunkSizeMeasured = false` · `ProvisionalChunkSizeBytes = 1024`(근거 없음)** 다.
- 규칙 16 이 *"값은 구현 시 NGO 실측으로 확정하고 근거와 함께 TDD 에 기록한다"* 고 지시하므로, **실측·기록이 끝날 때까지 그 조항은 미충족**이다.
- ⚠️ **실전에서 조각이 항상 1개인 것은 이 미충족을 없애 주지 않는다.** 상세와 실측 절차는 위 「B 단계 상세」.

---

### 16-사. ※ 근거 구분 (CLAUDE.md 규칙 10)

이 절과 위 단계 상세의 수치는 출처가 셋으로 갈린다. **섞어 적으면 나중에 재검증할 자리를 못 찾는다.**

| 출처 | 해당 항목 |
|---|---|
| **사용자 실기 관찰** | 인게임 패널 백그라운드 보고(16-다) · 화면에서 본 경기 동작 |
| **메인 세션이 로그 원본·`mono` 실행으로 측정해 전달한 값** | 실기 2판의 표 전체(seed · 유형 · TotalBytes · ChunkCount · HashMatch · Send/Resend · 준비 시간) · Client 검증 로그 지문 · 재경기 로그 지문 · 초기 골드 5000 고정 · 싱글 3판 seed · `MapChunkAssembler` 79 단언 · D 방식 시드 1~5000 재측정 · **커밋 해시 4개**(규칙 5 로 git 을 실행할 수 없다) · 로비 `NetworkObject` 0개 / 기존 `NetworkBehaviour` 14개 · **프리팹 `Prefabs/Misc/NetworkMapTransfer.prefab` 과 그 `DefaultNetworkPrefabs.asset` 등재** |
| **문서 작업 중 직접 실측** | `LogEvent` 46개(enum 파싱) · 신설 파일 행수(`MapHandoff` 99 · `MapChunkAssembler` 369 · `MapVerificationUseCase` 583 · `MapRootSeed` 86 · `NetworkMapTransfer` **1607**) · `IsChunkSizeMeasured=false` · `ProvisionalChunkSizeBytes=1024` · `MaxSearchCombinationCount` 200 · `EnsureInitialized` 호출부 2곳(`LoginBootstrapper.cs:121` · `GameBootstrapper.cs:529`) · `GameLog.cs:493` 콘솔 폴백 · `StartRematch` 447~497행 · `ReadMapTestModeEnabled` 의 `Resources.Load`(`NetworkGameManager.cs:1026~1028`) · `LogRules.md` 1.5 개정 블록 |

⚠️ **인계값과 문서 작업 실측이 어긋난 2건 — 실측값으로 적고 여기에 대조를 남긴다**(인계 수치는 항상 재실측한다).

| 항목 | 인계값 | 문서 작업 중 실측 | 처리 |
|---|---|---|---|
| `NetworkMapTransfer.cs` 최종 행수 | 1500행 | **1607행** | **실측값을 썼다.** 인계값은 측정 시점이 달랐을 수 있다 |
| `StartRematch()` 범위 | `:447~492` | **`:447~497`** | **실측값을 썼다.** 시작 행은 일치한다 |

⚠️ **이 체크아웃에서 확인할 수 없었던 것 2건**(없다는 뜻이 아니라 **확인 불가**라는 뜻이다): ① `Assets/_Project/Prefabs/Misc/NetworkMapTransfer.prefab` ② `Resources/Config/DefaultNetworkPrefabs.asset` 의 그 프리팹 등재. 이 체크아웃에는 신설 `.cs` 들의 `.meta` 조차 없다(에이전트가 Unity 밖에서 만든 파일이라 그렇다) — **Unity 쪽 산출물이 이 작업 트리에 들어와 있지 않다.** 실기에서 스폰이 성공한 것(`NetworkObjectId=1`)이 **프리팹과 등재가 실제로 존재한다는 간접 증거**이므로 그렇게 읽는다.

---

### 16-아. 🔴 맵 테스트 모드를 삭제한다 — **사용자 확정 결정 · 다음 작업** (2026-09-14 신설)

> **이번 회차에는 삭제하지 않는다. 결정과 영향 범위를 기록만 한다.** 메인 세션이 영향 범위를 보고한 뒤 사용자가 그렇게 정했다.

🔴 **사용자 확정(그대로 옮긴다)**: *"맵 테스트 모드는 현재 특별히 사용하고 있지 않아서 불필요한 것으로 보인다. 관련 구현 내용 삭제하고 문서도 삭제하면 될 것 같다."*

**왜 지금까지 눈에 띄지 않았나** — 위 **16-마 ②** 가 답이다. `TestStartingGold`(5000) 가 `Economy.StartingGold`(5000) 와 **우연히 같아서 켜도 화면이 바뀌지 않는다.** 그래서 「쓰고 있지 않다」는 상태가 오래 유지될 수 있었다.

#### 영향 범위 — 착수 전에 반드시 통째로 읽을 것

**12개 파일에 걸쳐 있다**(문서 작업 중 직접 실측 — `Assets/_Project/Scripts` 전수 검색):

`Application/UseCases/MapPreparationUseCase.cs` · `Application/UseCases/MapVerificationUseCase.cs` · `Bootstrap/GameBootstrapper.Map.cs` · `Domain/Map/Generators/IMapArchetypeGenerator.cs` · `Domain/Map/Generators/MapArchetypeGeneratorBase.cs` · `Domain/Map/MapDefinition.cs` · `Domain/Map/MapDefinitionCodec.cs` · `Domain/Map/MapDefinitionValidator.cs` · `Domain/Map/MapFallbackTemplateFactory.cs` · `Infrastructure/Config/GameConfig.cs` · `Infrastructure/Network/NetworkGameManager.cs` · `Infrastructure/Network/NetworkMapTransfer.cs`

🔴 **단순 삭제가 아니다 — 아래 8가지가 따라온다.**

**① `TestModeFlag` 가 canonical 바이트 안에 있다 → 맵의 바이트 형식 자체가 바뀐다.**
`Domain/Map/MapDefinitionCodec.cs` **67행 `WriteInt32(buffer, def.TestModeFlag);`**(직접 실측). 필드 순서는 `MapVersion → RootSeed → MapType → Width → Height → Orientation → NeutralMineCount → **TestModeFlag** → InitialGold` 이므로, 빼면 그 뒤의 모든 바이트가 4바이트씩 앞당겨진다.

**② 따라서 `MapVersion` 을 `1 → 2` 로 올려야 한다.**
`Domain/Map/MapDefinition.cs` **96행 `public const int CurrentMapVersion = 1;`**(직접 실측). 그 자리의 주석이 *"형식이 바뀌면 이 값을 올린다. 지원하지 않는 버전의 데이터는 아예 해석하지 않고 실패 처리한다"* 고 이미 지시하고 있다.

**③ 폴백 템플릿 5개를 재생성해야 한다.**
`Assets/_Project/Resources/MapTemplates/MapTemplate_{FullyOpen,Canyon,ThreeLane,ObstacleOpen,Outer}.bytes` **5개 전부 존재**(직접 실측). **옛 형식으로 인코딩된 바이너리**이므로 형식이 바뀌면 그대로는 읽히지 않는다. 제작 도구는 2단계 F 에서 만든 `Assets/Editor/Tools/MapFallbackTemplateBuilder.cs` 다(실재 확인).

**④ 검증기 자체 점검에 테스트 모드 케이스가 들어 있다.**
`Domain/Map/MapDefinitionValidator.cs` — **N9b**(테스트 모드 표식만 켜면 6번 실패, **1532~1543행**) · **P9c**(테스트 모드 + 5000 은 광산 수와 무관하게 통과, **1545~1558행**) · **규칙 3 표 자체 대조 루프 안의 테스트 모드 갈래**(**1572~1576행**). 그리고 갈래 자체는 **270행 `GetExpectedInitialGold`** 안에 있다(직접 실측).

**⑤ D 방식 검증을 함께 고쳐야 한다.**
`Application/UseCases/MapVerificationUseCase.cs` 는 **받은 바이트에서 `TestModeFlag` 를 읽어 재생성한 정의에 다시 심은 뒤 Encode** 하는 구조다(**497행 · 518행**, 그리고 그 함정을 설명하는 주석이 **31행 · 308~311행 · 478행**에 있다 — 직접 실측). **코드와 주석을 함께 정리해야 한다.**

**⑥ `MapPreparationUseCase.Prepare(ulong rootSeed, bool mapTestModeEnabled)` 의 시그니처가 바뀐다.**
**302행**(직접 실측). 호출부는 자체 점검(**809~810행**) 외에 `NetworkMapTransfer` · `GameBootstrapper.Map` 쪽에 있다.

**⑦ 🔴 규칙 문서 개정이 선행돼야 한다 — 코드만 지우면 규칙과 코드가 어긋난다.**
**`GameSystemRules/GameSystemRules_RandomMap.md` 규칙 3 과 규칙 12 가 테스트 모드를 정의하고 있다.** ⚠️ **이번 회차에는 규칙 문서를 고치지 않는다**(사용자가 범위 밖으로 확정). **삭제 작업에 착수할 때 규칙 개정을 먼저 한다.**

**⑧ ⚠️ `MapVersion` 을 올리면 2026-09-14 에 끝낸 전송 실기 검증이 무효가 된다.**
해시 · 바이트 수(`319 + 4 × 중립 광산 수`) · D 방식 복원이 **전부 새 형식 기준으로 다시 계산**되므로 **멀티 실기 재검증이 필요하다.** 「이미 검증했으니 괜찮다」로 넘기면 안 된다.

**✅ 덤 — 자동으로 해소되는 것 1건**: `NetworkGameManager.ReadMapTestModeEnabled()` 가 사라지므로 **위 16-라 3번의 「조합 루트 마찰 2건」 중 신규 1건이 함께 없어진다**(기존 1건은 남는다 — 그 항목의 추가 블록 참조).

**※ 근거 구분(규칙 10)**: 사용자 확정 문장은 **사용자 발언 그대로**, 12개 파일 목록 · 위 행 번호 전부 · 템플릿 5개 실재 여부는 **문서 작업 중 직접 실측**이다.

⚠️ **인계값과 문서 작업 실측이 어긋난 1건 — 실측값으로 적고 대조를 남긴다**(인계 수치는 항상 재실측한다).

| 항목 | 인계값 | 문서 작업 중 실측 | 처리 |
|---|---|---|---|
| 테스트 모드 참조 **줄 수** | 89줄 | **115줄**(주석 포함) / **95줄**(주석 줄 제외) | **실측값을 썼다.** 파일 수 **12개는 인계값과 일치**한다. 세는 기준(주석 포함 여부·검색어)이 달랐던 것으로 보이나 **확정할 수 없어 추정하지 않는다** |

---

### 16-자. ✅ 멀티 초기 골드 불일치 — **확인했고 문제없음** (2026-09-14 신설)

**의심의 모양**: `_resource = new ResourceUseCase(_config.StartingGold);`(`Bootstrap/GameBootstrapper.Setup.cs` **444행**)가 **Host 와 Client 양쪽에서 각자의 로컬 설정으로** 만들어진다. 그러면 두 사람의 `GameConfig` 가 다를 때 **초기 골드가 서로 달라지는 것 아닌가**.

**✅ 결론 — 결함이 아니다. 서버 권위 동기화가 Host 값으로 수렴시킨다.**

`Infrastructure/Network/NetworkResourceSync.cs`(직접 실측):
- `NetworkVariable<int> _blueGold` / `_redGold` 가 **`readPerm: Everyone` · `writePerm: NetworkVariableWritePermission.Server`**(**53~62행**)다. **클라이언트는 쓸 수 없다.**
- **서버만** `GameEvents.OnResourceChanged` 를 구독해 `NetworkVariable` 을 갱신하고(**103행 `if (IsServer)`**), **클라이언트는 `OnValueChanged` 콜백에서 자기 로컬 `ResourceUseCase` 를 보정**한다(파일 머리 설명 **6~13행** · 콜백 등록 **112~113행**). 서버 자신은 콜백 안의 `IsServer` 검사로 건너뛴다.

즉 **클라이언트의 로컬 초기값이 무엇이든 첫 동기화에서 Host 값으로 덮인다.**

🔴 **「확인했고 문제없음」으로 남기는 이유**: 이 구조는 **코드를 읽어야만 보인다.** 생성 지점만 보면 양쪽이 각자 만드는 것으로 읽혀 **다음 사람이 같은 의심을 반복하기 쉽다.** 그래서 **의심의 모양과 해소 근거를 함께** 적어 둔다.

⚠️ **다만 이것이 16-마 를 해소하지는 않는다** — 수렴하는 값이 **`Economy.StartingGold`(5000)** 이지 **맵이 정한 값**이 아니기 때문이다. **두 문제는 별개다.**

**※ 근거 구분(규칙 10)**: 위 행 번호와 권한 설정은 전부 **문서 작업 중 직접 실측**이다.
