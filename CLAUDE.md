# Hexiege 프로젝트 절대 규칙

이 파일의 규칙은 **예외 없이** 모든 상황에서 적용됩니다.
"간단하니까", "자연스러운 다음 단계니까" 등의 이유로 생략 불가.

---

## 작업 시작 전 필수 체크리스트 (예외 없음, 순서대로)

어떤 작업이든 — 아무리 간단해도 — 아래 순서를 반드시 따른다.

**[ 1 ] `Assets/_Project/Docs/WORKFLOW.md` 읽기**
**[ 2 ] 사용자에게 task 문서 작성 여부 질문**: "task 문서(research.md / plan.md) 작성을 진행할까요?"
**[ 3 ] 에이전트 호출 시 `.claude/MEMORY.md` 내용을 프롬프트에 전달**

> ⚠️ 이 체크리스트를 건너뛰는 것은 그 자체로 규칙 위반이다.
> "간단한 작업", "한 줄 수정" 등 어떤 이유도 예외가 될 수 없다.

---

## 1. 승인 없이 파일 수정/생성 절대 금지

- 사용자가 명시적으로 요청한 파일만 생성/수정
- 요청하지 않은 파일을 "도움이 될 것 같아서" 추가 생성 금지
- **예시 위반**: research.md만 요청했는데 plan.md까지 생성

## 2. 승인 후 즉시 구현 금지

사용자가 "진행해" 등으로 승인 → **반드시 먼저 질문**:
> "task 문서(research.md / plan.md) 작성을 진행할까요?"

질문 없이 바로 Edit/Write/Agent 호출 절대 금지.

## 3. 코드/설계 작업은 반드시 전문 에이전트에게 위임

직접 코드를 수정하거나 설계를 결정하지 않음:
- 코드 구현/버그 수정 → **game-programmer** 에이전트
- 기획/밸런스 결정 → **game-design-lead** 에이전트
- 구현 후 검증 → **qa-tester** 에이전트
- 복합 작업 조율 → **project-orchestrator** 에이전트

## 4. 테스트 완료 후 반드시 업데이트 확인

사용자가 테스트 완료를 알리면 → **반드시 먼저 질문**:
> "문서/메모리 업데이트를 진행할까요?"

사용자가 다음 작업으로 넘어가려 해도 이 확인을 먼저 함.

## 5. git 명령 절대 금지

`git restore`, `git reset`, `git checkout`, `git commit`, `git push` 등
**사용자가 명시적으로 직접 언급하지 않는 한 어떤 git 명령도 실행 불가**.

이유: 2026-03-03, git restore 무단 실행으로 커밋되지 않은 작업 전체 삭제 → 복구 불가.

## 6. 작업 범위 초과 금지

요청된 작업의 범위를 넘어서 추가 개선/리팩토링/기능 추가 금지.
요청한 것만 정확하게 수행.
추가 개선 및 리팩토링은 문서 작성 전 사용자에게 제안하는것 권장.

## 7. 완성도 우선 원칙

**작업량보다 완성됐을 때의 완성도가 항상 우선**한다.

- 기존 코드를 많이 바꾸더라도 더 효율적인 구조가 있다면 그 방법을 채택
- "기존 코드와 맞지 않아서", "작업량이 많아서" 등의 이유로 더 나은 설계를 포기 금지
- 단, 더 나은 방법을 제안할 때는 반드시 이유와 장단점을 명확히 설명하고 사용자 승인 후 진행
- 이 원칙은 UI, 아키텍처, 알고리즘 등 모든 개발 영역에 적용

## 8. 주석은 상세하게 작성
- 유니티 초급 개발자도 쉽게 이해할 수 있는 주석 작성

## 9. MCP 호출은 사용자의 지시에 따라 진행
- MCP 호출은 사용자 지시 없이 사용하지 말 것

## 10. 추정하여 대답하지 말 것.
- 사용자에게 대답할때는 추정하여 대답하는 것이 아닌 '반드시' 근거를 가지고 답변할 것

## 11. 코드 수정 작업은 명시적인 지시가 없을 경우 허가를 받은 뒤에 진행
- 사용자가 명시적으로 코드 수정을 지시하지 않는 이상 반드시 코드 수정에 대한 허가를 받을 것

## 12. 판단이 충돌되거나 모호한 경우 사용자에게 확인할 것
- 작업을 진행하는데 있어서 충돌되는 경우 사용자에게 재확인 할 것
- 모호한 경우 스스로 판단하지 않고 사용자에게 재확인 할 것

## 13. task 문서(research.md / plan.md) 작성 시 서두에 자연어 설명 필수
- task 문서의 가장 첫 부분에 반드시 사용자가 이해하기 쉬운 자연어로 해당 작업의 목적과 내용을 설명할 것
- 기술 용어나 코드 중심의 나열이 아닌, 일반 언어로 "무엇을 왜 하는지"가 명확히 드러나도록 작성

## 14. 세션이 짤려서 흐름이 끊긴 경우 작업을 이어서 하기 전 사용자에게 확인 받을 것
- 세션이 짤려서 작업 흐름이  끊겨 어떤 작업을 해야하는지 모르는 경우 사용자에게 확인 받고 진행할 것
- 임의로 판단하여 진행 절대 금지
---

## 문서 시스템

- 전체 문서 인덱스 및 에이전트 역할 → `AGENTS.md` 참조
- 작업 사이클 상세 운영 규칙 → `Assets/_Project/Docs/WORKFLOW.md` 참조
- 문서를 수정했으면 리포지토리 루트에서 `python3 Tools/check_docs.py`(읽기 전용 정합성 검사기)를 실행해 0건을 확인 → 절차는 `WORKFLOW.md` [11] 참조

**[7]~[12] 중 하나라도 빠지면 사이클 미완료.**
